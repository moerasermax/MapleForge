# MapleForge — 任務追蹤清單

> 本清單是**唯一真相來源（single source of truth）**：你用來追蹤進度、我用來執行。
> 對齊 `重構架構設計書.md` v0.2（§0.5/§7 為準）。建立 2026-05-31。

## 如何使用
- 每個任務一行，勾選框即狀態：`- [ ]` 未完成 / `- [x]` 完成。
- 進行中／阻塞用狀態標記：🟦 進行中、🟥 阻塞（後面註明卡在什麼）。
- 欄位：**ID** ｜ 標題 ｜ **DoD**＝驗收標準（達到才算完成）｜ **依賴** ｜ **大小**(S<1天 / M 1–3天 / L 3天+)。
- 改動規則：開始一個任務→標 🟦；完成→打勾並更新儀表板；卡住→標 🟥 並寫原因。
- **追蹤三層**：①本清單＝狀態真相來源 ②每完成一任務 `git commit` 訊息**帶任務 ID**（如 `M1-3: AES-OFB cipher 通過黃金測試`）＝可驗證帳本 ③`進度日誌.md`＝敘事紀錄。三者由 AI 在交付時順手同步，作者不需額外維護。

---

## 📊 進度儀表板

| 階段 | 目標 | 任務數 | 完成 | 狀態 |
|---|---|---|---|---|
| **X 基礎建設** | 橫切支撐（repo、測試、神諭、稽核） | 7 | 6 | 🟦 進行中（X2✅ conventions.md；X7✅ session invariants；X6 待做） |
| **M0 骨架** | 空 server 能啟動、listening | 5 | 5 | ✅ 完成 |
| **M1 管線打通** ★ | 客戶端連上→回「登入失敗」 | 8 | 8 | ✅ 完成（真客戶端封包被解密！） |
| **M2 登入成功** | 登入成功、建角選角 | 6 | 6 | ✅ 完成（M2-R backlog ✅ 完成） |
| **M2.5 客戶端啟動器** | 兩個登入畫面同時開 | 3 | 0 | 🟥 阻塞（需使用者在場/Approval Queue） |
| **M3 進地圖＋抽象** ★ | 進圖、走動、提取版本抽象 | 8 | 7 | 🟦 進行中（M3-1~7 完成；CHANGE_MAP ✅ 真機 GUI PASS(2026-06-19)；M3-8 未做） |
| **M4 戰鬥** | 打怪、掉寶、背包 | 6 | 0 | 🟦 進行中（batch-5 八路含 item-use 已入 master，但僅 headless/單元驗證；DoD 待 #12 真機 smoke） |
| **M5 腳本＋多實例** | NPC 可用、同機跑兩個伺服器 | 7 | 3 | 🟦 進行中（M5-1~3 完成；商店/倉庫/任務 API 等已入 master 待真機 smoke；多實例未啟用） |
| **M6+ 進階／真隔離** | 商城、組隊、多進程 | 6 | 0 | 🟦 進行中（商城/社交/技能等批次移植已入 master，完整 DoD 與 live 驗證未過） |
| **合計** | | **56** | **35** | 63%（以原任務 DoD 計；headless-only 批次不列完成） |

> ★ = 關鍵驗證里程碑。X 與各 M 並行（X 是支撐，不是前置階段）。

---

## X — 基礎建設 / 橫切（與各里程碑並行）

- [x] **X1** 建立新 server 的 git repo＋`.gitignore`（與舊 Java 專案分離） — **DoD**：`git init` 完成、第一個 commit、忽略 bin/obj/wz/logs ｜ 依賴：— ｜ S ｜ ✅ `MapleForge/`、commit 39100f4
- [x] **X2** 專案結構與命名規範文件（連結設計書、定 namespace/資料夾約定） — **DoD**：`docs/conventions.md` 存在、團隊（你＋AI）有共識 ｜ 依賴：— ｜ S ｜ ✅ `docs/conventions.md`（namespace職責/資料夾/命名/依賴方向/MF0001）
- [x] **X3** Roslyn Analyzer 專案 `MapleForge.Analyzers`（禁 `Core`/`Application` static 可變欄位） — **DoD**：違規會編譯失敗；第三方在白名單 ｜ 依賴：M0-1 ｜ M ｜ ✅ MF0001(Error)：偵測 static 可變欄位/屬性；const/readonly/get-only 豁免；接入 Core+Application；現有代碼零違規(commit 311054d)
- [x] **X4** 黃金測試框架 — **DoD**：能對固定輸入產生 byte 級對照樣本 ｜ 依賴：— ｜ M ｜ ✅ 改用**舊 Java 預言機**（`tools/oracle/GoldenVectors.java` 跑舊 `MapleAESOFB.class`）→ 比真封包更穩、全自動；真封包擷取留待更高層封包時選配
- [x] **X5** 「行為神諭」：從舊 Java 抽出 v113 規格筆記（`MapleAESOFB`/`LoginPacket`/opcode properties/`MapleServerHandler`） — **DoD**：cipher 算法、IV 規則、login 封包結構、關鍵 opcode 數值整理成文件 ｜ 依賴：— ｜ M ｜ ✅ `MapleForge/docs/v113-protocol-spec.md`（含 AES key/funnyBytes/getHello/opcodes）
- [ ] **X6** 第三方套件 static 審核清單（EF Core model cache、WZ lib、Serilog） — **DoD**：每個外部依賴的 process 級狀態被記錄＋對策 ｜ 依賴：— ｜ S
- [x] **X7** 新 session 常駐鐵律與方法論融合入口 — **DoD**：agent 入口、不可變流程鐵律、姊妹專案取經融合綱領完成並接入文件索引 ｜ 依賴：`docs/devlog/取經/取經總報告.md` ｜ S ｜ ✅ `AGENTS.md` + `docs/specs/session-invariants.md` + `docs/design/MapleForge方法論融合綱領.md`（2026-06-09）

---

## M0 — Solution 骨架
**目標**：一個能啟動、listening、讀得到設定的空伺服器。**驗收**：`dotnet run` → 監聽 loginPort、log 正常、讀得到 appsettings。

- [x] **M0-1** 建 .NET **10** Solution（`.slnx`）＋分層專案空殼（`Core`/`Application`/`Net`/`Persistence`/`Content`/`Scripting`/`Versioning`/`Adapters.V113`/`Host.Login`/`Host.Channel`/`Host.Shared`）＋專案參考方向（依賴由外往內） — **DoD**：`dotnet build` 綠燈、依賴方向正確 ｜ 依賴：X1 ｜ M ｜ ✅ 11 專案、建置 0 警告 0 錯誤
- [x] **M0-2** Generic Host 啟動裝配（`Host.CreateApplicationBuilder`、DI、生命週期、優雅關閉） — **DoD**：Host 能 Start/Stop ｜ 依賴：M0-1 ｜ S ｜ ✅ `MapleServerHost.AddMapleServerInstance`
- [x] **M0-3** 強型別設定：`appsettings.json`＋`ServerInstanceOptions`（**單一實例**：name/version/loginPort/db/rates）取代 `settings.ini` — **DoD**：設定能注入、改值生效 ｜ 依賴：M0-2 ｜ S ｜ ✅ 驗證讀到「爸爸的楓之谷」/8484
- [x] **M0-4** logging（MEL 內建 console）＋實例名稱標記 — **DoD**：log 有結構化欄位與實例標記 ｜ 依賴：M0-2 ｜ S ｜ ✅ log 帶 `[爸爸的楓之谷]`
- [x] **M0-5** 空 `TcpLoginListener` stub：開 TCP listener 聽 loginPort、accept 連線、log、**不解碼** — **DoD**：客戶端能連上、server log 印出連線 ｜ 依賴：M0-2/M0-3 ｜ S ｜ ✅ port 8484 探測 True、連線被記錄

---

## M1 — 管線打通 ★
**目標**：真 v113 客戶端連上、輸入帳密、**收到硬編的「登入失敗」**＝證明 cipher＋opcode＋framing 整條管線正確。**驗收**：客戶端顯示登入失敗（非逾時/斷線）。

- [x] **M1-1** Net 層：TCP server＋`MapleSession`（每連線生命週期、send 鎖） — **DoD**：穩定收發 raw bytes ｜ 依賴：M0 ｜ M ｜ ✅ `MapleSession`（M1 用 NetworkStream+ReadExactly；Pipelines 為日後優化）
- [x] **M1-2** v113 封包框架：4-byte 加密長度頭的讀/寫與分包 — **DoD**：能正確切出完整封包 ｜ 依賴：M1-1 ｜ M ｜ ✅ 收：header→Check→ReadLength→body→Crypt；送：WriteHeader+Crypt
- [x] **M1-3** v113 cipher 硬編：AES-OFB＋shuffle，實作 `IVersionCipherFactory.CreateSessionPair(recvIv,sendIv)→(recv,send)` — **DoD**：每連線每方向各一 cipher 實例 ｜ 依賴：X5 ｜ L ｜ ✅ `Adapters.V113/Crypto/`（MapleAesOfb+factory+constants）
- [x] **M1-4** cipher 黃金測試 — **DoD**：與真值逐 byte 相符 ｜ 依賴：X4、M1-3 ｜ M ｜ ✅ **5 項黃金向量 vs Java 預言機 byte 級相符**（NextIv/收送header/crypt/跨塊邊界/IV演化）＋15 項結構測試＝20 全綠。bit 級正確性已關閉盲點
- [x] **M1-5** 握手封包（getHello：未加密 `[len][version][patch][recvIv][sendIv][locale]`，送出後啟用雙向 cipher） — **DoD**：客戶端接受握手、不立刻斷線 ｜ 依賴：M1-3 ｜ M ｜ ✅ `V113LoginPackets.Hello`
- [x] **M1-6** 硬編 opcode 表（`V113RecvOp`/`V113SendOp`）＋登入請求封包識別 — **DoD**：能解出/路由客戶端 opcode ｜ 依賴：X5、M1-5 ｜ M ｜ ✅ 解密後讀 LE opcode、路由 LOGIN_PASSWORD
- [x] **M1-7** 回硬編「登入失敗」封包 — **DoD**：回 LOGIN_STATUS+reason ｜ 依賴：M1-6 ｜ S ｜ ✅ `V113LoginPackets.LoginFailed(5)`；**L3 整合測試驗證端到端**
- [x] **M1-8** 真客戶端 live 驗證（L4，超越原 smoke 計畫） — **DoD**：真客戶端連上、我方握手被接受、**server 解密到真客戶端加密封包** ｜ 依賴：M1-5 ｜ M ｜ ✅ **真 v113 客戶端連上 → server log `解密封包 opcode=0x17 len=2`＝終極 bit 級證明**。過程修了 `ijl15.dll`(缺檔)、用 WINXPSP3+16BITCOLOR 解 Gr2D、ESC 啟動。視窗化(dgVoodoo2 wrapper)被 Themida 擋(已還原)，詳見進度日誌

---

## M2 — 登入成功
**目標**：接 DB、真正驗證帳密、進到角色選單。**驗收**：登入成功、看到角色列表、能建角選角。

- [x] **M2-1** **LiteDB 持久層**（文件模型）：`Maple.Persistence` 接 LiteDB 5.0.21、`MapleDatabaseOptions`(每實例 .db 路徑)、`AddMaplePersistence` DI、Repository 介面在 Core — **DoD**：DI 註冊、build 綠 ｜ 依賴：M0-3 ｜ M ｜ ✅ 委派 Sonnet、Opus 驗證(commit a5c7940)。尚未接進 host(M2-4 接)
- [x] **M2-2** Account 文件 + `IAccountRepository`：`Maple.Core/Accounts` 文件結構 + `LiteDbAccountRepository`(collection+索引) — **DoD**：能存取 account 文件 ｜ 依賴：M2-1 ｜ S ｜ ✅ 委派 Sonnet
- [x] **M2-3** 帳密驗證邏輯：`IPasswordHasher`+`BcryptPasswordHasher`(BCrypt.Net-Next)、`AuthService.AuthenticateAsync(autoRegister)` — **DoD**：回成功/失敗、autoRegister 建帳 ｜ 依賴：M2-2 ｜ M ｜ ✅ 委派 Sonnet、11 單元測試綠
- [x] **M2-4** 登入成功封包＋世界/頻道列表封包 — **DoD**：客戶端進到選伺服器/頻道畫面 ｜ 依賴：M2-3、M1-7 ｜ M ｜ ✅ 登入成功+SERVERLIST+EndOfServerList+SERVERSTATUS+CHARLIST(0角色stub) 全實作；V113LoginOptions 加 WorldName/ChannelCount/CharSlots；33→34 測試綠（含 Serverlist+Charlist L3 整合測試）。**✅ live 已驗(2026-06-02)**：修 AuthSuccess 彈回(登入後須主動連送 ServerList+EndOfServerList,漏送→互等→彈回)→真客戶端登入成功後**前進到「選擇伺服器/頻道」畫面、世界「雪吉拉」**,封包層+視覺層雙驗證,全程未翻 Java。詳見任務歷程 `2026-06-02_01`
- [x] **M2-R** 處理 Codex review 發現（backlog）：①autoRegister race→TryAddAsync(commit 3c624ef) ②多實例DI單例→明確延M5 ③Account.Id LiteDB語意→接受tech debt(scale合理) ④帳號名正規化lowercase(commit 3c624ef) ⑤CancellationToken→LiteDB同步文件化、並發測試延backlog — 依賴：— ｜ M ｜ ✅
- [x] **M2-5** 角色資料 schema＋角色列表封包＋角色建立 — **DoD**：能建角、選角、進角色選單 ｜ 依賴：M2-4 ｜ L ｜ ✅ Character/CharacterStats/EquipEntry+ICharacterRepository (Core)、LiteDbCharacterRepository (Persistence)、CharacterService (App)、V113CharacterPackets(addCharStats/addCharLook/addCharEntry/CharNameResponse/AddNewCharEntry/ServerIp)、handler 全流程(LoginContext per-connection、CHECK_CHAR_NAME/CREATE_CHAR/CHAR_SELECT)；PacketReader 補 bounds check；35 測試全綠 (commit a01095d)。CHAR_SELECT → SERVER_IP stub(ch:8585)送出。**✅ live 已驗(2026-06-02,headless)**：CharList「TestHero」charId=1→SelectChar→ServerIp 8585 全鏈正確（修 DB 路徑發散+seed gender gate bug,詳見任務歷程 `2026-06-02_02`）
- [x] **M2-6** 接入 Roslyn Analyzer（X3）守 `Core`/`Application` — **DoD**：CI/build 會擋 static 違規 ｜ 依賴：X3 ｜ S ｜ ✅ 同 X3，Maple.Core.csproj + Maple.Application.csproj Analyzer 接入完成

---

## M2.5 — 客戶端啟動器（多開 workstream）
**目標**：解決「雙伺服器 demo 的客戶端那一半」。**驗收**：兩個客戶端同時開著、各自連不同 loginPort。

- [ ] **M2.5-1** 驗證 `dxwnd`/單人測試登入器 的 mutex bypass 能否雙開 — **DoD**：確認兩個 MapleStory.exe 能同時跑 ｜ 依賴：— ｜ M
- [ ] **M2.5-2** 客戶端連線目標 patch / launcher proxy（指向不同 IP:port） — **DoD**：可讓 client 連到指定 loginPort ｜ 依賴：M2.5-1 ｜ M
- [ ] **M2.5-3** 雙客戶端各連一個伺服器實例 — **DoD**：兩登入畫面同時、互不干擾 ｜ 依賴：M2.5-2、M2 ｜ S

---

## M3 — 進地圖＋抽象提取 ★
**目標**：進地圖、走動；**手上有真封包後才提取版本抽象接縫**。**驗收**：角色進圖、能走、被其他客戶端看到。

- [x] **M3-1** C# 二進位 WZ reader：目錄/property 解析 — **DoD**：能讀出 WZ 樹狀結構 ｜ 依賴：M0 ｜ L ｜ ✅ `Maple.Content/Wz/WzModel.cs`：WzFile.Open+ParseRootDirectory+ReadDirectoryEntries；2 L2 測試(String.wz/List.wz 有 children)綠
- [x] **M3-2** WZ：加密字串解碼＋版本 hash — **DoD**：字串/版本正確解出 ｜ 依賴：M3-1 ｜ M ｜ ✅ DecodeAsciiString(XOR 0xAA++)、DecodeUnicodeString(XOR 0xAAAA)、GetVersionHash/EncryptVersion 掃描 v0~999 自動檢測
- [x] **M3-3** WZ：PNG/canvas 解析 — **DoD**：能取出圖像/座標資料 ｜ 依賴：M3-1 ｜ M ｜ ✅ WzCanvasRef(dataOffset+dataLength skip)、WzVector(x,y)、WzSoundRef；伺服器僅需座標資料(已完整)；PNG 解壓(zlib)伺服器不需要，延 M4+
- [x] **M3-4** `IDataProvider` 抽象＋WZ 快取（`FileShare.Read`；**快取所有權決策**：process 級唯讀單例 vs 每實例） — **DoD**：核心透過介面取資料、所有權寫進文件 ｜ 依賴：M3-2 ｜ M ｜ ✅ IDataProvider/IDataNode(Core/Data)；WzDataProvider(process-級ConcurrentDict快取)；WzDataNode(adapter)；決策=process級唯讀單例(無運行時WZ變動)；4測試(GetAt/快取/null)綠(commit d510db8)
- [x] **M3-5** Channel host＋進場封包（spawn player into map） — **DoD**：角色出現在地圖 ｜ 依賴：M2-5、M3-4 ｜ M ｜ ✅ TcpChannelListener(port 8585)+IChannelConnectionHandler+V113ChannelOpcodes(PLAYER_LOGGEDIN=0x07,SET_FIELD=0x7B)+V113ChannelPackets.SetField(完整addCharacterInfo格式,含stats/inventory/skills/quests/rocks/monsterbook)+V113ChannelConnectionHandler；DI接入MapleServerHost；44測試綠(commit f420980)。**✅ live 已驗(2026-06-02,headless)**：PlayerLoggedIn(1)→SetField len308→角色 TestHero 進入地圖 100000000(server log `已進入地圖`)。詳見任務歷程 `2026-06-02_02`
- [x] **M3-6** 地圖載入（portals/foothold/spawn 從 WZ） — **DoD**：地圖元素正確載入 ｜ 依賴：M3-4 ｜ M ｜ ✅ MapData/MapPortal/MapFoothold(Core)；MapService.LoadMap(Application)；WZ子目錄遞迴讀取修正(重大bug fix)；Henesys 100000000:31portal+footholds+spawn點全驗證(commit b31ff79)
- [x] **M3-7** 移動封包解析＋廣播（對照舊 `MovementParse`） — **DoD**：自己走動、別人看得到 ｜ 依賴：M3-5 ｜ M ｜ ✅ IMapSessionRegistry+InMemoryMapSessionRegistry(Application)；V113MapPackets(SPAWN_PLAYER簡化版/REMOVE_PLAYER/MOVE_PLAYER廣播)；V113ChannelConnectionHandler接管廣播流程(進入→通知現有玩家/接收現有玩家Spawn→MOVE_PLAYER→廣播→離開通知)；live驗證待使用者(commit afe6e28)
### in-game 基礎移植（參照 Java 完整移植，見 `docs/design/Java移植路線圖.md` ①）
- [x] **移動**（移植 `MovementParse`）— V113MovementParser + handler 整合(Core Player.MoveTo 追蹤位置 + 原始 blob 廣播)。任務歷程 `2026-06-02_08`(commit 5c3898b)
- [x] **一般地圖聊天**（移植 `ChatHandler.GeneralChat`）— GENERAL_CHAT(0x2A)→CHATTEXT(0x9B) 送自己+廣播。任務歷程 `2026-06-02_09`(commit 9355b38)
- [x] **地圖物件同步 NPC**（移植 `MapleMapFactory.loadLife`+`spawnNPC`/`spawnNPCRequestController`）— 進場對該地圖每個 !Hide && id<9901000 NPC 送 SpawnNpc(0xF9)+RequestController(0xFB)；逐位元組對齊真 Java；Adapters.V113 38 / Application 28 綠。任務歷程 `2026-06-02_10`(commit 4d39f81)
- [x] **真客戶端進圖 NPC smoke**（里程碑驗證）— diag13 進 Henesys，三判準全過：server log `送出 26 個 NPC spawn` + AI 團隊雙人視覺確認 NPC(小幫手/計程車)渲染 + client 24s 續留；c→s 同步錄到。任務歷程 `2026-06-02_11`
- [x] **玩家人氣 GIVE_FAME(0x59)**（移植 `PlayersHandler.GiveFame`）— `FameService` 實作每日一次/同目標 30 天節流、同圖/等級/自我檢查；v113 `FAME_RESPONSE(0x24)` 成功/錯誤/被加人氣通知封包接上 Channel handler；Application 6 測試 + Adapters 4 測試 + Host build 綠。任務歷程 `2026-06-09_03`；待真機人氣 UI smoke。
- [x] **玩家表情 FACE_EXPRESSION(0x2C)**（移植 `PlayerHandler.ChangeEmotion`）— v113 `FACIAL_EXPRESSION(0xB9)` 封包 `[charId][expression]` 與 Channel 廣播接上；Adapters FaceExpression 2 測試 + Adapters 全測 119/119 + Host build 綠。任務歷程 `2026-06-09_04`；待真機表情 UI smoke，現金表情持有檢查待完整道具/現金道具系統。
- [x] **玩家椅子 USE_CHAIR(0x23) / CANCEL_CHAIR(0x22)**（移植 `PlayerHandler.UseChair/CancelChair` 主幹）— Core `Player` runtime chair state；v113 `SHOW_CHAIR(0xBD)` / `CANCEL_CHAIR(0xC6)` 封包與 Channel handler 接上；Core Chair 3 測試 + Adapters Chair 4 測試 + Core/Adapters 全測 + Host build 綠。任務歷程 `2026-06-09_05`；待真機椅子 UI smoke，釣魚椅/飛行椅/mount buff 特殊語義待補。
- [x] **玩家道具效果 USE_ITEMEFFECT(0x2D) / CANCEL_ITEM_EFFECT(0x43)**（移植 `PlayerHandler.UseItemEffect` 主幹）— Core `Player` runtime item effect state；v113 `SHOW_ITEM_EFFECT(0xBA)` 封包與 Channel handler 接上；Core ItemEffect 2 測試 + Adapters ItemEffect 3 測試 + Core/Adapters 全測 + Host build 綠。任務歷程 `2026-06-09_06`；待真機道具效果 UI smoke，取消效果 itemId=0 清除語義待確認。
- [x] **玩家鍵位 CHANGE_KEYMAP(0x7F) / KEYMAP(0x163)**（移植 `PlayerHandler.ChangeKeymap` 一般分支 + `MapleKeyLayout.writeData`）— Core `Character.Keymap` + `KeyBindingRecord`；Application 建角套 Java 預設 keymap；Channel 登入後送 `KEYMAP`，收到一般鍵位變更後持久化角色文件；Core Keymap 3 測試 + Adapters Keymap 4 測試 + Application Keymap 1 測試通過。任務歷程 `2026-06-09_07`；待真機鍵位 UI smoke，pet auto-pot 短分支已於 `2026-06-10_05` 補上資料保存，實際施放待寵物系統。
- [x] **玩家技能宏 SKILL_MACRO(0x68/0x7A)**（移植 `PlayerHandler.ChangeSkillMacro` + `MaplePacketCreator.getMacros`）— Core `Character.SkillMacros` + `SkillMacroRecord`；Channel 登入後有 macro 才送 `SKILL_MACRO`，收到變更後持久化角色文件；Core SkillMacro 4 測試 + Adapters SkillMacro 5 測試通過。任務歷程 `2026-06-09_08`；待真機技能宏 UI smoke。
- [x] **角色資訊 CHAR_INFO_REQUEST(0x5B) / CHAR_INFO(0x36)**（移植 `PlayerHandler.CharInfoRequest` 基礎入口 + `MaplePacketCreator.charInfo` 骨架）— Channel 只回同地圖目標，先 `EnableActions` 再送等級/職業/人氣/公會名稱與空的 Pet/Mount/Wishlist/MonsterBook/Medal 段；Adapters CharacterInfo 3 測試 + Adapters 全測 138/138 + Host build 綠。任務歷程 `2026-06-10_01`；待真機角色資訊 UI smoke，MonsterBook/Pet/Mount/勳章/婚姻/家族完整段待後續系統。
- [x] **怪物書封面 MONSTER_BOOK_COVER(0x32) / MONSTERBOOK_CHANGE_COVER(0x4E)**（移植 `PlayerHandler.ChangeMonsterBookCover` + `MonsterBookPacket.changeCover`）— Core `Character.MonsterBookCover`；Channel 驗 `0` 或 `238xxxx` 後持久化並回 change-cover；`SET_FIELD` MonsterBookInfo 寫 cover itemId；Core MonsterBookCover 3 測試 + Adapters MonsterBook 4 測試 + Core/Adapters 全測 + Host build 綠。任務歷程 `2026-06-10_02`；待完整 MonsterBook cards 與 card→mobId catalog。
- [x] **角色資訊更新 UPDATE_CHAR_INFO(0x97)**（移植 `PlayersHandler.UpdateCharInfo`）— Core 保存個人訊息、資訊頁表情、血型、生日、星座；Channel handler 解析 type 0/1/2 後持久化；`CHAR_INFO` 改寫實際欄位。Core/Adapters 全測通過。任務歷程 `2026-06-10_03`；待真機角色資訊 UI smoke。
- [x] **黑板關閉 CLOSE_CHALKBOARD(0x2B) / CHALKBOARD(0x9C)**（移植 `setChalkboard(null)` 清除分支）— Core `Player` runtime-only 黑板文字；Channel 關閉時清除並送自己+同圖其他玩家。Adapters 全測通過。任務歷程 `2026-06-10_04`；開黑板依賴後續 `USE_CASH_ITEM` 現金道具分支。
- [x] **寵物自動補藥 KEYMAP short branch**（補齊 `PlayerHandler.ChangeKeymap` 短封包分支）— `CHANGE_KEYMAP` 短封包保留 type/data，Core 保存 HP/MP 自動補藥 item id。Core/Adapters 全測通過。任務歷程 `2026-06-10_05`；實際自動補藥施放待寵物系統。
- [x] **傳送石地圖清單 SET_FIELD AddRocksInfo**（移植 `PacketHelper.addRocksInfo`）— Core 保存普通 5 格/VIP 10 格，空值 `999999999`；`SET_FIELD` 修正普通 5 格再 VIP 10 格。Core/Adapters 全測通過。任務歷程 `2026-06-10_06`；待真機傳送石 UI smoke。
- [x] **傳送石新增刪除 TROCK_ADD_MAP(0x60) / MAP_TRANSFER_RESULT(0x27)**（移植 `PlayerHandler.TrockAddMap` + `MTSCSPacket.getTrockRefresh`）— Channel 支援普通/VIP 新增目前地圖、刪除指定地圖並回 refresh。Adapters 全測通過。任務歷程 `2026-06-10_07`；`FieldLimitType.VipRock` 尚未移植，特殊 field limit 待後續地圖限制系統。
- [x] **玩家受傷 TAKE_DAMAGE(0x29) / DAMAGE_PLAYER(0xB8)**（移植 `PlayerHandler.TakeDamage` 主幹 + `MaplePacketCreator.damagePlayer` 無反傷 layout）— Channel 驗傷害/怪物 oid、扣 HP、回 `UPDATE_STATS(HP)` 並廣播受傷動畫。Adapters 封包測試 + Core/Application/Adapters 全測 + Host build 綠。任務歷程 `2026-06-10_08`；反傷/fake dodge/Power Guard 與真機受傷 smoke 待後續。
- [x] **丟楓幣 MESO_DROP(0x58)**（移植 `PlayerHandler.DropMeso` 主幹）— `DropService.TryDropMeso` 支援 10~50000 楓幣、餘額檢查、扣款與 player-drop 型楓幣掉落；Channel 回 `UPDATE_STATS(MESO)` 並廣播 `DROP_ITEM_FROM_MAPOBJECT`。Application/Adapters 測試通過。任務歷程 `2026-06-10_09`；地形落點/拾取權限時間與真機 drop smoke 待後續。
- [x] **同圖內部傳點 USE_INNER_PORTAL(0x5F) / CURRENT_MAP_WARP(0xC8)**（移植 `PlayerHandler.InnerPortal` + `instantMapWarp` 主幹）— 解析 portal name/x/y，驗當前地圖 portal 存在後更新玩家 runtime 位置並回 warp 封包。Adapters 測試通過。任務歷程 `2026-06-10_10`；距離反作弊與 `CHANGE_MAP_SPECIAL(0x5E)` script portal 仍待移植。
- [x] **傳送門 USE_DOOR(0x7D)**（移植 `PlayersHandler.UseDoor` + `MapleDoor.warp` 使用既有 door 主幹）— Core `Door`、Application `DoorService`、Adapters parser/handler/packets 完成；handler 回傳 warp decision，不接 central dispatch。Core/Application/Adapters 測試通過。任務歷程 `2026-06-18_03`；Mystic Door 建立待 `SPECIAL_MOVE(0x55)`，真機 smoke 待接線後驗。
- [x] **背包聚集 ITEM_GATHER(0x40)**（移植 `InventoryHandler.ItemGather` 主幹）— Core `Inventory.GatherByItemId` 依 itemId 重新塞回前方 slot；Channel 更新背包快照、持久化並送 Java 對應完成封包。Core/Adapters 測試通過。任務歷程 `2026-06-10_11`；cash/pet 特殊排序與真機背包 UI smoke 待後續。
- [x] **背包排序 ITEM_SORT(0x3F)**（移植 `InventoryHandler.ItemSort` 主幹）— Core `Inventory.SortByItemId` 依 itemId 排序重排；Channel 共用 arrange 流程更新背包並送完成封包。Core/Adapters 測試通過。任務歷程 `2026-06-10_12`；完整 item metadata 排序細節與真機背包 UI smoke 待後續。
- [ ] in-game 後續：IFieldRegistry+sink 重構、proper 每-Field objectId 配發器。補充：`IOnlinePlayerRegistry` 已於技術債任務整合；proper per-field objectId / field actor 仍待。
- [x] **腳走一般 portal 換圖 CHANGE_MAP(0x1E)** — 程式碼+單元測試已完成並 commit `3fd97c9`；`V113MapPackets.ParseChangeMap` + handler 查 WZ portal(tm/tn) 後 `WarpAsync(spawnPortalId)`；Adapters.V113 113 綠。**✅ 真機 GUI smoke PASS(2026-06-19)**：TestHero 走 portal 'west00' → 地圖 104040000（39 怪物 replay）。任務歷程 `2026-06-06_08`, `2026-06-19_01`
- [x] **換頻道 CHANGE_CHANNEL(0x1F) / CHANGE_CHANNEL(0x08)** — 單進程 MVP 已完成：handler 忽略目標 channel、flush inventory、保存角色文件後回同一個設定 channel endpoint（預設 `127.0.0.1:8585`），client 斷線重連交由既有 `PLAYER_LOGGEDIN` 流程；Adapters.V113 254+1skip 綠、Host build 綠。任務歷程 `2026-06-17_05`；真機 GUI smoke 待做。

- [ ] **M3-8** ★**版本抽象提取**：login/charinfo/movement ≥2 族就緒後，長出 `IOpcodeTable`/邏輯 opcode/serializer 接縫，把 v113 具體碼重構進 `Adapters.V113`
  - **執行者**：Sonnet
  - **資格要求**：架構設計
  - **前置探索**：agy: 跨版本封包結構差異
  - **DoD**：handler 不再認版本數字、Core/App 無 if-version ｜ 依賴：M3-7 ｜ L

---

## M4 — 戰鬥
**目標**：打怪、傷害、掉寶、背包。**驗收**：能打怪、掉寶、撿物、背包正確。

- [ ] **M4-1** 怪物生成＋spawn 封包 🟦 批次移植已入 master（monster spawn / combat service / 進圖送怪；item-use 召喚袋 spawn intent 已接），目前僅 headless+單元證據；待 #12 真機 smoke 驗怪物顯示與 SET_FIELD/SPAWN_PLAYER 不退。
  - **執行者**：Codex
  - **資格要求**：live-debug
  - **前置探索**：agy: WZ Mob struct + spawn opcode
  - **DoD**：地圖出怪 ｜ 依賴：M3 ｜ M
- [ ] **M4-2** 怪物 AI／移動 tick — 尚未正式完成；現階段有怪物執行期與被攻擊/死亡流程，沒有完整 AI/move tick/respawn scheduler。
  - **執行者**：Codex
  - **資格要求**：live-debug
  - **前置探索**：agy: Mob AI behavior in old Java
  - **DoD**：怪會動 ｜ 依賴：M4-1 ｜ M
- [ ] **M4-3** 攻擊封包解析（近戰/遠程/魔法，對照 `AttackInfo`/`DamageParse`） 🟦 近戰、遠程、魔法批次移植已入 master；待真機攻擊 smoke 與封包 fixture 收斂後再勾。
  - **執行者**：Codex
  - **資格要求**：live-debug
  - **前置探索**：agy: AttackInfo struct + opcode
  - **DoD**：攻擊被正確解析 ｜ 依賴：M4-1 ｜ L
- [ ] **M4-4** 傷害公式 🟦 傷害/擊殺/EXP 基礎流程已入 master；P003-D3 補 `MONSTER_BOMB(0xBB)` selfDestruct 無獎勵擊殺路徑與 kill animation broadcast；數值合理性、平衡與真機戰鬥 smoke 未驗。
  - **執行者**：Sonnet
  - **資格要求**：架構設計
  - **前置探索**：agy: Damage calculation in old Java
  - **DoD**：傷害數值合理、怪會死 ｜ 依賴：M4-3 ｜ M
- [ ] **M4-5** 掉寶＋撿物 🟦 `DropService`/掉落物/撿取流程已入 master；item-use 捕捉道具成功路徑已接 headless 驗證；待真機 kill→drop→pickup/catch smoke 與 inventory 顯示驗證。
  - **執行者**：Codex
  - **資格要求**：live-debug
  - **前置探索**：agy: Drop struct + pickup opcode
  - **DoD**：怪死掉寶、能撿 ｜ 依賴：M4-4 ｜ M
- [ ] **M4-6** 背包系統（inventory 編碼） 🟦 MVP-0 背包袋、ITEM_MOVE、穿脫裝、商店/撿物、item-use 四缺口（召喚袋/坐騎飼料/返回卷軸/捕捉道具）、`USE_ITEM(0x42)` 消耗補藥、`USE_UPGRADE_SCROLL(0x50)` 升級卷軸、`ITEM_UNLOCK(0x95)` Phase A 清 LOCK flag、`REWARD_ITEM(0x6A)` deterministic fallback reward path、`ITEM_MAKER(0x6B)` P003-D4 完整合成 catalog/service/handler、`USE_TREASUER_CHEST(0x6C)` deterministic chest reward path 已入工作區；仍需 WZ/RandomRewards 資料源與真機 GUI smoke 驗顯示/穿脫/道具使用/持久化邊界，不以 headless-only 勾完成。
  - **執行者**：Sonnet
  - **資格要求**：架構設計
  - **前置探索**：agy: Inventory packet format
  - **DoD**：道具進背包、顯示正確 ｜ 依賴：M4-5 ｜ L

---

## M5 — 腳本＋多實例
**目標**：既有 NPC `.js` 能用；正式啟用多實例。**驗收**：NPC 能對話買賣傳送；同機同時跑兩個獨立伺服器。

- [x] **M5-1** Jint 宿主整合 — **DoD**：能執行一段 JS ｜ ✅ Jint 4.9.2 接入 Maple.Scripting（`JintNpcScriptFactory`，CLR sandbox off + 遞迴/敘述/逾時上限、regex 剝 load/importPackage + no-op shim）。任務歷程 `2026-06-02_12`(commit 122c468)
- [x] **M5-2** NPC 對話 API 重實作（對照 `NPCConversationManager`） — **DoD**：API 對齊既有腳本用法 ｜ ✅ 乾淨分層：`INpcScriptContext`(cm surface) + `NpcContext`(薄橋接) + `NpcConversation`(session-local, queue-and-flush) + `NpcDialog` DTO；cm 以 lowercase 委派物件 expose。MVP API：sendNext/Prev/NextPrev/Ok/YesNo/Simple/GetText/GetNumber/dispose/warp/gainMeso/getJob/getMeso/getMap（shop/quest/storage 排除）。同 commit
- [x] **M5-3** 跑既有 `scripts/npc/*.js` — **DoD**：代表性 NPC 能完整對話 ｜ ✅ headless 跑通真計程車 1002000.js 全流程（多輪對話→YesNo→扣 meso→warp，跨封包狀態持續）；c2s NPC_TALK(0x33)/NPC_TALK_MORE(0x35)、s2c NPC_TALK(0x13C)+`V113NpcDialogEncoder` 對齊 Java getNPCTalk。**待**：真客戶端 GUI smoke。同 commit
- [ ] **M5-4** portal / quest / reactor 腳本管理 — **DoD**：傳送點/任務/反應堆腳本可運作 ｜ 依賴：M5-2 ｜ L ｜ 🟦 quest API/WzQuestCatalog 已批次移植入 master；一般腳走 portal 換圖另以 CHANGE_MAP 先做；script portal/reactor 與完整任務腳本仍待。
- [ ] **M5-5** 商店系統 — **DoD**：能買賣 ｜ 依賴：M5-3 ｜ M ｜ 🟦 NPC shop service/封包/腳本 `cm.openShop` 已批次移植入 master；P003-D5/D6/D7 已補 HiredMerchant Core/Application/Persistence、0x34/0x38/0x3A Adapters/Fredrick/spawn、`PLAYER_INTERACTION(0x73)` merchant create/visit/add/remove/buy/exit/chat/open/close、啟動過期轉 claimable 與 LiteDB E2E；P003-D7b 修補 Fredrick/Claim 重複領取 race，並補 HiredMerchant S2C unverified fixture 註記；P003-D8b 修補店內 UI 廣播範圍、鎖紀律、close merchant `shopErrorMessage` 版型與 reload 例外降級。待：真機 GUI 買賣 smoke 與 HiredMerchant S2C layout capture/校準。
- [ ] **M5-6** ★**多實例正式啟用**：`instances[]` 設定、`ServerInstance` 隔離、每實例獨立 DB/port/world、`StartAsync` try/catch 故障降級 — **DoD**：設定加一筆即起第二個 server、互不干擾、A 異常不殺 B ｜ 依賴：M3-8 ｜ L
- [ ] **M5-7** 多實例 DB 策略決定（每實例一 schema vs `instance_id`） — **DoD**：策略寫進設計書並實作 ｜ 依賴：M5-6 ｜ M ｜ 🟦 MongoDB provider 已批次整合，host 預設 provider 曾切 MongoDB；正式多實例 DB 策略與本機 smoke 設定仍待定（本機無 mongod 時用 `Persistence:Provider=LiteDb`）。

---

## M6+ — 進階 / 真隔離
**目標**：補齊到「超越舊伺服器」。**驗收**：逐項完成；多進程真隔離可選。

- [ ] **M6-1** 購物商城 CashShop — **DoD**：商城可進、可購買 ｜ 依賴：M5 ｜ L ｜ 🟦 minimal CashShop 已批次移植入 master；P003-D3 補 `COUPON_CODE(0xE7)` Core coupon repository + LiteDB/Mongo persistence + NX/MaplePoints/item/meso 兌換主路徑；P003-D7b 修正 coupon redeemed item S2C 為 Java multi-item 版型，並修 `period <= 0` 永久期限語意；完整商城模式、coupon seed/admin tooling 與真機 UI 驗證待補。
- [ ] **M6-2** 組隊／公會／好友 — **DoD**：社交系統可用 ｜ 依賴：M5 ｜ L ｜ 🟦 Party/Buddy/Guild、Family 家族、人氣 GiveFame、多頻道聊天與 NOTE_ACTION 留言 MVP 已批次移植；`IOnlinePlayerRegistry` 已統一；family/note dispatch/DI、guild chat/完整 live 社交 smoke 待補。
- [ ] **M6-3** 技能進階（四轉等） — **DoD**：技能系統完整 ｜ 依賴：M4 ｜ L ｜ 🟦 skills + ranged/magic 基礎已批次移植入 master；P003-D2 補 `ARAN_COMBO(0x92)` runtime combo + buff candidate；「技能系統完整」未達，需真機技能/BUFF/四轉等後續驗證。
- [ ] **M6-4** 活動系統 — **DoD**：活動可運作 ｜ 依賴：M5 ｜ M ｜ 🟦 `COCONUT(0xCF)` / `RPS_GAME(0x80)` / `CP_BeansGameAction(0xE0)` 已由 MVP stub 升級為 Core state model + v113 handler 主路徑；P003-D2 補 `SNOWBALL(0xCD)`/`LEFT_KNOCK_BACK(0xCE)` handler parity；P003-D3 補 `CP_BeansUpdate(0xE1)` reset+exit、`GAME_POLL(0xA3)` Java disabled parity、`MAPLETV(0x10A)` no-broadcaster parity；Coconut lifecycle/team assignment、RPS/Beans 完整 reward/event UX、Snowball/MapleTV/Monster Carnival state machine 與真機 event smoke 未做。
- [ ] **M6-5** 多進程版（真故障隔離） — **DoD**：實例跑在獨立進程、互不波及 ｜ 依賴：M5-6 ｜ L
- [ ] **M6-6** 「超越舊伺服器」清單（待你定義具體超越項：效能/體驗/新系統…） — **DoD**：清單成形並排程 ｜ 依賴：— ｜ S

---

## P002 — 技術債清償（ENTER_CASH_SHOP / USE_SKILL_BOOK / USE_CASH_ITEM）

- [x] T1: ENTER_CASH_SHOP(0x20) 完整 reconnect 模式切換（pending transition + CS loop + 30s stale expiry）
- [x] T2: USE_SKILL_BOOK(0x4C) ISkillBookCatalog 全鏈（Core→Content→Adapters）
- [x] T3-A: USE_CASH_ITEM batch A（黑板/寵物改名/寵物技能/寵物食品/留言/祝賀歌）
- [x] T3-B: USE_CASH_ITEM batch B（AP/SP Reset/Item Tag/Lock/Karma/Hammer + stubs）
- [x] T3-C: USE_CASH_ITEM batch C（喇叭 8 類 + MapleTV stub）
- [x] T3-D: USE_CASH_ITEM batch D（傳送石 7 類 + FieldLimitType）
- [x] D9: GPT-5.5 Review + blocker fixes
- [x] D10: 鐵律歸檔

---

## P003 — 移植清尾 + MVP stub 補完整（HiredMerchant / ItemMaker / 雜項 stub）

> 計畫檔：`docs/devlog/執行計畫/P003_移植清尾與stub補完整.md`；任務歷程：`docs/devlog/任務歷程/2026-07-06_01_移植_P003清尾與stub補完整.md`

- [x] D0/D0b: 移植狀態地圖大修正（❌44→實際僅 USE_FISH_MERCHANT 未接）+ Mob 特殊四項研究
- [x] D1: opcode swap 修正 + SHOW_EXP_CHAIR/REWARD_ITEM/USE_TREASUER_CHEST/CP_UserThrowGrenade
- [x] D2: ARAN_COMBO combo buff + CYGNUS_SUMMON/SNOWBALL/LEFT_KNOCK_BACK
- [x] D3: COUPON_CODE/GAME_POLL/MAPLETV/CP_BeansUpdate + MONSTER_BOMB
- [x] D4/D4b: ITEM_MAKER 合成系統完整移植 + SkillBook catalog Item.wz 萃取 165 筆
- [x] D5/D6/D7: HiredMerchant 三刀（Core/App/Persistence → Adapters 0x34/0x38/0x3A → 0x73 子指令+啟動重載+E2E）
- [x] D7b/D8b: 兩輪外部審查（agy 前置 4 項 + GPT-5.5 終審 5 項）缺陷修補
- [x] D8複核: agy 複核 9 項全 ✅，判定可收案
- [x] D9: 鐵律歸檔（本次）

**最終戰績**：645→**775 passed / 1 skipped**（+130，此差值基準不一致，實際基準一致差值為 +71，見稽核任務歷程 `2026-07-07_01`）；`dotnet build` 0 warning/0 error；Core/Application 禁區 grep 全程 clean。HiredMerchant/ItemMaker 完整落地，惟 S2C 全數 Java-source candidate/unverified、真機 GUI smoke 未跑，故 M4-6/M5-5/M6-1/M6-3/M6-4 DoD 暫不勾完成，僅加狀態註記（見各列）。殘餘 TODO 列 P004 候選：REWARD_ITEM/寶箱 RandomRewards catalog（2026-09-06 已完成金/銀寶箱權重表，見任務歷程 `2026-09-06_04`；REWARD_ITEM 本身的 SQL-based 通用獎勵表仍留 deterministic fallback，需先定 SQL 資料遷移策略）、Snowball/Shammos-Node/MonsterCarnival/MTS 完整拍賣、562x 技能書（2026-09-06 已查明：本 v113 client 的 Item.wz 無 562x 資料，是資料缺口非程式缺陷，見任務歷程 `2026-09-06_03`；PARTY_SEARCH_START/STOP 已於同日完整移植，見 `2026-09-06_01`）。

---

## 待你拍板的開放決策（隨里程碑推進再定）
- **[M3]** WZ 共享快取所有權：process 級唯讀單例 ✅推薦 vs 每實例。
- **[M5]** 多實例 DB：每實例一 schema ✅推薦 vs 共用 schema＋`instance_id`。
- **[M5]** 多實例典型用途：同版本多世界 / 不同版本並排 / 兩者。
- **[#12]** 批次移植後真機 smoke：batch-5 八路已全部收官；先用 LiteDB provider 驗 SET_FIELD/SPAWN_PLAYER/CharacterInfo 不退，再驗 CHANGE_MAP 腳走 portal，最後抽驗 batch-5 代表功能（reactor/trade/Duey/BBS/ring/npc-services/buff-items/item-use）。
- **[M6]** 「超越舊伺服器」的具體定義（你最想超越的點）。

---

*下一步：校準本清單顆粒度 → 開工 M0。*
