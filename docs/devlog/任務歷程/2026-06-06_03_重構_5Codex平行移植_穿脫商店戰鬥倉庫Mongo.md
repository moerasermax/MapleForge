---
編號: 2026-06-06_03
標題: 5 Codex 平行移植：背包穿脫 / NPC商店 / 戰鬥 / 倉庫 + LiteDB→MongoDB
類型: 重構
狀態: 🚧 執行中
建立: 2026-06-06 (session)
更新: 2026-06-06 (session)
關聯里程碑: Java→.NET 移植主線（M3-8~M6）
關聯記憶: v113-pivot-port-from-java, current-state-resume, default-collaborate-with-ai-team, sibling-projects-learnings
關聯commit: 待填
---

## 🎯 目標（執行前先寫死，過程不偷改）

使用者拍板：用 **5 個 Codex(gpt-5.5 xhigh) 平行**推進 Java→.NET 移植——4 個遊戲系統 + 1 個持久層換 Mongo。Java(`TestMapleStoryV113_Server` OdinMS)是範本，本階段＝改寫移植進乾淨分層（協定鎖 Adapters.V113 / 領域 Core / 用例 Application，保留零-static、Core 零 V113 import），能優化就優化。
**驗收判準**：5 個工作單元各自在其 worktree build 綠 + 單元測試綠；我中央整合（opcode/派發/DI 接線）後**整解 build 綠 + 既有測試不退**；至少 headless 驗各系統主流程跑通。MongoDB：repository 實作 + DI 切換 + EphemeralMongo 自測綠。

## 📋 背景與假設

- 決策（本 session 使用者拍板）：①這批就換 MongoDB（非延後）②首批四系統＝背包穿脫(MVP-1)/NPC商店/戰鬥攻擊/倉庫storage。
- 現況：持久層 = **LiteDB 5.0.21**，藏在 `IAccountRepository`/`ICharacterRepository` 介面後（Core 定義、Persistence 實作）→ 遊戲移植與 DB 無關。本機**無 mongod**（Mongo agent 用 EphemeralMongo 自帶 binary 跑測試）。
- 已移植（master `93db08c` 基礎）：移動/聊天/地圖NPC spawn、NPC 對話 Jint 引擎、背包 MVP-0(背包袋 gain/move/swap)。
- 平行安全設計（**關鍵**）：
  - **共用檔（agents 禁改、我中央整合）**：`Channel/V113ChannelConnectionHandler.cs`(switch 派發)、`Channel/V113ChannelOpcodes.cs`(opcode 常數)、`World/Player.cs`(已 partial)、`Host.Shared/MapleServerHost.cs`(DI)。
  - **agents 只產新檔**：Player 擴充用 `World/Player.<系統>.cs`(`partial class Player`)；封包用新 `V113<系統>Packets.cs`；handler 邏輯寫成可被派發呼叫的方法/類別。
  - **回報而非自接**：每 agent 回報「需要的 opcode 值 / 派發 case 對應 / 要註冊的 DI 服務」，我在 master 中央接線。
  - **隔離**：5 個 git worktree（branch `port/{equip,shop,combat,storage,mongo}`），各自 build（**只 build 自己的專案**避免本機記憶體爆/BSOD），互不鎖 bin/obj。

## 🪜 計畫步驟

- [x] 0. 勘查共用衝突點 + Player partial 化（master prep）
- [ ] 1. commit+push prep；建 5 worktree + branch
- [ ] 2. 派 5 Codex(gpt-5.5 xhigh)，各帶完整背景 + 平行協定 + 自驗(build 自專案/測試)
- [ ] 3. 收 5 份（session_id 記此檔；大輸出子代理讀）
- [ ] 4. 中央整合到 master：merge 新檔 → 接 opcode/派發/DI → 整解 build 綠 → 既有測試不退 → headless 驗
- [ ] 5. checkpoint commit+push；清 worktree；回填三本帳

## 📜 執行歷程（邊做邊追加，附時間）

- **T0** 對齊決策(Mongo 這批換 + 四系統)；勘查 Player(改 partial)/派發 switch/opcode 檔/無 mongod；定平行協定。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> 已做 master prep（Player→partial）。下一步：commit+push prep → 建 5 worktree(`port/equip|shop|combat|storage|mongo`，置於 `V113/_worktrees/`) → 派 5 Codex(gpt-5.5 xhigh，workFolder=各 worktree)。**平行協定見上「📋 背景」：agents 禁改 4 個共用檔、只產新檔、回報接線。** 5 個 session_id 收回後記在此。整合在 master 由我做。若崩潰：worktree 與 branch 都在磁碟，`git worktree list` 可查，未整合的 agent 產出在各 branch。

## ✅ 結果與結論

> 待整合後回填。

## 🔗 產出

> 待填。
