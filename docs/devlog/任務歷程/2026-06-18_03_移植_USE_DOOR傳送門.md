---
編號: 2026-06-18_03
標題: 移植 USE_DOOR 傳送門
類型: 移植
狀態: ✅ 完成
建立: 2026-06-18 08:05
更新: 2026-06-18 08:11
關聯里程碑: M3 in-game 後續
關聯記憶:
關聯commit: 8e7b5b7（統籌整合 commit，含 Door 新檔+dispatch+DI）
---

## 🎯 目標（執行前先寫死，過程不偷改）

移植 v113 `USE_DOOR(0x7D)` Mystic Door 使用流程：新增 runtime-only `Door` domain object、thread-safe `DoorService`、v113 handler/parser/packet helpers，並以 xUnit 覆蓋 town-to-target 與 target-to-town/backwarp 兩個方向。完成判準：Core/Application 不含 v113 byte/opcode 知識，Adapter byte layout 只在 `Maple.Adapters.V113`，targeted tests 通過。

## 📋 背景與假設

Java source map：`PlayersHandler.UseDoor` 與 `server/maps/MapleDoor.java`。本輪只移植 USE_DOOR 使用既有 door 的 warp 主幹；Mystic Door 施放/建立會由未來 `SPECIAL_MOVE(0x55)` skill handler 呼叫 `DoorService.CreateDoor`，不在本任務範圍。

## 🪜 計畫步驟

- [x] 1. 讀 Java reference 與現有 `Player` / `FieldInstance` / `MapService` / channel test patterns。
- [x] 2. 新增 Core `Door` domain object。
- [x] 3. 新增 Application `DoorService`，支援 create/get/remove/warp decision。
- [x] 4. 新增 v113 `V113DoorHandler` 與 `V113DoorPackets`，不接 central dispatch。
- [x] 5. 新增 Adapters tests 覆蓋 target-to-town 與 town-to-target。
- [x] 6. 跑 targeted tests，收尾更新本任務歷程。

## 📜 執行歷程（邊做邊追加，附時間）

- **08:05** 建立任務歷程，狀態切為執行中；下一步讀 Java 與現有 C# patterns。
- **08:08** 完成 Core/Application/Adapters 新檔案與 Door handler tests；focused Door tests 5/5 通過。
- **08:10** Adapters.V113 全測 287+1skip、Core 82、Application 134 通過；補 v113 protocol spec、進度日誌、任務追蹤。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

已完成本 slice。後續若要接線：在 channel dispatch 中注入/呼叫 `V113DoorHandler.HandleUseDoorAsync`，成功時把 `DoorWarpResult` 交給既有 `WarpAsync`，失敗時送 `SelfPackets`；Mystic Door 建立由未來 `SPECIAL_MOVE(0x55)` 呼叫 `DoorService.CreateDoor`。

## ✅ 結果與結論

達標。USE_DOOR 的 mode byte 依 Java 實作固定解讀為 `0 => target-to-town/backwarp`、`1 => town-to-target`；`DoorService` 以 ownerId 同時索引 town/target 兩側地圖，並保留 owner/party access control。未修改 `V113ChannelConnectionHandler.cs` 或 `V113ChannelOpcodes.cs`。

## 🔗 產出

- `src/Maple.Core/World/Door.cs`
- `src/Maple.Application/Maps/DoorService.cs`
- `src/Maple.Adapters.V113/Channel/V113DoorHandler.cs`
- `src/Maple.Adapters.V113/Channel/V113DoorPackets.cs`
- `tests/Maple.Adapters.V113.Tests/DoorHandlerTests.cs`
- `docs/specs/v113-protocol-spec.md`
- `docs/devlog/進度日誌.md`
- `docs/devlog/任務追蹤.md`
- `docs/devlog/任務歷程/README.md`
