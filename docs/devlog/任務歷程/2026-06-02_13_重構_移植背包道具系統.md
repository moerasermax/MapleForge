---
編號: 2026-06-02_13
標題: 移植背包/道具系統（路線圖②後半，團隊決策→自動推進，先不寫測試）
類型: 重構
狀態: 🚧 執行中
建立: 2026-06-02 21:00
更新: 2026-06-02 21:00
關聯里程碑: 移植路線圖 ②背包/道具（解鎖商店+戰鬥掉寶）
關聯記憶: v113-pivot-port-from-java, default-collaborate-with-ai-team, current-state-resume
關聯commit: 接 20afadd
---

## 🎯 目標（執行前先寫死，過程不偷改）

> 把 v113 背包/道具系統的 **MVP-0 切片**移植到 MapleForge 乾淨分層（設計見 `docs/design/背包道具-領域分層設計.md`，團隊紅隊定案）：讓「角色背包裡的道具能在客戶端顯示（五欄真實序列化）+ 格內 ITEM_MOVE 移動/合併 round-trip」可行。持久快照(ItemRecord 扁平 DTO 掛 Character)≠執行期富領域(Inventory 聚合 hydrate 於 Player)；變動唯一入口 Player（比照 GainMeso）。不照 OdinMS static 風格、邊做邊重構。
>
> **完成判準（DoD，已鎖死）**：①Core/Inventory 富領域(InventoryType/Item/Equip/Inventory/Inventories)+ ItemRecord 扁平 DTO，與 Character/Player 關係清楚(hydrate/flush) ②`Character.Equips`→`Items(List<ItemRecord>)`，SetField 五欄(EQUIP/USE/SETUP/ETC/CASH)接真實 records 取代 stub ③c2s ITEM_MOVE **格內 move/merge**(src>0,dst>0) 解析→per-session 序列化更新→ModifyInventory(0x1B) 回包 ④`cm.gainItem` 能種道具(兼 NPC 整合) ⑤build 綠 + headless/里程碑 smoke(進圖看到道具+拖移 round-trip) ⑥既有測試不退。
>
> **MVP-1（同里程碑，DoD 外但接著做）**：equip/unequip owner-side。**Defer**(需先做 app-journal)：use/drop/pickup/sell/trade/sort/gather/pet。

## 📋 背景與假設

> 路線圖①+②NPC 對話核心已完成。Java 權威：`InventoryHandler.java`(2819 行,巨大)、SetField 背包序列化在 PacketHelper.addCharacterInfo/addInventoryInfo、道具靜態資料 MapleItemInformationProvider(?)。MapleForge 現況：只有 `Character.Equips`(EquipEntry，給 SetField 渲染穿戴)，無完整背包，SetField inventory 區塊大多 stub。
> 使用者指示：團隊繼續自動推進、先不寫單元測試、build 綠+headless 驗證即可。

## 🪜 計畫步驟

- [x] 0. 團隊紅隊決策（Gemini-family 分層紅隊 + codex gpt-5.4 持久化/dupe + Opus 綜整）→ 設計定案寫 `docs/design/背包道具-領域分層設計.md`、鎖 DoD。
- [x] 1. Core/Inventory：`InventoryType`/`Item`/`Equip`/`Inventory`/`Inventories`/`ItemRecord`（純領域，build 綠）。✅（見 22:30 條目）
- [x] 2. `Character.Items(List<ItemRecord>)`(並存 Equips)；`Player` hydrate/FlushInventory + GainItem/HasItem/MoveItem。✅
- [x] 3. Adapters.V113 `AddInventoryInfo` 五袋真實序列化 + `AddBagItemInfo`。✅
- [x] 4. `V113InventoryPackets`(ParseItemMove 0x41 + ModifyMove 0x1B mode2)；handler `HandleItemMoveAsync`(僅格內 move)。✅ ＊用例直接在 handler 呼 Player（未抽 `MoveItem.Apply`），可接受。
- [x] 5. `cm.gainItem/haveItem` 全鏈接通 + headless 領域驗證跑通。✅
- [x] 5b. **Opus 續 session 獨立複驗**：全新紅隊(Gemini-family+codex gpt-5.4)重 derive 出同一設計；全解決方案 build 綠；**全部 5 測試專案 126 測試通過**(Adapters38/Application28/Content9/PacketDecoder22/HeadlessClient29)。
- [ ] 6.（MVP-1 / 下一步）equip/unequip + look-broadcast；或 NPC 商店；或真客戶端 GUI smoke 驗位元組。

## 📜 執行歷程（邊做邊追加，附時間）

- **21:00** NPC 對話引擎完成(commit 122c468/20afadd)後，使用者「請團隊繼續自動推進」。蒐集脈絡(InventoryHandler 2819 行、MapleForge 僅 EquipEntry、SetField inventory stub)。發團隊決策+設計 consult：claude-ultra(pid 32248)、Gemini(pid 9556)、agy(pid 34540)。
- **(續session)** 上輪 3 PID 因 MCP/本機重啟全部 not found、結果遺失。Opus 已自行掃完 Java 協定事實(IItem/Item/Equip、MapleInventory slot→item、ITEM_MOVE 統一分支、ModifyInventory opcode 0x1B modes 0-3、addItemInfo 布局)＋讀完架構文件，先形成分層立場。**重派紅隊**：Gemini-family(pid 21912)、codex(gpt-5.3 不支援→改 gpt-5.4 pid 25660)。
- **紅隊回收+綜整**：兩個獨立模型在關鍵處收斂出**比初版更好**的方案——
  - Q1 選背包：**一致背書**（Gemini 補：載入時驗 slot-limit 免 client 崩）。
  - Q2 放哪(crux)：**雙雙挑戰「富聚合掛 Character 文件根」**→改為 **持久扁平 ItemRecord DTO 掛 Character + 執行期富 Inventory 聚合 hydrate 於 Player**（codex：別讓 LiteDB 重建 Item/Equip 繼承樹；Gemini：別讓文件根變活狀態機）。比照 Position 前例。
  - Q3 MVP：codex 給更利的切線——**reorder/equip roll-back 無害=可先做不需 journal；use/drop/sell/trade 改價值/所有權=雷區、需 app-journal**。Gemini：equip 的 look-broadcast 可拆為薄 follow-up。
  - Q4 風險：耐久邊界(deferred 那批上線前需 fsync app-journal)、dupe(per-player 單緒佇列+apply 時對權威狀態重驗 src/qty)、LiteDB 嵌入單文件原子寫+1MB 上限。
  - → 設計定案 `docs/design/背包道具-領域分層設計.md`，DoD 鎖死，狀態切 🚧。

- **22:30 [MVP-0 實作完成 + headless 驗證通過]** 自動推進落地（微調設計：為控風險**增量**——MVP-0 先做背包袋(正 slot)，`Character.Equips` 不動、equip/unequip unification 留 MVP-1，故不碰既有 char-look）：
  - **Core/Inventory**：`InventoryType`(enum 1-5)、`Item`/`Equip:Item`、`Inventory`(slot→Item，Gain/Move 含 swap/merge/移到空格不變式)、`Inventories`(5 袋，Hydrate/Flush)、`ItemRecord`(扁平持久 DTO，ToItem/From)。
  - **Core 整合**：`Character.Items`(List&lt;ItemRecord&gt;) 並存於 Equips；`Player` ctor hydrate、`GainItem/HasItem/MoveItem/FlushInventory`。
  - **Adapters.V113**：`AddInventoryInfo` 五袋真實序列化(取代 stub) + `AddBagItemInfo`(equip/stack 分支對齊 Java)；`V113InventoryPackets`(ParseItemMove c2s 0x41 + ModifyMove s2c 0x1B mode2)；opcodes；handler `HandleItemMoveAsync`(MVP-0 僅格內 move，穿脫/丟棄 defer log)。
  - **NPC 整合**：`INpcScriptContext.GainItem/HaveItem` + NpcContext(itemId 前綴判 type) + Jint facade `cm.gainItem/haveItem`。
  - **build 綠**(0 error 含 MF0001)；既有測試 Adapters 38 / Application 28 不退。
  - **★headless 驗證通過★**(拋棄式 console)：gain 4 道具(正確 type/slot/equip 旗標)→flush→**hydrate roundtrip 保真**(ItemRecord↔Item/Equip)→move 1→5→swap 5↔2→**merge 併量 350**→cm.gainItem 類型路由正確(USE/EQUIP 分流)。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> ✅ **背包 MVP-0 實作完成、build 綠、headless 驗證跑通(gain/hydrate/move/swap/merge/cm.gainItem)。** 下一步可選：①**MVP-1 equip/unequip**(穿脫裝，需 Character.Equips↔Items 統一 + look-broadcast)②**真客戶端 GUI smoke**(用 cm.gainItem 種道具或 seed，進圖開背包看道具+拖移 round-trip；可與 NPC 對話 smoke 批量一起驗)③NPC 商店(現在有背包+gainItem 寫入路徑,順理成章)。**已知 MVP 限制**：merge 時 ModifyMove 仍送 mode2(嚴格應 mode1+mode3)，移到空格/swap 正確；穿脫/丟棄 defer。技術債見設計 doc。**暫存資料夾 tmp_inv_check 被 dotnet 鎖住未刪**(不影響、未入 git)，稍後清。

## ✅ 結果與結論

> 背包/道具 MVP-0 移植完成並 headless 驗證跑通。團隊紅隊（Gemini scoping + agy v113 封包事實；claude-ultra 卡死 kill、codex 帳號失效）→ 定案乾淨分層（持久扁平 ItemRecord ≠ 執行期富 Inventory，hydrate/flush，變動唯一經 Player）。為控風險採增量：背包袋先行、Equips/穿脫留 MVP-1。build 綠、既有測試不退、領域全流程（gain/hydrate roundtrip/move/swap/merge/cm.gainItem 類型路由）headless 驗證成立。封包層(五袋序列化/ITEM_MOVE/ModifyMove)對齊 Java+agy 事實、待 GUI smoke 驗位元組。
> 心法：①「持久快照 ≠ 執行期富領域」是本專案複雜聚合的通用模式（比照 Position；別讓 LiteDB 重建繼承樹）②MVP 切線用「rollback 安全性」分（reorder/equip 無害 vs use/drop/sell 改價值需 app-journal）比「功能大小」更利③團隊 consult 部分失敗時（agent 卡死/帳號失效），有 agy 封包事實 + 自身架構知識即可收斂，不空等。

## 🔗 產出

> - Core/Inventory：`InventoryType.cs`/`Item.cs`/`Inventory.cs`(Inventory+Inventories)/`ItemRecord.cs`（新）
> - Core：`Characters/Character.cs`(+Items)、`World/Player.cs`(+Inventory/GainItem/HasItem/MoveItem/FlushInventory)
> - Adapters.V113：`Channel/V113InventoryPackets.cs`(新)、`V113ChannelPackets.cs`(AddInventoryInfo 五袋+AddBagItemInfo)、`V113ChannelOpcodes.cs`、`V113ChannelConnectionHandler.cs`(HandleItemMoveAsync)
> - Application：`Npc/INpcScriptContext.cs`(+GainItem/HaveItem)、`Npc/NpcContext.cs`
> - Scripting：`JintNpcScriptFactory.cs`(cm.gainItem/haveItem facade)
> - 設計：`docs/design/背包道具-領域分層設計.md`
> - commit：待填本次
