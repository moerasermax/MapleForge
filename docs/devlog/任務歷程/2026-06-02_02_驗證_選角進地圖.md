---
編號: 2026-06-02_02
標題: 從世界選擇 → 選角 → 進地圖（blocker#3）
類型: 驗證
狀態: ✅ 完成（協定層；真客戶端視覺層列為後續）
建立: 2026-06-02 12:10
更新: 2026-06-02 12:21
關聯里程碑: M2-5 / M3-5 / blocker#3
關聯記憶: current-state-resume, protocol-re-without-source
關聯commit: f420980
---

## 🎯 目標（執行前先寫死，過程不偷改）

> 承接 blocker#2（已到世界選擇畫面），打通**世界選擇 →（雪吉拉）→ CharList 顯示 TestHero → 選角 → CHAR_SELECT→SERVER_IP → 切 Channel(8585) → 進地圖**整條鏈。
> **完成判準**：真客戶端（或 headless 等價驗證）走完選角後，**角色出現在地圖**（SET_FIELD 成功）；視覺層看到遊戲地圖畫面。
> 退階判準：若真客戶端卡住，至少 headless 把 charlist→select→server_ip→channel handshake→set_field 全跑通且封包正確。

## 📋 背景與假設

- 前置：blocker#2 已解（登入成功→世界選擇畫面，世界「雪吉拉」）。本輪 seed 已建角 TestHero（map/等級已校正）。
- 既有實作（任務追蹤）：M2-5 CHAR_SELECT→SERVER_IP(ch:8585) stub 已送出；M3-5 Channel host + SET_FIELD(完整 addCharacterInfo) 已實作，live 待驗。
- 假設：server 端鏈路已具備，未驗的是真客戶端是否接受整條 handoff 並真的進圖（尤其 SERVER_IP 的 IP/port 與 Channel 握手、SET_FIELD 布局）。
- 神諭：真客戶端行為（有沒有進圖）＋解碼 capture；**不翻 Java**，s2c 布局卡住才走反組譯自推。

## 🪜 計畫步驟

- [x] 1. 探查：headless client（`tools/Maple.Tools.HeadlessClient/Program.cs`）已內建完整鏈 login→serverlist→charlist→select→serverip→channel→PlayerLoggedIn→等 SetField。選定為主驗路徑（focus-independent）。
- [x] 2. 主驗：headless 跑通 charlist→select→server_ip→channel→**SetField（進地圖 100000000）**。
- [ ] 3.（後續）真客戶端 GUI 從世界選擇續點到進地圖（機器淨空、勿搶焦點）— 視覺最後一哩，另開任務。
- [x] 4. server 端 log + headless s2c 雙向確認各步正確。

## 📜 執行歷程（邊做邊追加，附時間）

- **12:10** 開檔定標。探查 headless client → 確認已內建完整鏈（含 channel/SetField），選為主驗路徑。
- **12:14（首跑 headless）** 卡在 **CharList 0 角色**（packet 14 bytes）。server log 也說 `← CHARLIST 0 角色`→ 確認是 server 真查到 0，非解析錯。
- **~12:16 根因定位**：**server 與 seed 讀寫不同 DB**。`DataDirectory:"data"` 是相對路徑：seed 對 repo root → `MapleForge/data/`；server 用 `dotnet run --project` 時 CWD/content root 變成專案目錄 → `src/Maple.Host.Login/data/`。AutoRegister 讓登入照樣成功（blocker#2 沒露餡），但 testuser 在 server 的 DB 裡無角色。另發現 seed 帳號未設 `Gender`/`SecondPassword`→ 會觸發 CHOOSE_GENDER gate（headless 不處理）。
- **~12:18 修正**：①`appsettings.json` `DataDirectory` 改絕對路徑 `...\MapleForge\data`（seed 已支援 rooted；server `Path.Combine(絕對,..)` 也原樣用 → 兩邊收斂）②`seed-test-data.ps1` 帳號補 `Gender=0`+`SecondPassword="000000"`（過 gate，模擬已完成性別/PIN 設定）。rebuild Login host(複製新 appsettings 到 bin)+reseed。
- **12:20（再跑 headless，DB 收斂後）** **整條鏈打通**：CharList len174「TestHero」charId=1 → SelectChar(1) → ServerIp 127.0.0.1:8585 → Channel → PlayerLoggedIn → **s2c 0x7B SetField len308 → ★ IN GAME**。server log：`← CHARLIST 1 角色`、`角色 TestHero SET_FIELD 送出 → 地圖 100000000`、`已進入地圖 100000000`。
- **12:21** 停 server + build-server shutdown（0 殘留）。**順帶發現後續清理項**：客戶端突然斷線時 server 在 `MapleSession.RunAsync`(MapleSession.cs:85) 拋例外、`TcpChannelListener` log `fail:`+stacktrace（應改為優雅處理斷線，記 info 而非 fail）。不在本任務目標內。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> ✅ 協定層完成（headless 角色 TestHero 進地圖 100000000）。**剩兩條後續（各自另開歷程檔）**：
> ① **真客戶端視覺最後一哩**：GUI 從世界選擇 →（雪吉拉）→ 頻道 → 選角 → 進地圖；diag11 目前只到登入鈕，需擴充續點流程。跑時機器淨空勿搶焦點。
> ② **斷線優雅處理**（小韌性 bug）：`MapleSession.RunAsync`(MapleSession.cs:85) 對客戶端正常斷線拋例外、`TcpChannelListener`(line 64) log `fail:`+stacktrace → 應辨識為正常斷線記 info。
> ＊重啟後若要重驗：起 server（聽 8484/8585）→ `dotnet run --project tools/Maple.Tools.HeadlessClient -- 127.0.0.1 8484 testuser test1234`，看到「★ SetField — IN GAME」即達標。

## ✅ 結果與結論

> **達標（協定層，符合目標的主完成判準「headless 等價驗證走完選角→SET_FIELD 成功」）。** 整條 charlist→select→server_ip→channel→PlayerLoggedIn→SetField 一次跑通，TestHero 進入地圖 100000000(Henesys)，封包（headless s2c）+ server log 雙向確認。
> **根因與修補**：blocker#3「CharList 0 角色」真因不是缺角色，而是 **server 與 seed 讀寫不同 DB**（相對 `DataDirectory` 在 `dotnet run --project` 下解析到專案目錄；AutoRegister 又讓登入照樣成功而長期遮蔽此 bug）＋ seed 帳號未設 Gender/PIN 會撞 CHOOSE_GENDER gate。修：appsettings DataDirectory 絕對化 + seed 補 Gender/SecondPassword。
> **可轉移心法**：(1) **headless 協定客戶端是最強的 focus-independent 驗證利器** —— 不碰 GUI 就把整條登入→進圖鏈自動跑通，比真客戶端 GUI 穩定可重複，黃易那邊也該先建等價 headless。(2) **AutoRegister 這類「容錯」會遮蔽資料路徑 bug**：登入成功 ≠ 讀對 DB；驗證要追到「能不能查到既有資料」而非只看「有沒有成功」。(3) 全程未翻 Java。

## 🔗 產出

- 改檔：`src/Maple.Host.Login/appsettings.json`（DataDirectory→絕對路徑）、`tools/seed-test-data.ps1`（帳號補 Gender/SecondPassword）
- 驗證資產：headless catalog JSON（`tools/Maple.Tools.HeadlessClient/bin/.../catalog-login.json`、`catalog-channel.json`）
- 記憶更新：`current-state-resume` blocker#3
- 後續任務（待開）：真客戶端視覺進圖、斷線優雅處理
- commit：待填（使用者要求時才 commit）
