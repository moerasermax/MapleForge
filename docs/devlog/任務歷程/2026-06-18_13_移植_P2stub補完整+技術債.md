---
編號: 2026-06-18_13
標題: P2 MVP stub 補完整實作 + 技術債清償
類型: 移植
狀態: 🚧 執行中
建立: 2026-06-18 23:30
更新: 2026-06-18 14:17
關聯里程碑: M4/M5/M6+
關聯記憶: current-state-resume, pm-dispatch-only-workflow
關聯commit: 待填
---

## 🎯 目標（執行前先寫死，過程不偷改）

> 把 P2 Wave 2-4 的 25 個 MVP stub 補完整實作邏輯 + 清償 3 項技術債。
> 完成判準：
> 1. 每個原 stub handler 有實際業務邏輯（不再只是 enableActions）
> 2. 新系統有 Core 模型 + Application service + focused tests
> 3. 技術債：ENTER_CASH_SHOP 設計落地、USE_SKILL_BOOK 接 Item.wz、USE_CASH_ITEM 完整路由
> 4. build 0 error，測試增長，禁區 clean

## 📋 背景與假設

- P2 全量 44 opcode 已入 dispatch（全為 MVP stub：讀欄位+enableActions）
- HEAD `3545fef`，515 綠
- Java 複雜度勘察已完成，最重的是 HiredMerchant/MTS/ItemMaker
- 自動推進模式：做到完或撞停止條件

## 🪜 計畫步驟

- [x] Phase A: 快速補完 (~5 opcode) — 改現有 handler 加邏輯即可
- [ ] Phase B: 中量補完 (~8 opcode) — 需新 Core 模型
- [ ] Phase C: 重量級子系統 (~10 opcode) — 需完整子系統移植
- [ ] Phase D: 技術債 3 項

## 📜 執行歷程（邊做邊追加，附時間）

- **23:30** 建任務歷程。啟動 Phase A 派工。
- **14:03** 本 session 承接使用者指定的 Phase A quick completions：只升級 ITEM_UNLOCK / USE_TELE_ROCK / USE_SCRIPTED_NPC_ITEM / MOB_NODE / CP_UserOldAntiMacroQuestionResult 這 5 個既有 stub；其餘 Phase B-D 保留在原大任務後續。
- **14:17** Phase A 完成：新增 Core `ItemFlags`、Application `MapService.MapExists`、adapter helper/dispatch for 5 opcode；新增 8 個 focused adapter tests；Host.Shared build、Adapters/Core/Application tests 與架構 using check 全過。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> Phase A quick completions 已完成且驗證通過；整體大任務仍是 Phase B-D 待續。下一步=若繼續此大任務，從 Phase B 中量 stub 補完整開始，先重新確認 Java source map 與現有未提交/併行變更。

## ✅ 結果與結論

> Phase A 達標：5 個指定 opcode 不再只是 read+EnableActions。`ITEM_UNLOCK` 會清 Equip item LOCK flag 並回 inventory update；`USE_TELE_ROCK` map mode 可 warp；`USE_SCRIPTED_NPC_ITEM` MVP 會驗證並消耗 Use item；`MOB_NODE` 驗 mob OID 並 log；old anti-macro answer 讀取並 log。Tele-rock use-result S2C 是 MapleForge candidate，仍待真 client smoke。

## 🔗 產出

> 程式：`ItemFlags`、`MapService.MapExists`、`V113ItemUnlockHandler`、`V113TeleRockHandler`、`V113ScriptedNpcItemHandler`、`V113MobNodeHandler`、channel dispatch wiring、inventory modify update helper。
> 測試：Adapters.V113 新增 Phase A helper tests；313 passed + 1 skipped。
> 文件：`v113-protocol-spec.md`、`進度日誌.md`、任務歷程 README。
> commit：未建立；工作區已有 unrelated EventSystems 變更，且與本任務共用 `V113ChannelConnectionHandler.cs` / protocol spec / devlog index，為避免混入他人變更先不 checkpoint。
