---
編號: 2026-06-06_01
標題: 帶 ai-cli 團隊去姊妹專案（跑跑卡丁車／黃逸群俠傳）開發取經
類型: 研究
狀態: ✅ 完成
建立: 2026-06-06 (session)
更新: 2026-06-06 (session)
關聯里程碑: 工具鏈/方法論基礎建設
關聯記憶: default-collaborate-with-ai-team, tool-first-and-strengthen, packet-capture-design
關聯commit: 289ec81
---

## 🎯 目標（執行前先寫死，過程不偷改）

帶 ai-cli 團隊（Codex／Antigravity／Kiro）去兩個同門更成熟的姊妹私服專案取經，產出一份**可執行的「MapleForge 採用清單」**。
**驗收判準**：`MapleForge/docs/devlog/取經/` 下集齊三份夥伴報告 + 一份我綜整的《取經總報告》，且總報告含「值得採用項 → MapleForge 現況 → .NET 落地做法 → 優先度(P0/P1/P2)」的具體表格（不只是泛泛而談）。

## 📋 背景與假設

- 兩個取經對象（同一使用者的姊妹專案）：
  1. **跑跑卡丁車** `D:\WorkSpace\AI_Lab\研究中\跑跑卡丁車`（C#/.NET；特色：stub server 完整反編譯原碼當「標準答案」、真機迭代迴圈）
  2. **黃逸群俠傳私服** `D:\WorkSpace\AI_Lab\研究中\黃逸群俠傳私服`（Python；更成熟：8-context DDD + git-hook 強制 Doc-Sync Gate + 完整工具鏈 locate.py/game.ps1/packet_archive.py/testbot.py/資源瀏覽器）
- 關鍵前提（取經要落地的差異）：
  - MapleForge 技術棧是 **.NET/C#**，黃逸是 Python → 工具可移植但需轉寫；DDD/Doc-Sync 需適配。
  - MapleForge **已有 Java 原碼（OdinMS 系）當權威** → 等同跑跑的 decompiled/，「原碼當標準答案」方法論可直接平移；source-less 反編譯SOP 對 MapleForge 價值較低。
- 工作模式：依記憶 `default-collaborate-with-ai-team`，本任務由 ai-cli 團隊分頭探查；我（Claude）統籌＋綜整。

## 🪜 計畫步驟

- [x] 1. 我用 Explore 子代理先摸清兩專案地形（保護主上下文）
- [x] 2. 派 ai-cli 三夥伴分頭取經（各聚焦一鏡頭，報告寫到 `docs/devlog/取經/`）
- [ ] 3. 收三份報告（大輸出用子代理讀，只取關鍵）
- [ ] 4. 綜整《取經總報告》：採用清單 + 優先度
- [ ] 5. 回填三本帳（任務追蹤／進度日誌／記憶）

## 📜 執行歷程（邊做邊追加，附時間）

- **T0** 讀兩專案 CLAUDE.md + Explore 子代理深掃 → 已掌握：黃逸 Doc-Sync Gate／反編譯SOP／DDD／工具鏈；跑跑 真機迭代迴圈／原碼當標準答案。
- **T1** 使用者澄清「夥伴＝ai-cli」→ 派三夥伴背景跑：
  - Codex(gpt-5.5 xhigh) pid 38544 → 黃逸 工具鏈+Doc-Sync+DDD → `取經/Codex_工具鏈與架構.md`
  - Antigravity pid 22640 → 跑跑 方法論 → `取經/Antigravity_方法論.md`
  - Kiro pid 39888 → 兩專案 封包存檔DB+資料瀏覽 → `取經/Kiro_資料與封包工具.md`

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> ✅ 取經完成，四份報告齊（含《取經總報告》）。**下一步若要動手實作**：從 P0 起——①Doc-Sync Gate MVP（Codex 在 `取經/Codex_工具鏈與架構.md` 已給完整 `doc_sync_check.py` Python 骨架+兩個 githook+MapleForge 路徑映射，可直接落地）②`docs/specs/persistence-model.md` ③`tools/Maple.Tools.PacketArchive`（SQLite，schema 見 Kiro/Codex 報告）④S2C 預言機（與 packet-capture-design 待續①②併做）。Codex session_id=`019e9a19-0846-7c92-b8f3-7005fe79d3d3` 可續接。

## ✅ 結果與結論

> 達標。三夥伴各交一份聚焦報告 + 我綜整《取經總報告》（採用清單含優先度表）。最大收穫三點：
> 1. **Doc-Sync Gate（P0）**：黃逸最成熟的工程制度，git-hook 硬擋「改 code 不補文件」；Codex 已給可直接上線的 .NET 版骨架。
> 2. **S2C 預言機（P0，新打法）**：Antigravity 提出用**舊 Java server 雙向 Winsock 側錄**產 S2C 明文黃金向量 → 直接解掉 CLAUDE.md 寫死的「server→client 無 ground truth」硬限制。
> 3. **方法論平移**：跑跑的 `decompiled/`＝MapleForge 的 Java 原碼，「真機迭代迴圈 + 別猜照原碼 + 1:N 發送 + 背景心跳辨識 + Console I/O 別卡發送」整套可平移。
> 明確「不該照搬」：source-less 逆向優先級、8-context 目錄樹、Mongo 偵測規則、Doc-Sync 初版過嚴。

## 🔗 產出

> `docs/devlog/取經/`：`Codex_工具鏈與架構.md`、`Antigravity_方法論.md`、`Kiro_資料與封包工具.md`、`取經總報告.md`。記憶：新增 `sibling-projects-learnings`。
