---
編號: 2026-09-25_92
標題: P090 — 死亡時取消特定 buff（playerDead：dispelSkill(0) + MORPH/MONSTER_RIDING/SUMMON/PUPPET）
類型: 移植
狀態: ✅ 完成
建立: 2026-09-25
更新: 2026-09-25
關聯里程碑: P090（/goal 第 20 件）
關聯記憶: <空>
關聯commit: 見 git log（P090）
---

## 🎯 目標（執行前先寫死，過程不偷改）

P076 只移植了 `playerDead` 的經驗值區塊；Java 在那之前還會取消召喚/分身類 buff、變身、騎寵、召喚、傀儡。補上並接到兩條死亡路徑，
召喚獸透過 P089 的共用副作用一併移除。完成判準：測試綠燈、build 0/0、全測試無退化。

## 📋 背景與查證

- Java `playerDead`：（靈魂之石、事件副本分支略）→ `dispelSkill(0)` → `cancelEffectFromBuffStat(MORPH)` / `MONSTER_RIDING` / `SUMMON` / `PUPPET` → `checkFollow` → 經驗值區塊。
- Java `dispelSkill(0)`：走訪 `effects`（`ConcurrentEnumMap`，依 buff stat 列舉順序），第一個 `isSkill()` 且來源 ∈ {4331003, 4331002, 4341002, 22131001, 1321007,
  2121005, 2221005, 2311006, 2321003, 3111002, 3111005, 3211002, 3211005, 4111002} 的 effect → `cancelEffect` 後 `break`。
- Java `cancelEffectFromBuffStat(stat)`：取持有該 stat 的 effect 整個取消（多 stat 的 buff 全部一起取消）。
- MapleForge `CancelBuffBySource` 以來源 ID 取消該 effect 的所有 stat，對應 `cancelEffect(effect)`；`_activeBuffs` 以 stat 為鍵，排序以列舉值近似 EnumMap 順序。
- 封包順序：`enableActions`（setHp）→ 取消的 buff → 護身符/經驗值 → HP 更新。

## 🪜 計畫步驟

- [x] 1. Core `Player.CancelBuffsOnDeath()`
- [x] 2. Application `PlayerDeathService.OnPlayerDied` 回傳 `PlayerDeathOutcome(CancelledBuffs, Penalty)`
- [x] 3. Adapter `V113DeathPackets.Build` 插入 CancelBuff；兩條死亡路徑呼叫 `V113BuffCancellationEffects.ApplyAsync`
- [x] 4. 更新既有測試簽章、新增測試；build、全測試、三本帳、commit

## 📜 執行歷程（邊做邊追加，附時間）

- 依計畫實作；既有 `ChannelDeathPacketsTests` 改用 `PlayerDeathOutcome`（斷言不變）。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> 已完成並 commit。下一個候選：零呼叫者 `CreateGuildAlliance`/`MerchantBuyError`/`MarriageRingLook` 逐一驗證。

## ✅ 結果與結論

- 達標：死亡時召喚獸消失、變身/騎寵等 buff 解除，客戶端收到對應 cancelBuff。
- 1156 passed / 1 skipped（P089 基準 1153 +3：Core 170→172、Adapters.V113 566→567）；build 0/0；禁區 grep clean。

## 🔗 產出

- 修改：`src/Maple.Core/World/Player.Death.cs`、`src/Maple.Application/Combat/PlayerDeathService.cs`、`src/Maple.Adapters.V113/Channel/V113DeathPackets.cs`、`src/Maple.Adapters.V113/Channel/V113ChannelConnectionHandler.cs`、`src/Maple.Adapters.V113/Channel/V113PlayerTickHandler.cs`
- 測試：`tests/Maple.Core.Tests/World/PlayerDeathPenaltyTests.cs`、`tests/Maple.Adapters.V113.Tests/ChannelDeathPacketsTests.cs`
