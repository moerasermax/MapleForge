---
編號: 2026-09-25_99
標題: P097 — 換裝後更新 Messenger 聊天室內的角色外觀（updateMessengerPlayer）
類型: 移植
狀態: ✅ 完成
建立: 2026-09-25
更新: 2026-09-25
關聯里程碑: P097（/goal 第 27 件）
關聯記憶: <空>
關聯commit: 見 git log（P097）
---

## 🎯 目標（執行前先寫死，過程不偷改）

P091 衍生：Java `equipChanged` 除了 `updateCharLook` 之外，還會 `World.Messenger.updateMessenger` 更新聊天室裡其他成員看到的外觀；
MapleForge 沒有。完成判準：封包對照 Java、換裝後通知同聊天室其他成員、測試綠燈、build 0/0、全測試無退化。

## 📋 背景與查證

- Java `MapleCharacter.equipChanged`：`if (getMessenger() != null) World.Messenger.updateMessenger(messengerId, name, channel)`。
- Java `World.Messenger.updateMessenger`：對聊天室其他成員（名字不同者）送 `updateMessengerPlayer(namefrom, from, position, fromchannel - 1)`。
- Java `updateMessengerPlayer`：`MESSENGER` + `0x07` + position + `addCharLook(chr, mega=true)` + name + `short channel`。
- MapleForge `V113MessengerPackets.WriteCharLook` 的 mega byte 寫 0（= Java mega=true），直接重用。
- 查證副產物：既有加入流程 `HandleMessengerAsync` 收到的是 `ChannelIndex + 1`（1-based）並直接寫進 `addMessengerPlayer` 的 channel，Java 是 `fromchannel - 1`（0-based），疑似差一，另開 P098 查證；本 P 的新封包照 Java 用 0-based `_options.ChannelIndex`。

## 🪜 計畫步驟

- [x] 1. `V113MessengerPackets.UpdateMessengerPlayer`
- [x] 2. `V113MessengerHandler.NotifyLookChangedAsync`；`HandleItemMoveAsync` 換裝成功後呼叫
- [x] 3. 測試、build、全測試、三本帳、commit

## 📜 執行歷程（邊做邊追加，附時間）

- 依計畫實作。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> 已完成並 commit。下一步 P098：查證 Messenger 加入封包頻道值差一（`ChannelIndex + 1` vs Java `fromchannel - 1`）。

## ✅ 結果與結論

- 達標：聊天室成員換裝後，其他成員的聊天室畫面同步新外觀。
- 1182 passed / 1 skipped（P096 基準 1180 +2：Adapters.V113 574→576）；build 0/0；禁區 grep clean。封包 unverified。

## 🔗 產出

- 修改：`src/Maple.Adapters.V113/Channel/V113MessengerPackets.cs`、`src/Maple.Adapters.V113/Channel/V113MessengerHandler.cs`、`src/Maple.Adapters.V113/Channel/V113ChannelConnectionHandler.cs`
- 測試：`tests/Maple.Adapters.V113.Tests/MessengerHandlerTests.cs`
