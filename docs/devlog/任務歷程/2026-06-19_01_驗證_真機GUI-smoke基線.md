---
編號: 2026-06-19_01
標題: 真機 GUI Smoke 基線驗證（進圖穩定性）
類型: 驗證
狀態: ✅ 完成
建立: 2026-06-19 00:00
更新: 2026-06-19
關聯里程碑: pending-batched-gui-smoke
關聯記憶: pending-batched-gui-smoke, realclient-full-automation-decision, gui-test-record-and-diagnose-visually
關聯commit: 待填
---

## 🎯 目標（執行前先寫死，過程不偷改）

> 跑 diag13-enter-game.ps1 全自動真客戶端 smoke，確認 645 綠 + P2 全量 + P002 技術債清償後，真客戶端仍能穩定進圖並續留 24s 以上。
> **完成判準**：client 存活 24s + server log 出現 `PLAYER_LOGGEDIN` + `進入地圖` + 無 unhandled exception/crash

## 📋 背景與假設

> 自 2026-06-02 最後一次真機 GUI smoke 以來，新增大量 handler（P2 Wave 1-4 + P002），headless 測試全綠但未驗真客戶端。假設新 handler 的 server→client 封包不會導致客戶端崩潰（因為 opcode 對齊且有 headless 驗證），但需實證。

## 🪜 計畫步驟

- [x] 1. Build server 確認編譯通過
- [x] 2. 跑 diag13-enter-game.ps1 全自動
- [x] 3. 讀 server log 驗證 PLAYER_LOGGEDIN + 進入地圖 + 無 exception
- [x] 4. 報告結果

## 📜 執行歷程（邊做邊追加，附時間）

- **00:00** 建立任務歷程，準備執行
- **執行** Build server 通過，準備 diag13
- **執行** 遭遇 MongoDB provider 啟動 bug：`MongoGuildBbsRepository` 的 `ux_guild_bbs_id` unique index 在 `_id` 上無效（`The field 'unique' is not valid for an _id index specification`）；切換 LiteDb provider 繞過，diag13 加上 `$env:Persistence__Provider = 'LiteDb'` 修正
- **執行** Play! 自動點擊不穩定（D3D CreateDevice 未被觸發），需手動點 Play! 才能進入遊戲
- **PASS** 基線進圖驗證通過：`PLAYER_LOGGEDIN` + `SET_FIELD → 100000000` + `26 NPC spawn` + 無 exception；續留穩定
- **PASS** 7 項 in-game 操作驗證全通：A1 移動、A2 聊天、A3 表情、B1 NPC 對話×2、C1 開背包、E1 走 portal 換圖
- **FAIL** D1 Cash Shop：進入到一半斷線回登入畫面（reconnect 架構 bug）
- **跳過** C2-C8/F1-F3/G2-G3/H2（角色 Lv1 空身，client 側不送 opcode）

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> ✅ 完成。下一步：增強 seed 腳本（給角色武器/道具/等級）→ 補完跳過項（C2-C8/F1-F3/G2-G3/H2）+ 修 Cash Shop reconnect 架構（獨立 CS port 8700 或修 OperationCanceledException 時序）。

## ✅ 結果與結論

**判定：基線 PASS（7 項 in-game PASS；Cash Shop FAIL；4 類受限跳過）**

### PASS
| 項目 | 操作 | Server 驗證 |
|------|------|-------------|
| 基線穩定性 | 進圖+續留 | `PLAYER_LOGGEDIN` + `SET_FIELD → 100000000` + `26 NPC spawn` + 無 exception |
| A1 走路移動 | 方向鍵 | client 穩定連線 |
| A2 一般聊天 | Enter→打字→Enter | `角色 TestHero 地圖聊天「1」` |
| A3 表情 | F1-F7 | client 不崩（broadcast 無 Info log 正常）|
| B1 點 NPC 1012000 | 點擊 NPC | `NPC 1012000 對話開始` |
| B1 點 NPC 9330012 | 點擊 NPC | `NPC 9330012 對話開始` |
| C1 開背包 | 按 I | client 不崩 |
| E1 走 portal 換圖 | 走到 portal | `TestHero 走 portal 'west00' → 地圖 104040000`（39 怪物 replay）|

### FAIL
| 項目 | 現象 | 根因 |
|------|------|------|
| D1 Cash Shop | 進入到一半斷線回登入畫面 | Java OdinMS CS 在獨立 port 8700；MapleForge 導回同 channel port 8585 + OperationCanceledException 太早關連線，客戶端收不到 redirect 封包。需修 CS reconnect 架構 |

### 受限跳過（角色 Lv1 空身）
- C2-C8 背包操作（無道具）
- F1-F3 戰鬥（無武器，client 側不送 opcode）
- G2-G3 配點（無 SP/AP）
- H2 組隊（未達 10 等）

### 附帶發現
- MongoDB provider 啟動 bug：`MongoGuildBbsRepository` 的 `ux_guild_bbs_id` unique index 在 `_id` 上無效（`The field 'unique' is not valid for an _id index specification`），需用 LiteDb provider 繞過
- diag13-enter-game.ps1 的 Play! 自動點擊不穩定（D3D CreateDevice 未被觸發），需手動點 Play!
- diag13 已加上 `$env:Persistence__Provider = 'LiteDb'` 修正

## 🔗 產出

- 驗證記錄：本任務歷程
- 附帶修正：`scripts/diag/diag13-enter-game.ps1` 加入 `$env:Persistence__Provider = 'LiteDb'`
- 待辦：增強 seed 腳本（武器+紅水+meso+等級）+ 修 Cash Shop reconnect 架構
