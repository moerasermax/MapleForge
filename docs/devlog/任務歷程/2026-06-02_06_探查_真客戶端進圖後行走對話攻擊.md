---
編號: 2026-06-02_06
標題: 真客戶端進圖後：續留地圖 → 行走 → 對話 → 攻擊
類型: 探查
狀態: 🚧 執行中（✅續留地圖達成；行走/對話/攻擊續攻）
建立: 2026-06-02 14:35
更新: 2026-06-02 15:30
關聯里程碑: M3-7 移動 / M4 戰鬥 / blocker#3 in-game
關聯記憶: current-state-resume, default-collaborate-with-ai-team, realclient-full-automation-decision
關聯commit: 待填
---

## 🎯 目標（執行前先寫死，過程不偷改）

> 讓真客戶端進地圖後**能正常遊玩**：①角色續留地圖(不斷線) ②行走 ③NPC 對話 ④攻擊。
> **本任務先攻第一關卡判準**：確認真客戶端進圖後**是否續留地圖**(server channel 連線不斷、有 in-game 視窗)，並補到 map-render 截圖。續留成立才談行走/對話/攻擊。
> ＊大目標分階段，本檔先收斂「續留+行走」；對話/攻擊視情況另開。

## 📋 背景與假設

- 前置：真客戶端全自動進地圖已達成(task `2026-06-02_04`，server `已進入地圖 100000000` 可重現)。
- 待釐清(task04 遺留)：server log `已進入地圖`後緊接`離開地圖`——**是 diag13 finally 殺客戶端、還是真客戶端自己斷線**(後者=in-game/SET_FIELD 協定不完整)？進遊戲後 `FindByClass("MapleStoryClass")` 抓不到窗→**視窗 class 可能變了**。
- 行走/攻擊預期走 **DirectInput**(windower 的 DInput hook 本來就可能能做，見 [[keyboard-injection-investigation]])。
- 工作模式(CLAUDE.md)：卡關預設找團隊；自動化用「動作→驗訊號→分支」。

## 🪜 計畫步驟

- [ ] 1. GUI run：進圖後**不馬上殺**客戶端，enum 該 pid any-class 視窗、印 class、截 map-render；觀察 channel 連線是否自行斷(server log 看`離開地圖`時間 vs 我 kill)。
- [ ] 2. 若自行斷線→找團隊(claude-ultra v113 / Codex windower)查 in-game 協定缺什麼(keepalive ping？spawn 後續封包？)。
- [ ] 3. 續留成立→行走：windower DInput hook 注入方向鍵，server 看 MOVE_PLAYER 廣播(M3-7 已實作)。
- [ ] 4.（後續）NPC 對話、攻擊。

## 📜 執行歷程（邊做邊追加，附時間）

- **14:35** commit/push 完成(master 8491db4)後開本檔。
- **14:4x ★關卡定位**：GUI run(進圖後觀察迴圈)→ **真客戶端進圖後 ~4 秒「自己斷線」(非我 kill)**：`[t+4s] client存活=True channel離開=True 窗class=#32770`。`#32770`=Windows 對話框,截圖 `enter-map-1.png` 讀到 **「error code : 38（到達檔案結尾。）」=EOF**。→ **我們的 SET_FIELD/addCharacterInfo 封包缺欄位/太短,真客戶端解析時讀超過封包尾→報錯斷線**(headless 之前「過」是只認 opcode 不校驗內容)。這就是 walk/talk/attack 的前置關卡=**SET_FIELD 協定完整性**。下一步:讀我方 `V113ChannelPackets.SetField` + 找團隊查 v113 setField 結構缺什麼。
- **14:5x 找團隊查 SET_FIELD EOF**：讀了我方 `V113ChannelPackets.cs`(多處標「猜的」:pet OIDs 24B/TMS padding 25B+5×byte1/CRand 4int/尾端 4short)。並行召喚 **claude-ultra(PID30544,接續) + agy(PID14304,逐欄對 Java)** 分析 v113 getCharInfo 缺哪些欄位致 EOF。
- **15:0x ★EOF 根因破解(claude-ultra + agy 雙方一致)**：①🔴**P0 主因 `AddEquipItemInfo` 每件裝備 −~10B+順序錯**(Java 是先寫位置 pos 取絕對值 1 byte→再 type 1 byte;我方先 type→pos 寫成 short;裝備數值 Java 15 short 我 14;owner Java short 我 WriteInt;incSkill byte+itemExp int 我漏/寫 short;tracking long 8B 我沒寫)。TestHero 有初心者裝備→一進圖即爆。②🟠**P1 CRand 4 int→應 3**(+4B 位移)。③🟡**P2 AddRingInfo 3 short→應 4**。④AddRocksInfo 順序反(應 5 VIP→10 一般,值錯非EOF)。淨差≈+4−10×裝備數−2,4件≈−38B→EOF。claude-ultra 並指**我標「猜的」5 處有 4 處對的別動**(pet OID 24/padding 25+5×1/stat 8 short 含 HP/MP 是 short 非 int/尾端 4 short)——靠 Java 才沒誤改。**此題一般協作即解→非大難題→不需 ultra 模式**(符合 [[escalate-ultra-mode-needs-team-consensus]])。下一步:讀 Java addItemInfo byte-exact→改 V113ChannelPackets→rebuild→hands-off 測客戶端是否續留。
- **15:1x SET_FIELD 修正已套用**(對 Java byte-exact):①CRand 4→3 int ②AddEquipItemInfo 全重寫(位置先寫 1 byte 絕對值→type→...15 short→incSkill byte→itemExp int→tracking long) ③AddRingInfo 3→4 short ④AddInventoryInfo marker 8→7。build 0 錯誤、Adapters.V113 測試 29/29 綠。**註:重度對照 v113 Java(repo內),已記哪些靠 Java;黃易無 Java 時這類欄位靠擷取解碼自證。**
- **15:2x~15:5x 多輪 live 驗證撞登入失敗**：我一度誤推「焦點被搶」。**使用者看畫面糾正=「帳號密碼錯誤」**，並建議全程截圖/從畫面診斷(記 [[gui-test-record-and-diagnose-visually]])。看截圖+grep server 失敗行→真因:**注入出大寫**,server 收到 `帳密錯誤 'TESTUSER'`(全大寫)。帳號大寫沒差(server ToLowerInvariant 正規化),但**密碼 BCrypt 大小寫敏感→TEST1234≠test1234→密碼不符**。caps lock 現查是 OFF→疑那幾輪 caps 開 或 windower 注入帶 shift。
- **對策**:diag13 加 `ClearCapsLock`(注入前強制關 caps)。使用者要求截圖定時清理(已清 78→42MB)。
- **15:30 ★★EOF 修正 live 驗證成功 — 真客戶端續留地圖!**：caps 守衛生效(登入 try1 過,帳號正確)→進地圖後**觀察迴圈整 24 秒全程**:`client存活=True channel離開=False 窗class=MapleStoryClass(808×631)`——**channel 不再 4 秒自斷、視窗是遊戲畫面不是 #32770 錯誤框、server 無「離開地圖」**。map 截圖 `enter-map-1~6.png` 每張 ~330KB(完整遊戲畫面);`enter-map-6.png` 肉眼確認:**TestHero 在 Henesys 地圖,有小地圖/HUD/NPC對話框/技能列**。SET_FIELD 補欄位(CRand3/裝備重寫/Ring4/marker7)根除 error 38。**進地圖前置關卡正式打通,SET_FIELD 經「真客戶端續留」行為神諭驗證(可升非-unverified)。** 下一步:行走(windower DInput 方向鍵)→對話→攻擊。caps 守衛=暫時對策,windower 注入大小寫應由 Codex 治本(case-exact 不依賴 caps)。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> 待做「步驟1」：改 diag13 進圖後**不殺客戶端**(或延長到 ~20s)、enum any-class 視窗截 render、觀察 channel 是否自行斷線。需使用者 hands-off ~4 分鐘。先確認「續留」再談行走。
> ＊kbd.txt 寫鎖間歇競爭仍在(每輪前清乾淨+等 2-3s;必要時交 Codex 修 windower 開檔 share mode)。

## ✅ 結果與結論

> （待補）

## 🔗 產出

> （待補）
