---
編號: 2026-09-25_84
標題: P082 — SUMMON/PUPPET buff 取消或到期時移除召喚獸
類型: 移植
狀態: ✅ 完成
建立: 2026-09-25
更新: 2026-09-25
關聯里程碑: P082（/goal 第 12 件）
關聯記憶: <空>
關聯commit: 見 git log（P082）
---

## 🎯 目標（執行前先寫死，過程不偷改）

接續 P081：召喚獸要跟著 SUMMON/PUPPET buff 一起結束。對照 Java `deregisterBuffStats`，在「客戶端 CANCEL_BUFF」與「到期取消」
兩條路徑移除召喚獸並全圖廣播 `removeSummon(summon, true)`。完成判準：測試綠燈、build 0/0、全測試無退化。

## 📋 背景與查證

- Java `MapleCharacter.deregisterBuffStats`（1832 行起）：每個被移除的 stat 若是 SUMMON 或 PUPPET，取 `mbsvh.effect.getSourceId()`
  → `summons.get(summonId)` → `map.broadcastMessage(removeSummon(summon, true))` + `map.removeMapObject` + `summons.remove`。
- MapleForge 兩條 buff 結束路徑：`HandleCancelBuffAsync`（`SkillService.CancelBuff`）與每個收到的封包前的 `SendExpiredBuffCancelsAsync`
  （`SkillService.CancelExpiredBuffs`）；兩者都回 `PlayerBuffCancellation(SourceId, Stats)`，足以比對。
- 注意：MapleForge buff 到期目前是「玩家送任何封包時順便檢查」，玩家完全閒置時不會到期（Java 用 BuffTimer 排程）。另開候選。

## 🪜 計畫步驟

- [x] 1. `SummonService.RemoveForCancelledBuffs(field, ownerId, cancellations)`（只看含 SUMMON/PUPPET stat 的取消）
- [x] 2. 連線 handler `RemoveCancelledSummonsAsync`，兩條路徑都呼叫；到期路徑改為直接取 cancellations（封包不變）
- [x] 3. 測試、build、全測試、三本帳、commit

## 📜 執行歷程（邊做邊追加，附時間）

- 依計畫實作；`V113SkillMoveHandler.CancelExpiredBuffs` 保留給既有測試使用，連線 handler 改直接呼叫 `SkillService` 取得 cancellations 再組同樣的 `CancelBuff` 封包。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> 已完成並 commit。下一步 P083：換圖/登出時清除玩家的召喚獸（Java `changeMapInternal`/`removalTask`），或 buff 到期移入世界 tick。

## ✅ 結果與結論

- 達標：召喚獸在 buff 時間到或玩家取消時從所有人畫面消失。
- 1133 passed / 1 skipped（P081 基準 1130 +3：Application 316→319）；build 0/0；禁區 grep clean。

## 🔗 產出

- 修改：`src/Maple.Application/Skills/SummonService.cs`、`src/Maple.Adapters.V113/Channel/V113ChannelConnectionHandler.cs`
- 測試：`tests/Maple.Application.Tests/Skills/SummonServiceTests.cs`
