---
編號: 2026-07-07_02
標題: P003 D7b 外部審查缺陷修補
類型: 修補
狀態: ✅ 完成
建立: 2026-07-07 03:34
更新: 2026-07-07 03:40
關聯里程碑: P003
關聯記憶:
關聯commit: 本提交（P003-D7b 修補外部審查缺陷）
---

## 🎯 目標（執行前先寫死，過程不偷改）

> 修補外部審查指出的四項 P003 D7b 缺陷：CashShop coupon redeemed S2C 版型錯位、HiredMerchant ClaimAsync 重複領取 race、coupon period<=0 永久語意、HiredMerchant S2C fixture 缺 unverified 註記。完成判準：`dotnet build` 綠、動到的測試專案單跑綠、全專案零退化（基線 772+1skip）、禁區 grep clean、commit 並 push。

## 📋 背景與假設

> 基底 HEAD 7a6f347。Java OdinMS 是行為 oracle，但 S2C fixture 仍非真 client ground truth，需標示 unverified。Core/Application 禁 v113 byte layout，Application 不引入 static mutable state；並發鎖若需要只能掛在 DI service instance。

## 🪜 計畫步驟

- [x] 1. 對照 Java `MTSCSPacket.showCouponRedeemedItem` 與本地 CashShop packet fixture，修正 byte layout。
- [x] 2. 修正 `PlayerShopService.ClaimAsync` 競態並補並發測試；檢查商人購買/收錢同型競態。
- [x] 3. 修正 CashShop coupon `days <= 0` 永久語意並補測試。
- [x] 4. 補 HiredMerchant S2C exact byte fixture 的 unverified 註記。
- [x] 5. 跑 build、相關/全專案測試、禁區 grep，更新文件並 commit/push。

## 📜 執行歷程（邊做邊追加，附時間）

- **03:34** 建立 D7b 任務歷程；下一步讀 Java oracle 與四個目標檔。
- **03:40** 完成四項修補：coupon redeemed 改 Java multi-item 版型；`PlayerShopService` 加 owner/store instance lock；coupon `Time=0` 改永久；HiredMerchant S2C fixture 補 unverified 註記。驗證：build 綠；Application 153、Adapters 426+1skip、Core 111、Persistence 11、Content 21、Net 2、PacketDecoder 22、HeadlessClient 29；禁區 grep clean。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> D7b 程式、測試、文件與 commit 已完成，下一步 push。

## ✅ 結果與結論

> 達標。外部審查四項缺陷已修補；新增 3 個測試，逐專案總測由 772+1skip 升至 775+1skip。CashShop coupon item S2C 仍依規標記為 Java-source candidate/unverified，待真 v113 client capture 校準。

## 🔗 產出

> 主要檔案：`V113CashShopPackets.cs`、`CashShopService.cs`、`PlayerShopService.cs`、CashShop/PlayerShop/Adapters 測試與本任務文件。commit：本提交。
