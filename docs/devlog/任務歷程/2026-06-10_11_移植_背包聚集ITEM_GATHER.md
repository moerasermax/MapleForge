---
編號: 2026-06-10_11
標題: 移植背包聚集 ITEM_GATHER
類型: 移植
狀態: 完成
建立: 2026-06-10
更新: 2026-06-10
關聯里程碑: Java→.NET 移植主線 / 背包
關聯記憶: v113-pivot-port-from-java
關聯commit: 未提交
---

## 目標

移植舊 Java `InventoryHandler.ItemGather` 主幹，將指定背包依 itemId 重新聚集到前方格位，並回完成封包。

完成判準：

1. Core `Inventory` 支援版本無關 gather 行為。
2. v113 adapter 接上 `ITEM_GATHER(0x40)` parser。
3. Channel handler 更新角色背包快照並送完成/放行封包。
4. 有 Core 與 Adapter 測試覆蓋。

## 執行歷程

- 建檔定目標。
- 對照舊 Java `InventoryHandler.ItemGather`，確認封包為 `tick + inventory type`，完成封包使用 Java enum `SORT_ITEM_RESULT(0x33)`。
- Core `Inventory` 新增版本無關 `GatherByItemId`，依 itemId 重新塞回前方 slot。
- `V113InventoryPackets.ParseArrange` 共用解析排序/聚集 request；Channel handler 更新角色背包快照並持久化。
- 補 Core inventory arrange 測試與 Adapter 完成封包測試。

## 接手點

- 本任務已完成。後續可補 cash/pet item 特殊排序規則與真機背包按鈕 smoke。

## 結果

- 完成 `ITEM_GATHER(0x40)` 主幹移植。
- 驗證：Core 45/45、Application 83/83、Adapters.V113 165/165、Host build 0 警告 0 錯誤。
- 邊界：目前聚集語義與排序主幹一致，先不處理舊 Java cash item / pet id 的特殊比較。
