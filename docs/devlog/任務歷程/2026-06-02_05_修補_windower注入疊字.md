---
編號: 2026-06-02_05
標題: 硬化 windower 鍵盤注入「每字元疊兩次」（委派 Codex）
類型: 修補
狀態: ✅ 完成（疊字消除 + 每批 re-arm，live 驗證帳號單次 testuser 登入成功）
建立: 2026-06-02 12:46
更新: 2026-06-02 13:04
關聯里程碑: blocker#3 視覺層前置 / windower 注入可靠性
關聯記憶: keyboard-injection-investigation, roundtable-capability-not-feature-bidding
關聯commit: afe6e28
---

## 🎯 目標（執行前先寫死，過程不偷改）

> 消除 windower 鍵盤注入的「每字元送兩次」bug（testuser→tteessttuusseerr），使真客戶端自動登入帳密正確。
> **完成判準**：windower 重新編譯通過 + 我方 live（diag13）跑出帳號 `testuser`（非疊字）、server log `✓ 登入成功`。
> 分工：**程式碼修正委派 Codex（binary/injection 本行）；live GUI 驗證由我（Opus）跑** diag13。

## 📋 背景與假設

- 來源：task `2026-06-02_04` 卡在此 bug（連兩次 server log `account='tteessttuusseerr'`→CHOOSE_GENDER，到不了世界選擇）。
- **強根因假設（grep 證據）**：windower.cpp 兩條注入路徑共用 `g_keyboardQueue[]` 但各自用獨立索引前進 → 每 key 送兩次：
  - 路徑A（方法A，已驗證繞 IME）：AttachThreadInput+SendInput（en-US 佈局），索引 `g_keyboardQueueIndex`（~2219/2288/2300/2317）+ SendInput（~2641/2653）。
  - 路徑B（舊路）：PeekMessage/GetMessage 返回點偽造 synthetic WM_KEYDOWN/CHAR/KEYUP，索引 `g_keyboardMsgQueueIndex`（~2411/2426/2447/2453/2468/2542/2548/3640）+ KBD_MSG_STAGE_*。
- 修向：兩路徑互斥（方法A 為主→注入啟用時停用路徑B），確保每字元剛好一次；保留 en-US 佈局切換 + KBD_DEBUG。

## 🪜 計畫步驟

- [ ] 1. 委派 Codex（codex-ultra）讀 windower.cpp、驗證根因、改互斥、build.bat 編譯、回報。
- [ ] 2. 我 review Codex 的改動 + 編譯結果。
- [ ] 3. 我跑 diag13 live 驗：帳號非疊字、server `登入成功`。
- [ ] 4. 達標則回 task 04 續視覺進圖。

## 📜 執行歷程（邊做邊追加，附時間）

- **12:46** 開檔。grep 確認雙路徑共用佇列雙索引根因。
- **12:47** 經 ai-cli MCP 委派 **Codex (codex-ultra)，PID 4968**：附完整症狀+雙路徑根因假設+行號+build.bat 指令+「不碰 C#/不 commit/live 由我跑」。
- **12:51 Codex 完成(exit0)**：**根因與假設完全一致**(SendInput 路徑 + synthetic WM_CHAR 路徑都對同一批鍵觸發→每 printable char 兩次)。改 `windower.cpp`：新增 `IsKeyboardSyntheticMessageInjectionEnabled()`(固定關)停用 synthetic forged WM_* 消費、啟動 SendInput worker 後清 legacy queue、GetKeyState 的 hasMessageQueue 套同 gate；保留方法A 佈局/KBD_DEBUG/Tab・backspace。`build.bat` exit0(windower.dll 183808 + windower_host.exe 122880 重建,僅 vswhere 非致命警告)。
- **12:54 diag13 live** 帳號欄空→客戶端「帳號不符」對話框、server 無登入封包。疊字消了但似乎欄位收不到字。
- **12:57 diag11(全診斷)+grep inject log ★精準定位回歸**：Codex 的修消了疊字，但**只有第一批 kbd.txt 注入、後續全丟**。windower log 鐵證：`loaded string="" keys=14 → consumed by SendInput worker`(第一批 backspace OK)；之後 `loaded string="testuser"/"\t"/""/"test1234" → DROPPED: SendInput worker did not start`。即 SendInput worker 只啟動一次、不為後續 kbd.txt 寫入 re-arm → 字母全沒注入 → 帳號空。
- **12:59** resume Codex session(019e86a8) 餵回此回歸+log 鐵證，要求 worker 為每批 kbd.txt 重啟、且維持每字元一次。
- **13:01 Codex 修好回歸(exit0)**：回歸根因=`g_keyboardWorkerActive` 重入旗標到 `KeyboardInternalWorkerProc` 最後才清，而最後含 `ScanLoginTextAnchors()` 記憶體掃描；第一批送完鍵後 worker 還在掃描→後續批次被判 worker still active→走清 queue drop 分支。修=送鍵階段(`TryAttachThreadInputSendText`)返回即 `InterlockedExchange(g_keyboardWorkerActive,0)` re-arm,掃描不阻塞下批;synthetic 仍停用。build exit0(13:01)。
- **13:03 diag11 全診斷 live 複驗 → ✅ 全綠**：server `✓ 登入成功 account='testuser'`(單次,不疊字);windower inject log **5 批全 `consumed by SendInput worker`**(""×14→testuser→\t→""×14→test1234,無 dropped);WndProc T-E-S-T-1-2-3-4 每字一次;SendInput 每字一次;VK_PROCESSKEY=0、WM_CHAR=43。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> ✅ 完成。windower 注入已可靠(每字一次+每批 re-arm+方法A 保留)。**下一步＝回 task `2026-06-02_04`（真客戶端視覺進地圖）**：登入已穩，跑 `diag13-enter-game.ps1` 看世界/頻道/選角畫面截圖逐次逼近座標→進地圖。

## ✅ 結果與結論

> **達標（委派 Codex 兩輪修，我 live 驗證）。** windower 鍵盤注入疊字根除：
> - 第1輪：synthetic WM_CHAR 路徑與 SendInput 路徑雙送 → Codex 停用 synthetic（gate `IsKeyboardSyntheticMessageInjectionEnabled` 固定關）。
> - 第2輪（回歸）：`g_keyboardWorkerActive` 重入旗標到 worker 最後(含 ScanLoginTextAnchors 掃描)才清→後續批次被判 active 而 drop → Codex 改送鍵階段返回即 re-arm。
> - live：account='testuser' 單次、5 批全 consumed、每字一次、登入成功。
> **分工心法（印證圓桌）**：C++/binary/injection 由 Codex（本行）修、Opus 出精準根因假設+log 鐵證+live 驗證閘門。我給的「雙路徑共用佇列雙索引」假設一次命中，回歸也靠 windower 自己的 log（loaded/consumed/dropped）秒定位 → **好的 instrumentation log 是跨 agent 協作的關鍵**。

## 🔗 產出

- 改檔（Codex）：`tools/windower/windower.cpp`（停用 synthetic 注入 gate + worker 每批 re-arm）；重建 `windower.dll`/`windower_host.exe`
- 驗證：diag11 全診斷 log（account 單次 / 每批 consumed / 每字一次）
- 記憶更新：`keyboard-injection-investigation`、`current-state-resume`
- 解除阻塞：task `2026-06-02_04` 可續
- commit：待填
