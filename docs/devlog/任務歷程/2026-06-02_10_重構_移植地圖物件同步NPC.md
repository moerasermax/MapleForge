---
編號: 2026-06-02_10
標題: 移植地圖物件同步（進場 spawn NPC，參照 Java MapleMap+spawnNPC）
類型: 重構
狀態: ✅ 完成
建立: 2026-06-02 17:00
更新: 2026-06-02 18:30
關聯里程碑: 移植路線圖 ①in-game 基礎（最後一塊）
關聯記憶: v113-pivot-port-from-java, current-state-resume
關聯commit: 待填（本次）
---

## 🎯 目標（執行前先寫死，過程不偷改）

> 參照 Java（`MapleMapFactory.loadLife` 載 WZ life→NPC、`MaplePacketCreator.spawnNPC`/`spawnNPCRequestController`、`MapleNPC.sendSpawnData`），把地圖 **NPC 同步給進場客戶端**：進地圖（SET_FIELD 之後）時，server 把該地圖所有可見 NPC 的 SPAWN_NPC(+controller) 封包送給該玩家。
>
> **完成判準**：
> 1. `MapData` 含靜態 NPC 清單（從 WZ `life` 節點 type=`n` 載入：npcId/x/cy/f/fh/rx0/rx1/hide）。
> 2. Core/World `Npc : IFieldObject`（ObjectId/Position/Type=Npc）。
> 3. `V113MapPackets.SpawnNpc` / `SpawnNpcRequestController` 封包建構器，位元組布局對照 Java（SPAWN_NPC=0xF9、SPAWN_NPC_REQUEST_CONTROLLER=0xFB、REMOVE_NPC=0xFA）。
> 4. channel handler 進場時對該地圖每個非隱藏 NPC 送 spawn(+controller)。
> 5. 單元測試綠：MapService 載 Henesys(100000000) NPC 非空 + 封包結構斷言 + Core Npc。
> 6. Adapters.V113 / Application 既有測試不退（34 / 25 起跳）。

## 📋 背景與假設

> in-game 基礎路線圖最後一塊（移動 08、聊天 09 已完成）。架構決策見 `docs/design/in-game-執行期狀態架構.md`：NPC=IFieldObject、靜態資料在 MapData、執行期在 Core/World。
> Java ground truth：
> - `MapleMapFactory.loadLife`：life 子節點(numbered) 有 type/id/cy/f/fh/rx0/rx1/x/y/hide；type 字串首字 `n`=NPC。
> - `spawnNPC`：[short 0xF9][int objectId][int npcId][short x][short cy][byte (f==1?0:1)][short fh][short rx0][short rx1][byte show]。
> - `spawnNPCRequestController`：[short 0xFB][byte 1][int objectId][int npcId][short x][short cy][byte (f==1?0:1)][short fh][short rx0][short rx1][byte miniMap]。
> - opcode 直接沿用 Java SendPacketOpcode 原值（已驗 SPAWN_PLAYER/CHATTEXT/MOVE_PLAYER 皆 1:1）。
> 假設：WZ life 節點子節點命名為 "0","1",...（同 portal）；NPC objectId 暫用 handler 內每連線計數器（base 1000，避開玩家 charId 小號）——proper 每-Field 配發器待 IFieldRegistry 重構落地（架構風險#5）。NPC 對話/controller 互動回應留待 M5（Jint）。

## 🪜 計畫步驟

- [ ] 1. Core/Maps `MapNpc`（靜態 WZ NPC def）+ MapData.Npcs
- [ ] 2. MapService.LoadNpcs（讀 life 節點 type 首字 n）
- [ ] 3. Core/World `Npc : IFieldObject`
- [ ] 4. Adapters opcodes（SpawnNpc/RemoveNpc/SpawnNpcController）+ V113MapPackets.SpawnNpc/SpawnNpcRequestController
- [ ] 5. handler 注入 MapService、進場送 NPC spawn
- [ ] 6. 單元測試（MapService NPC 載入 / 封包結構 / Core Npc）
- [ ] 7. 跑測 Adapters.V113 + Application 綠 → commit/push → 回填三本帳

## 📜 執行歷程（邊做邊追加，附時間）

- **17:00** 讀完 Java MapleMapFactory.loadLife / MaplePacketCreator.spawnNPC(+controller) / SendPacketOpcode（0xF9/0xFA/0xFB）/ MapleNPC.sendSpawnData，及 MapleForge 現況（MapData 只有 portal/foothold、handler 用 IMapSessionRegistry 未接 FieldInstance）。定目標、開工。
- **17:20 [使用者要求暫停—用量快見底]** 已落地步驟 1-4 + 步驟5一半：
  - ✅ Core/Maps `MapNpc.cs`（NpcId/X/Cy/F/Fh/Rx0/Rx1/Hide）
  - ✅ `MapData.Npcs`
  - ✅ `MapService.LoadNpcs`（讀 life 節點、type 首字 n、id 字串 parse）
  - ✅ Core/World `Npc.cs`（IFieldObject，組合持 MapNpc + ObjectId，Position 由 X/Cy/Fh 建）
  - ✅ opcodes：`V113ChannelSendOp.SpawnNpc=0xF9 / RemoveNpc=0xFA / SpawnNpcRequestController=0xFB`
  - ✅ `V113MapPackets`：`SpawnNpc(Npc,show)` / `SpawnNpcRequestController(Npc,miniMap)` / `RemoveNpc(int)` + 共用 `WriteNpcBody`（**改良**：吃 Core `Npc` 物件而非散參數，spawn/controller 共用 body）
  - 🟡 handler 建構子已加 `MapService _mapService` 參數（DI 無需改：MapService 已是 singleton，會自動注入）

- **17:45** 步驟 5-6 全落地：handler `SpawnMapNpcsAsync`(進場對每個 `!Hide && NpcId<9901000` 的 NPC 配發 objectId base 1000、送 SpawnNpc+Controller)；測試三檔——`ChannelNpcPacketTests`(SpawnNpc/Controller 黃金向量逐位元組 + f==1→dir0 + RemoveNpc 結構)、`FieldWorldTests` +2(Npc Position 由 def 推得、Field 同時容 player+npc)、`MapServiceTests` +1(Henesys NPC 非空)。自推黃金向量(LE)：SpawnNpc=`F9 00 E8 03 00 00 40 54 89 00 64 00 C8 00 01 05 00 50 00 78 00 01`、Controller=`FB 00 01 …(同 body)… 01`。
- **17:45 [阻塞]** `dotnet test` 與 ai-cli 分發都被擋：`claude-sonnet-4-6`(權限分類器)暫時不可用→auto 模式無法判定非唯讀動作安全性。已重試多次未恢復，停止空轉。
- **18:30 [新 session 接手—解除阻塞、收尾]**
  - ✅ **跑測全綠**：Adapters.V113 **38**(含 4 新 NPC 測試,34→38)、Application **28**(FieldWorld +2、MapService +1,25→28)。
  - ✅ **改用「直接比對真 Java 源」取代 Codex 覆核**（更強 ground truth，消除自推不確定性）。逐位元組核對 `MaplePacketCreator.spawnNPC/removeNPC/spawnNPCRequestController`(531-584) + `SendPacketOpcode`(0xF9/0xFA/0xFB) + `MapleMapFactory.loadLife`(473-492) + `MapleNPC.sendSpawnData`(53-58)：
    - spawnNPC 欄位序 objectId/npcId/x/cy/dir(f==1?0:1)/fh/rx0/rx1/show **完全吻合**。
    - controller 前綴 `byte 1`、尾 `miniMap` **吻合**；removeNPC = opcode+int **吻合**。
    - WZ 載入 cy/x/f/fh/rx0/rx1/hide 對照 loadLife **吻合**（Java 用 `type.equals("n")`、我方 `type[0]=='n'` 首字判斷——對 NPC 等價，無其他 n 開頭 life 類型）。
    - 送包順序 spawnNPC→controller、`id<9901000` gate 與 sendSpawnData **完全吻合**。
  - 自推黃金向量 `F9 00 E8 03 00 00 40 54 89 00 64 00 C8 00 01 05 00 50 00 78 00 01` 解出 objectId=1000/npcId=9000000/x=137/cy=100/dir=1/fh=5/rx0=80/rx1=120/show=1，與 Java 布局自洽，測試斷言通過。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> ✅ 本任務已收尾、commit+push 完成。**下一步＝路線圖②：NPC 對話/商店（移植 Jint 腳本引擎 + NPCConversationManager + c2s NPCHandler talk）**，或先做「真客戶端進圖看得到 NPC」里程碑 smoke（diag13 進圖後截圖確認 Henesys NPC 出現）。proper 每-Field objectId 配發器 + IFieldRegistry 重構（架構風險#5）仍待辦。

## ✅ 結果與結論

> 地圖 NPC 同步移植完成，逐位元組對齊真 Java 源。進地圖（SET_FIELD 後）server 對該地圖每個非隱藏、非 PlayerNPC(id<9901000) 的 NPC 送 `SpawnNpc`+`SpawnNpcRequestController`。測試 Adapters.V113 38 / Application 28 全綠。NPC objectId 暫用每連線計數器(base 1000)；NPC talk/controller c2s 回應留 M5（Jint）。

## 🔗 產出

> - Core：`Maps/MapNpc.cs`(新,WZ NPC def)、`Maps/MapData.cs`(+Npcs)、`World/Npc.cs`(新,IFieldObject)
> - Application：`Maps/MapService.cs`(LoadNpcs,讀 life type 首字 n)
> - Adapters：`Channel/V113ChannelOpcodes.cs`(SpawnNpc/RemoveNpc/SpawnNpcRequestController)、`Channel/V113MapPackets.cs`(SpawnNpc/Controller/RemoveNpc+WriteNpcBody)、`Channel/V113ChannelConnectionHandler.cs`(SpawnMapNpcsAsync)
> - 測試：`tests/Maple.Adapters.V113.Tests/ChannelNpcPacketTests.cs`(新,黃金向量)、`FieldWorldTests.cs`(+2)、`Maps/MapServiceTests.cs`(+1)
