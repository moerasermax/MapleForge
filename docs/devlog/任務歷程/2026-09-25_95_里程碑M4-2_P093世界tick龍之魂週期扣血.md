---
編號: 2026-09-25_95
標題: P093 — M4-2 世界 tick：龍之魂（DRAGONBLOOD，職業 131/132）每 4 秒扣血
類型: 移植
狀態: ✅ 完成
建立: 2026-09-25
更新: 2026-09-25
關聯里程碑: M4-2 / P093（/goal 第 23 件）
關聯記憶: <空>
關聯commit: 見 git log（P093）
---

## 🎯 目標（執行前先寫死，過程不偷改）

對照 Java `handleCooldowns` 的 job 131/132 分支 `canBlood(now)` + `doDragonBlood`：龍之魂期間每 4 秒扣 `x` HP 並顯示 buff 特效，
HP 不夠時取消 buff。沿用 P092 的 `PeriodicBuffTick` 模式。完成判準：測試綠燈、build 0/0、全測試無退化。

## 📋 背景與查證

- Java `registerEffect`：`isDragonBlood()` → `prepareDragonBlood()`；`canBlood(now)`：`lastDragonBloodTime > 0 && + 4000 < now`。
- Java `doDragonBlood`：無 buff → 重設 0；`prepareDragonBlood()`；`hp - x <= 1` → `cancelEffectFromBuffStat(DRAGONBLOOD)`；否則 `addHP(-x)` +
  `showOwnBuffEffect(src, 5)` + 沒有 MORPH 時 `broadcastMessage(this, showBuffeffect(id, src, 5), false)`。
- Java `handleCooldowns` isAlive 區塊：`job == 131 || job == 132` 才跑 Dragon Blood/Berserk，順序 Dragon Blood → Berserk → Recovery → hurt。

## 🪜 計畫步驟

- [x] 1. Core `Player.TryDragonBlood(now)`；`PeriodicBuffTick` 加 `SourceId`
- [x] 2. Application `SkillService.TryDragonBlood`（職業 131/132 判斷）
- [x] 3. Adapter `ShowOwnBuffEffect`/`ShowForeignBuffEffect` + `TickDragonBloodAsync`（在回復術之前）；測試、build、全測試、三本帳、活文件、commit

## 📜 執行歷程（邊做邊追加，附時間）

- 依計畫實作；Berserk（職業 132 的 HP 門檻特效 `doBerserk`）屬另一件，未做。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> 已完成並 commit。候選：Berserk 特效；寵物飢餓；坐騎疲勞。

## ✅ 結果與結論

- 達標：龍騎士開龍之魂後每 4 秒扣血並顯示特效，HP 不足自動解除。
- 1169 passed / 1 skipped（P092 基準 1162 +7：Core 175→177、Application 332→336、Adapters.V113 570→571）；build 0/0；禁區 grep clean。封包 unverified。

## 🔗 產出

- 修改：`src/Maple.Core/World/Player.PeriodicBuffs.cs`、`src/Maple.Application/Skills/SkillService.cs`、`src/Maple.Adapters.V113/Channel/V113SkillPackets.cs`、`src/Maple.Adapters.V113/Channel/V113PlayerTickHandler.cs`
- 測試：`tests/Maple.Core.Tests/Skills/PlayerPeriodicBuffTests.cs`、`tests/Maple.Application.Tests/Skills/SkillServiceTests.cs`、`tests/Maple.Adapters.V113.Tests/ChannelPlayerTickHandlerTests.cs`
- 文件：`docs/design/世界tick-設計.md`、`docs/specs/v113-protocol-spec.md`
