---
編號: 2026-09-25_87
標題: P085 — 換圖後跟隨型召喚獸在新地圖落地位置重新出生
類型: 移植
狀態: ✅ 完成
建立: 2026-09-25
更新: 2026-09-25
關聯里程碑: P085（/goal 第 15 件）
關聯記憶: <空>
關聯commit: 見 git log（P085）
---

## 🎯 目標（執行前先寫死，過程不偷改）

接 P083（跟隨型召喚獸從舊地圖靜默移出）與 P084（落地位置正確）：對照 Java `MapleMap.addPlayer` 的 SUMMON 分支，換圖後
若 SUMMON buff 仍在，把召喚獸放到主人位置重新出生並廣播。完成判準：測試綠燈、build 0/0、全測試無退化。

## 📋 背景與查證

- Java `MapleMap.addPlayer`（2156-2167 行）：`getStatForBuff(SUMMON)` 不為 null → `summons.get(stat.getSourceId())` →
  `setPosition(chr.getPosition())` + foothold → `spawnSummon(summon)`（`spawnAndAddRangedMapObject` 重新分配物件 ID 並廣播）。
- 只有 SUMMON buff 的召喚獸會被帶走（PUPPET 與原地型在 `removePlayer` 已取消，P083）。
- MapleForge `Summon.ObjectId` 唯讀 → 以同技能/等級/HP/移動型態在新 field 建立新實例（對應 Java 重新分配 OID）。

## 🪜 計畫步驟

- [x] 1. `SummonService.Reattach(field, owner, carried, position)`（SUMMON buff 仍由該技能提供才重生）
- [x] 2. `WarpAsync`：保留 `DetachSummonsOnLeaveAsync` 回傳的 Carried，新地圖初始化/重播完後重生並 `BroadcastPacketToMapAsync`
- [x] 3. 測試、build、全測試、三本帳、commit

## 📜 執行歷程（邊做邊追加，附時間）

- 依計畫實作；位置用 P084 同步後的 `player.Position`（= 落地 portal）。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> 已完成並 commit。下一步 P086：新進場玩家看得到既有召喚獸（Java `sendSpawnData` 重播），比照 `SendFieldDropsAsync`。

## ✅ 結果與結論

- 達標：帶著聖龍/鳥類/四轉召喚換圖，召喚獸會跟到新地圖。
- 1139 passed / 1 skipped（P084 基準 1137 +2：Application 320→322）；build 0/0；禁區 grep clean。

## 🔗 產出

- 修改：`src/Maple.Application/Skills/SummonService.cs`、`src/Maple.Adapters.V113/Channel/V113ChannelConnectionHandler.cs`
- 測試：`tests/Maple.Application.Tests/Skills/SummonServiceTests.cs`
