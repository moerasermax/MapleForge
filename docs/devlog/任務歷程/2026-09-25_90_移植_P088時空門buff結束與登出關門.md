---
編號: 2026-09-25_90
標題: P088 — 時空門隨 buff 結束或主人登出/進商城時關閉並送 destroy data
類型: 移植
狀態: ✅ 完成
建立: 2026-09-25
更新: 2026-09-25
關聯里程碑: P088（/goal 第 18 件）
關聯記憶: <空>
關聯commit: 見 git log（P088）
---

## 🎯 目標（執行前先寫死，過程不偷改）

接 P087：門要跟著時空門 buff 結束而關閉，主人登出/進商城（Java `changeRemoval`）時也要關。對照 Java `cancelEffect` 的 `isMagicDoor()`
分支與 `removeDoor`/`sendDestroyData`。完成判準：關門有測試、build 0/0、全測試無退化。

## 📋 背景與查證

- Java `MapleCharacter.cancelEffect`（1913 行）：`effect.isMagicDoor() && !getDoors().isEmpty()` → `removeDoor()` + `silentPartyUpdate()`。
- Java `removeDoor`：對目標地圖、村莊地圖所有角色 `door.sendDestroyData(client)`，再從兩張地圖移除並 `clearDoors()`。
- Java `sendDestroyData` 條件：client 在目標地圖、或是主人、或是主人隊伍成員 → `removeDoor(owner, false)` +（隊伍）`partyPortal` 重設 + `spawnPortal(999999999, 999999999)`。
  所以村莊裡的非主人非隊友收不到。
- Java `changeRemoval`（登出、換頻、進商城都會呼叫）尾段：`if (!getDoors().isEmpty()) removeDoor()`。換圖不關門。
- MapleForge buff 結束兩條路徑沿用 P082 的共用 helper（改名 `ApplyBuffCancellationSideEffectsAsync`，同時處理召喚獸與門）。

## 🪜 計畫步驟

- [x] 1. `DoorService.CloseDoor(ownerId)` 回傳被關的門
- [x] 2. `CloseMagicDoorAsync`：目標地圖全員 + 在村莊的主人 → RemoveDoor + RemoveTownPortal
- [x] 3. buff 結束路徑（門技能來源）、進商城轉場、登出清理接上；測試、build、全測試、三本帳、commit

## 📜 執行歷程（邊做邊追加，附時間）

- 依計畫實作；P082 的 `RemoveCancelledSummonsAsync` 改名為 `ApplyBuffCancellationSideEffectsAsync`，field 為 null 時仍會處理關門。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> 已完成並 commit。候選：隊伍 `partyPortal`（PARTY_OPERATION 0x24）；零呼叫者掃描剩 `CreateGuildAlliance`/`MerchantBuyError`/`MarriageRingLook`。

## ✅ 結果與結論

- 達標：時空門 buff 到期/取消、主人登出或進商城後，門從目標地圖消失，村莊傳送連結重設。
- 1151 passed / 1 skipped（P087 基準 1149 +2：Application 330→332）；build 0/0；禁區 grep clean。

## 🔗 產出

- 修改：`src/Maple.Application/Maps/DoorService.cs`、`src/Maple.Adapters.V113/Channel/V113ChannelConnectionHandler.cs`
- 測試：`tests/Maple.Application.Tests/Maps/DoorServiceOpenDoorTests.cs`
