---
編號: 2026-07-06_01
標題: P003 移植清尾 + MVP stub 補完整（Codex 主刀，PM 派工留痕）
類型: 移植
狀態: ✅ 完成
建立: 2026-07-06 10:24
更新: 2026-07-07 05:30（D9 鐵律歸檔）
關聯里程碑: M4-6 / M6-1 / M6-4（stub 補完整橫跨多里程碑）
關聯記憶: current-state-resume, pm-dispatch-only-workflow, v113-pivot-port-from-java
關聯commit: f9a7c22(D1) 1bc22a1(D2) 9f7a60d(D3) bc021dc(D4b) 39f739a(D4) f64bce1(D5) 8c22d6a(D6) 7a6f347(D7) ed49640(D7b) f3eadf1(D8b) ＋文件 34a7f91 ff87115(D0)
---

## 🎯 目標（執行前先寫死，過程不偷改）

> 依已批准計畫 `docs/devlog/執行計畫/P003_移植清尾與stub補完整.md` 完成 D0-D9 全部派工：
> ①移植狀態地圖修正到與程式碼一致（❌→✅/🚫/🟨，統計重算）
> ②小件 stub 補完整（PlayerHandler 雜項 8 + CouponCode/GamePoll/MapleTV/BeansUpdate + Mob 4 依 D0b 結論）
> ③ItemMaker 完整 + SkillBook catalog 資料
> ④HiredMerchant 全套三刀（Core→Adapters→啟動重載）
> **驗收判準**：全部派工 exit 0 + 各對應測試專案綠 + 既有 645 測試零退化 + 禁區 grep（Core 無 V113 using）為空 + Review 通過 + 三本帳歸檔。每派工留五欄（PID/wait/exit/commit/驗收）。

## 📋 背景與假設

- 偵察（Sonnet PID 24116）核實：地圖標 ❌44 中 43 個已接 dispatch（~10 完整、~33 EnableActions stub）；唯一沒接的 USE_FISH_MERCHANT 在 Java 也是 disabled placeholder。
- 真缺口：HiredMerchant（Java ~1024 行）、ItemMaker（~568 行）；MonsterCarnival 延 P004（agy 估 5-7 天）；MTS 維持 stub。
- 附帶 bug：V113Opcodes.cs ClientError=0x0C/ClientFeedback=0x0F 與 Java 對調。
- 圓桌：agy（研究）✅ / Sonnet（執行席）✅ / GPT-5.5（Review 席）❌ 額度撞限——Review 由 W4 補位。
- **Codex 額度撞限至 7/7 09:43；使用者同意兌換 1 次 usage limit reset（帳上 4 次）→ W1 起跑前需使用者在 codex TUI `/usage` 兌換並回報。**
- 基線：HEAD `f638132`，645 綠；工作區有 6/19 GUI smoke 遺留未 commit 變更（D0 先 checkpoint）。

## 🪜 計畫步驟

- [x] W0: D0 地圖大修正+遺留 checkpoint（Sonnet）/ D0b Mob 四項 Java 深度研究（Sonnet）
- [x] W1: D1→D2→D3 小件 stub 補完整（Codex 單線循序，避免同檔衝突）
- [x] W2: D4 ItemMaker + D4b SkillBook catalog（Codex 平行）
- [x] W3: D5→D6→D7 HiredMerchant 三刀
- [x] W4: D8-前置(agy)→D7b(修補)→D8(GPT-5.5終審)→D8b(修補)→D8複核(agy) 全 diff Review 與修補收斂
- [x] W5: D9 測試補齊+三本帳歸檔+結果回寫

## 📜 執行歷程（邊做邊追加，附時間）

- **10:05** 偵察派工 Sonnet(24116) 完成：地圖嚴重過期，真實缺口=HiredMerchant/ItemMaker/MTS/Carnival
- **10:10** 圓桌：agy(44740)✅、Sonnet 執行席(42024)✅、GPT-5.5(30620)❌ 額度撞限（7/7 09:43 恢復）
- **10:15** 查得 Codex 帳上有 4 次 usage limit reset；使用者拍板兌換 1 次今天照跑
- **10:24** 計畫批准，建本檔+計畫檔，開始派 W0
- **10:45** W0 完成：D0(42912) 兩 commit `34a7f91`(遺留checkpoint)+`ff87115`(地圖大修正)，新統計 ✅129/🟨29/🟦1/❌0/🚫5；D0b(38652) 結論=Mob 四項只補 MONSTER_BOMB，其餘三項綁 Shammos護送/Node 子系統維持 stub 延 P004 評估
- **D1 開工** Codex 接手本刀：修正 Login `CLIENT_FEEDBACK`/`CLIENT_ERROR` opcode 對調，並對照 Java 補 `SHOW_EXP_CHAIR`、`REWARD_ITEM`、`USE_TREASUER_CHEST`、`CP_UserThrowGrenade`。先讀 MapleForge dispatch/handler/tests，再鎖定 Java `RecvPacketOpcode.java`、`PlayerHandler.java`、`InventoryHandler.java` 與相關 packet/reward 類別。
- **D1 完成** 修正 opcode 對調，新增 `V113RewardItemHandler` 與 11 條 fixture 級測試。`SHOW_EXP_CHAIR` / `CP_UserThrowGrenade` 對照 Java 均為 no-op/EnableActions；`REWARD_ITEM` / `USE_TREASUER_CHEST` 補解析、驗證、消耗與 deterministic reward path，資料源缺口以 TODO 標註。驗證：`dotnet build` 綠、Adapters 402 passed / 1 skipped、逐專案總測 714 passed / 1 skipped。
- **D2 開工** Codex 延續 HEAD `f9a7c22`：本刀處理 `ARAN_COMBO`、`CYGNUS_SUMMON`、`SNOWBALL`、`LEFT_KNOCK_BACK`。先讀 Java `PlayerHandler.java` 對應方法與 MapleForge 既有 skill/buff/event 封包，再決定 ARAN combo runtime state 放 Core 的最小形狀，以及 Snowball/knockback 是否落最小事件模型或保守增強 stub。
- **D2 完成** `ARAN_COMBO` 完成 Core runtime combo state、Application combo buff gate、Adapter `GIVE_BUFF(ARAN_COMBO)` fixture；`CYGNUS_SUMMON` 接 Java NPC script intent；`SNOWBALL` / `LEFT_KNOCK_BACK` 依 PM 接受判定做 handler parity，完整 `MapleSnowball` event 子系統延 P004。新增 8 條測試；`dotnet build` 綠，逐專案總測 722 passed / 1 skipped。
- **D3 開工** Codex 延續 HEAD `1bc22a1`：本刀處理 `COUPON_CODE`、`GAME_POLL`、`MAPLETV`、`CP_BeansUpdate`、`MONSTER_BOMB`。先對照 Java `CashShopOperation.CouponCode`、`UserInterfaceHandler.InGamePoll`、`BeanGame.BeansUpdate`、`MobHandler.handleMonsterBomb`，並確認此 Java tree 沒有 `MAPLETV` dispatch。
- **D3 完成** `COUPON_CODE` 建 Core coupon model/repository、LiteDB/Mongo persistence 與 CashShopService 兌換流程，支援 Cash/GASH、MaplePoints、item、meso；`GAME_POLL` / `MAPLETV` 做 Java parity parser + `EnableActions`；`CP_BeansUpdate` reset runtime BeansGameSession 並送 exit；`MONSTER_BOMB` 補 WZ selfD、無獎勵擊殺路徑與 kill animation 廣播。新增 11 條測試；`dotnet build` 綠，逐專案總測 733 passed / 1 skipped。
- **17:09 D4 接手** 本 session 接手 ItemMaker 完整移植；先完成必讀文件與 P003 計畫載入，接著對照 Java `ItemMakerHandler.java` / `ItemMakerFactory.java`、MapleForge 既有 catalog/RNG/inventory 模式，再落 Content catalog、Application 用例與 v113 adapter handler/tests。
- **17:38 D4 完成** `ITEM_MAKER(0x6B)` 從 `EnableActions` stub 升級為 Content `WzItemMakeCatalog` + Application `ItemMakerService` + v113 handler；WZ 實讀 61 筆寶石配方、726 筆職業裝備配方，新增 13 條 D4 測試。驗證：`dotnet build` 綠；逐專案總測 747 passed / 1 skipped；Core/Application 禁區 `rg` 無 V113 依賴。
- **D5→D7 HiredMerchant 三刀完成**（詳細敘事另存 `2026-07-06_02_移植_HiredMerchantCut1.md` / `2026-07-06_03_移植_HiredMerchantCut2Adapters.md` / `2026-07-07_01_移植_HiredMerchantCut3.md`）：Cut1（Core `IPlayerShop`/`PlayerShop`/`HiredMerchant`+`PlayerShopService`+LiteDB/Mongo，commit `f64bce1`）→ Cut2（Adapters 0x34/0x38/0x3A 接線+封包，commit `8c22d6a`）→ Cut3（`PLAYER_INTERACTION(0x73)` merchant 子指令+啟動重載+position 持久化+LiteDB E2E，commit `7a6f347`）。過程中 Codex 三度撞用量上限（W2 的 D4/D4b 共用一次、D5 一次、D7 一次）並多次 CLI 假死需 kill 續跑（D2/D4×2/D4b×2）。
- **D8-前置（agy，PID 252，唯讀）** 審 `f638132..8c22d6a`（D0-D6 全 diff）：抓到 2 BLOCKER（① `ShowCouponRedeemedItem` S2C 版型錯位有 crash 風險 `V113CashShopPackets.cs:131-143` ② `PlayerShopService.ClaimAsync` 雙重領取 race `PlayerShopService.cs:343-376`）+ 2 WARNING（③ coupon `days==0` 應為永久 `-1` `CashShopService.cs:277` ④ HiredMerchant 測試缺 unverified 註記）；判定需修補。
- **D7b 完成（PID 48452，commit `ed49640`）**：修補 D8-前置四項缺陷 — coupon redeemed item 改 Java multi-item 版型、`PlayerShopService` 加 owner/store instance lock 消除 claim race、coupon period 語意修正、HiredMerchant fixture 補 unverified 註記。詳細敘事另存 `2026-07-07_02_修補_P003D7b外部審查缺陷.md`。驗證：775 passed / 1 skipped（772→775，+3），`dotnet build` 綠，禁區 grep clean。
- **D8 完成（GPT-5.5，PID 49356，唯讀）**：終審 D7+D7b 全 diff，抓到 5 項缺陷 — ①HiredMerchant 店內 UI 廣播範圍錯誤（誤全圖廣播應只送 owner+visitors）②adapter 直接 mutate repository 繞過 `PlayerShopService` 鎖 ③`CLOSE_MERCHANT` 回錯封包版型 ④啟動 reload 例外未降級（可能拖垮整個 host 啟動）⑤D7 E2E fixture 缺 unverified 註記；判定需修補，交 D8b。
- **D8b 完成（PID 26136，commit `f3eadf1`）**：修補 D8 五項缺陷 — 新增 `IHiredMerchantSessionDispatcher` 把店內 UI 封包收斂到 owner+visitors、enter/leave/maintenance mutation 移入 `PlayerShopService` 鎖、`CLOSE_MERCHANT` 改 Java `shopErrorMessage(0x15,0)`、啟動 reload try/catch 降級、補齊 unverified 註記與訪客/非訪客廣播範圍測試。詳細敘事另存 `2026-07-07_03_修補_P003D8b終審缺陷.md`。驗證：775 passed / 1 skipped（維持不退化），`dotnet build` 綠，禁區 grep clean。
- **D8複核完成（agy，PID 35860，唯讀）**：複核 D8 五項缺陷是否確實修補＋D7b 四項是否仍成立，共 9 項全數 ✅ 確認修補到位，無新缺陷，**判定 P003 可收案**。
- **D9（本次，Sonnet）**：鐵律歸檔——回填本檔 D7/D7b/D8/D8b/D8複核 五欄、切狀態 ✅、回填任務追蹤/進度日誌/移植狀態地圖/計畫檔執行結果、產出 session report，一個 commit 收尾。

## 派工留痕（五欄）

| 派工 | PID | wait | exit | commit | 驗收 |
|------|-----|------|------|--------|------|
| 偵察 | 24116 | ✅ | 0 | —（唯讀） | ✅ 產出真實剩餘清單 |
| 圓桌-agy | 44740 | ✅ | 0 | —（討論） | ✅ 研究輸入採納 |
| 圓桌-Sonnet | 42024 | ✅ | 0 | —（討論） | ✅ 拆刀修正採納 |
| 圓桌-GPT5.5 | 30620 | ✅ | 1 | — | ❌ 額度撞限，W4 補位 |
| D0 | 42912 | ✅ | 0 | 34a7f91 + ff87115 | ✅ 地圖 ✅129/🟨29/❌0，grep 逐項核對吻合，已 push |
| D0b | 38652 | ✅ | 0 | —（唯讀） | ✅ 結論：只補 MONSTER_BOMB；BA/BC/BE 維持 stub（Shammos/Node 無地基） |
| D1 | 33980 | ✅ | 0 | f9a7c22 | ✅ opcode swap 修正；SHOW_EXP_CHAIR/ThrowGrenade Java parity；REWARD_ITEM/TREASURE_CHEST deterministic path；714 passed / 1 skipped，已 push |
| D2 | 41780→46296（CLI 中斷續跑） | ✅ | 0 | 1bc22a1 | ✅ ARAN_COMBO combo buff + CYGNUS_SUMMON/SNOWBALL/LEFT_KNOCK_BACK；722 passed / 1 skipped，已 push |
| D3 | 10448 | ✅ | 0 | 9f7a60d | ✅ COUPON_CODE 兌換全鏈/GAME_POLL/MAPLETV parity/CP_BeansUpdate/MONSTER_BOMB 無獎勵擊殺；733 passed / 1 skipped，已 push |
| D4 | 38244(撞限)→34224→47016→39448(CLI中斷×2續跑) | ✅ | 0 | 39f739a | ✅ ItemMaker catalog/service/handler 完整；WZ 61+726=787 配方；新增 13 測試；747 passed / 1 skipped，已 push |
| D4b | 42164(撞限)→13560→24828→38828(CLI中斷×2續跑) | ✅ | 0 | bc021dc | ✅ SkillBook catalog Item.wz 萃取 165 筆（228x×26/229x×139），Content 18 綠，已 push |
| D5 | 50044(18:10撞限)→45832(22:09續) | ✅ | 0 | f64bce1 | ✅ HiredMerchant Cut1 Core/App/Persistence 完成；+14 測試；761 passed / 1 skipped，禁區 grep clean，已 push |
| D6 | 53288 | ✅ | 0 | 8c22d6a | ✅ Cut2 Adapters 0x34/0x38/0x3A 接線+SPAWN/UI/Fredrick 封包（unverified）；+6 測試；767 passed / 1 skipped，已 push。發現 0x73 merchant 子指令缺口→PM 判定併入 D7 |
| D7 | 23468(23:20撞限)→22992(續) | ✅ | 0 | 7a6f347 | ✅ 0x73 merchant 子指令＋startup reload＋position 持久化＋LiteDB E2E；新增 5 測試；772 passed / 1 skipped，build 綠，禁區 grep clean |
| D8-前置 | 252 | ✅ | 0 | —（唯讀） | ✅ agy 審 f638132..8c22d6a：**2 BLOCKER**（①ShowCouponRedeemedItem 封包結構錯位 crash 風險 V113CashShopPackets.cs:131-143 ②ClaimAsync 雙重領取 race PlayerShopService.cs:343-376）+ 2 WARNING（③coupon days==0 應為永久-1 CashShopService.cs:277 ④HiredMerchant 測試缺 unverified 註記）；判定需修補。禁區/static 稽核乾淨 |
| D7b | 48452 | ✅ | 0 | ed49640 | ✅ 修補 D8-前置四項：coupon redeemed item 改 Java multi-item 版型、PlayerShopService 加 owner/store lock 消除 ClaimAsync race、coupon period<=0 改永久語意、HiredMerchant fixture 補 unverified 註記；新增 3 測試；775 passed / 1 skipped，build 綠，禁區 grep clean，已 push |
| D8 | 49356 | ✅ | 0 | —（唯讀） | ✅ GPT-5.5 終審 D7+D7b：**5 項缺陷**（①店內 UI 廣播範圍錯誤誤全圖廣播 ②adapter 直改 repository 繞過 PlayerShopService 鎖 ③CLOSE_MERCHANT 回錯封包版型 ④啟動 reload 例外未降級 ⑤D7 E2E fixture 缺 unverified 註記）；判定需修補，轉 D8b |
| D8b | 26136 | ✅ | 0 | f3eadf1 | ✅ 修補 D8 五項：新增 IHiredMerchantSessionDispatcher 收斂店內 UI 廣播範圍、enter/leave/maintenance mutation 收斂進 PlayerShopService 鎖、CLOSE_MERCHANT 改 shopErrorMessage(0x15,0)、啟動 reload try/catch 降級、補齊 unverified 註記＋訪客/非訪客廣播範圍測試；775 passed / 1 skipped（不退化），build 綠，禁區 grep clean，已 push |
| D8複核 | 35860 | ✅ | 0 | —（唯讀） | ✅ agy 複核 D8 五項＋D7b 四項共 9 項全數確認修補到位、無新缺陷，**判定 P003 可收案** |

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> **P003 已全數完成並收案**：D0-D9 全部派工 exit 0；agy 複核 9 項缺陷全 ✅；最終 775 passed / 1 skipped、`dotnet build` 0 warning/0 error、Core/Application 禁區 grep clean；三本帳（任務追蹤/進度日誌/移植狀態地圖）與計畫檔執行結果已回填，session report 已產出，commit 待 push。下一個任務請開新的任務歷程檔。

## ✅ 結果與結論

- `CLIENT_FEEDBACK` / `CLIENT_ERROR`：對照 Java `RecvPacketOpcode.java` 修正為 `CLIENT_FEEDBACK=0x0C`、`CLIENT_ERROR=0x0F`，新增 Login opcode fixture。
- `SHOW_EXP_CHAIR(0x24)`：Java `PlayerHandler.ShowExpChair` 只讀 `chairid` 並回 `enableActions`；MapleForge 改成具名 parser + fixture，維持 Java parity no-op。
- `REWARD_ITEM(0x6A)`：Java `InventoryHandler.UseRewardItem` 讀 slot/itemId、驗 reward item、抽 `Etc.wz` reward、扣箱、給獎並播放 reward animation。MapleForge 本刀完成解析/驗證/消耗/給 deterministic fallback reward `2000000 x1`/animation candidate；`Etc.wz` reward catalog 留 TODO。
- `USE_TREASUER_CHEST(0x6C)`：Java `InventoryHandler.UseTreasureChest` 讀 slot/itemId、金/銀箱驗 cash key、消耗箱+鑰匙、走 `RandomRewards` 給獎。MapleForge 本刀支援 `4280000/4280001` 與 `5490000/5490001`，給 deterministic first-entry reward；權重表與 rare broadcast 留 TODO。
- `CP_UserThrowGrenade(0x67)`：Java `PlayerHandler.ThrowGrenade` 為空 handler；MapleForge parser 保留 payload 並回 `EnableActions`，未猜測不存在於 oracle 的廣播封包。
- `ARAN_COMBO(0x92)`：Java `PlayerHandler.AranCombo` 只由 dispatch 傳 `toAdd=1`；Aran job 2000..2112、4 秒逾時歸零、上限 30000，combo 10/20/.../100 時依 `21000000` skill level 呼叫 `applyComboBuff`。MapleForge 將 combo count/last time 放 Core `Player` runtime state，Application `SkillService.AddAranCombo` 套門檻與 skill gate，Adapter 達門檻時送 `GIVE_BUFF(ARAN_COMBO)` Java-source candidate/unverified；`21000000` effect data 缺口以 fallback + TODO 標註。
- `CYGNUS_SUMMON(0x91)`：Java dispatch 走 `UserInterfaceHandler.CygnusSummonNPCRequest`，job 2000 啟 NPC 1202000，job 1000 啟 NPC 1101008。MapleForge 轉成 NPC script intent，腳本缺失時回 `EnableActions` 防卡。
- `SNOWBALL(0xCD)`：Java `PlayerHandler.snowBall` handler 本身只讀註解所列 team/unknown/position/stage 並 `enableActions`；真正 hit 在一般近戰攻擊路徑呼叫 `MapleSnowballs.hitSnowball`。MapleForge 本刀做 parser + parity response，完整 event 延 P004。
- `LEFT_KNOCK_BACK(0xCE)`：Java `PlayerHandler.leftKnockBack` 只在 `mapId / 10000 == 10906` 送 `MaplePacketCreator.leftKnockBack()` + `enableActions`。MapleForge 補 snowball-map gate 與 `LEFT_KNOCK_BACK(0x11A)` Java-source candidate/unverified；其他圖保守 `EnableActions`。
- `COUPON_CODE(0xE7)`：Java `MapleServerHandler` 先 `skip(2)` 再呼叫 `CashShopOperation.CouponCode`；handler 查 DB `nxcode`、依 type 發放 GASH/MaplePoints/item/meso，失敗 `MTSCSPacket.sendCSFail(0xB3)`。MapleForge D3 新增 Core `CashCoupon` / `ICashCouponRepository`、LiteDB/Mongo `cash_coupons` repository、`CashShopService.RedeemCouponAsync`，支援四種 reward 與帳號/角色持久化旗標；item success S2C 是 Java-source candidate/unverified。
- `GAME_POLL(0xA3)`：Java `UserInterfaceHandler.InGamePoll` 受 `ServerConstants.PollEnabled=false` gate 控制；預設無投票狀態機。MapleForge 解析 tick/selection 後回 `EnableActions` parity，保留 `GAME_POLL_REPLY(0xDB)` helper 供未來 poll repository 接入。
- `MAPLETV(0x10A)`：此 Java tree 有 enum/opcode 但 `MapleServerHandler` 無 dispatch；現金道具 MapleTV 路徑也只提示沒有 MapleTV broadcaster。MapleForge 保留 payload parser + `EnableActions` parity，完整 MapleTV 廣播子系統延後。
- `CP_BeansUpdate(0xE1)`：Java `BeanGame.BeansUpdate` 呼叫 `resetBeans()` 並送 `BeansPacket.exitBeans()`。MapleForge reset Core `BeansGameSession` runtime state，送 `LP_BeanGameShoot(0x154)` action 6 + `EnableActions`；S2C fixture 標 unverified。
- `MONSTER_BOMB(0xBB)`：Java `MobHandler.handleMonsterBomb` 讀 mob oid，要求玩家存活、未 hidden、job 421/422、monster stats `selfD != -1`，再 `killMonster(..., false, false, selfD)`。MapleForge D3 讓 `MapService.LoadMobStats` 讀 WZ `info/selfDestruction/action` 到 `MobStats.SelfDestructAnimation`，`CombatService.KillMobWithoutRewards` 移除怪物且不觸發 EXP/drop hook，Adapter 廣播 `KILL_MONSTER` animation；hidden runtime state 尚未建模。
- 測試：D1 新增 11 條 adapter fixture；D2 新增 8 條測試（Core 2、Application 2、Adapters 4）；D3 新增 11 條測試（Application 4、Persistence 1、Adapters 6）。D3 驗證：`dotnet build --nologo -v quiet` 0 warning / 0 error；逐專案總測 Core 104、Application 140、Content 17、Persistence 7、Net 2、Tools.PacketDecoder 22、Tools.HeadlessClient 29、Adapters 412 / 1 skipped，合計 733 passed / 1 skipped。
- `ITEM_MAKER(0x6B)`：對照 Java `ItemMakerHandler.ItemMaker` / `ItemMakerFactory`，MapleForge D4 新增 Core `IItemMakeCatalog` 中性 contract、Content `WzItemMakeCatalog`、Application `ItemMakerService` 與 v113 handler。支援 makerType 1 製作寶石/裝備、makerType 3 以 100 個 ETC 製作結晶、makerType 4 裝備分解；保留 Java 行為：Maker 技能等級 gate、材料/楓幣扣除、催化劑 90% 成功率、寶石 stat 加成、裝備隨機化、分解產物數量 RNG。WZ 實讀 `Etc.wz/ItemMake.img` 61 筆寶石配方與 726 筆職業裝備配方（合計 787）。
- D4 邊界：`SHOW_ITEM_GAIN_INCHAT` / `SHOW_FOREIGN_EFFECT` 的 ItemMaker 成功效果依 Java `MaplePacketCreator.ItemMaker_Success*` 實作，fixture 標 `unverified`；尚未跑真 v113 client ItemMaker GUI smoke。
- 測試：D4 新增 13 條（Content 3、Application 5、Adapters 5）。D4 驗證：`dotnet build --nologo -v quiet` 0 warning / 0 error；Core 104、Content 21、Application 145、Persistence 7、Net 2、Tools.PacketDecoder 22、Tools.HeadlessClient 29 全綠；Adapters 417 passed / 1 skipped；逐專案合計 747 passed / 1 skipped；Core/Application 禁區 `rg -n "Maple\.Adapters\.V113"` 無命中。
- HiredMerchant 全套（D5→D8b）：對照 Java `HiredMerchantHandler`/`HiredMerchant`/`AbstractPlayerStore`/`PlayerInteractionHandler`，落地 Core 領域模型（`PlayerShop`/`HiredMerchant`/listing/visitor/blacklist/settlement）、Application `PlayerShopService`（建店/上架/購買/收益/過期/claim，全數受 owner/store instance lock 保護）、Persistence LiteDB/Mongo `hired_merchants`+`hired_merchant_items`、Adapters 0x34/0x38/0x3A + `PLAYER_INTERACTION(0x73)` merchant 全子指令、啟動過期重載服務。經 D8-前置/D8 兩輪外部審查共 9 項缺陷（coupon 封包版型、claim race、期限語意、店內 UI 廣播範圍、鎖紀律繞過、CLOSE_MERCHANT 版型、啟動降級、unverified 註記×2）於 D7b/D8b 全數修補，D8複核確認無殘留。S2C 封包全數標 Java-source candidate/unverified，待真機 GUI smoke 校準。
- **P003 最終狀態**：測試由起點基線 645 升至 **775 passed / 1 skipped**（+130，此差值基準不一致，見下行修正）；`dotnet build` 全程 0 warning/0 error；Core/Application 禁區 grep 全程無 V113 命中。過程歷經 3 次 Codex 額度撞限（W2 的 D4+D4b 共用一次、D5 一次、D7 一次，均由使用者兌換 usage limit reset 因應）與多次 CLI 假死中斷續跑（D2、D4×2、D4b×2）。殘餘 TODO（列待辦不擋收案）：HiredMerchant/ItemMaker 全數 S2C 待真機 GUI smoke 校準；REWARD_ITEM/寶箱 RandomRewards 權重 catalog；Snowball event 子系統、Shammos/Node、MonsterCarnival、MTS 完整拍賣列 P004 候選；562x 技能書未萃取。
- **稽核修正（2026-07-07_01）**：「645」基線只加總 Core/Content/Application/Adapters 4 個測試專案，漏計 P003 開工前已存在的 Persistence(6)/Net(2)/Tools.PacketDecoder(22)/Tools.HeadlessClient(29) 共 59 條（於 `f638132` worktree 實跑複核）。8 專案一致基準：起點 **704** → 終點 **775**，實際新增 **+71**（非 +130）。D1→D8b 逐步差值（+69/+8/+11/+14/+14/+6/+5/+3/+0）本身內部一致，唯獨起點「645」與後續各步「逐專案總測」計算基準不同，才產生表面 +130 的誤差。

## 🔗 產出

- `src/Maple.Adapters.V113/Login/V113Opcodes.cs`
- `src/Maple.Adapters.V113/Channel/V113RewardItemHandler.cs`
- `src/Maple.Adapters.V113/Channel/V113ChannelConnectionHandler.cs`
- `tests/Maple.Adapters.V113.Tests/LoginOpcodeTests.cs`
- `tests/Maple.Adapters.V113.Tests/ChannelRewardItemHandlerTests.cs`
- `docs/specs/v113-protocol-spec.md`
- `docs/design/移植狀態地圖.md`
- `src/Maple.Core/World/Player.Skills.cs`
- `src/Maple.Application/Skills/SkillService.cs`
- `src/Maple.Application/Skills/WzSkillCatalog.cs`
- `src/Maple.Adapters.V113/Channel/V113PlayerEventHandler.cs`
- `tests/Maple.Core.Tests/Skills/PlayerSkillsTests.cs`
- `tests/Maple.Application.Tests/Skills/SkillServiceTests.cs`
- `tests/Maple.Adapters.V113.Tests/ChannelPlayerEventHandlerTests.cs`
- `src/Maple.Core/CashShop/CashCoupon.cs`
- `src/Maple.Core/CashShop/ICashCouponRepository.cs`
- `src/Maple.Persistence/CashShop/LiteDbCashCouponRepository.cs`
- `src/Maple.Persistence/CashShop/MongoCashCouponRepository.cs`
- `src/Maple.Adapters.V113/Channel/V113CashShopPackets.cs`
- `src/Maple.Adapters.V113/Channel/V113UserInterfaceHandler.cs`
- `src/Maple.Adapters.V113/Channel/V113MonsterBombHandler.cs`
- `src/Maple.Adapters.V113/Channel/V113EventMiniGameHandler.cs`
- `tests/Maple.Application.Tests/CashShop/CashShopServiceTests.cs`
- `tests/Maple.Application.Tests/Combat/CombatServiceTests.cs`
- `tests/Maple.Persistence.Tests/CashCouponRepositoryTests.cs`
- `tests/Maple.Adapters.V113.Tests/ChannelD3HandlerTests.cs`
- `src/Maple.Core/Items/ItemMakeDefinition.cs`
- `src/Maple.Core/World/Player.RangedCombat.cs`
- `src/Maple.Content/Items/WzItemMakeCatalog.cs`
- `src/Maple.Application/Items/ItemMakerService.cs`
- `src/Maple.Adapters.V113/Channel/V113ItemMakerHandler.cs`
- `src/Maple.Host.Shared/MapleServerHost.cs`
- `tests/Maple.Content.Tests/Items/WzItemMakeCatalogTests.cs`
- `tests/Maple.Application.Tests/Items/ItemMakerServiceTests.cs`
- `tests/Maple.Adapters.V113.Tests/ChannelItemMakerHandlerTests.cs`
- `src/Maple.Core/PlayerShops/{IPlayerShop,PlayerShop,HiredMerchant,PlayerShopItemListing,PlayerShopState,PlayerShopStatus,IHiredMerchantRepository}.cs`
- `src/Maple.Application/PlayerShops/PlayerShopService.cs`
- `src/Maple.Persistence/PlayerShops/{HiredMerchantDocuments,LiteDbHiredMerchantRepository,MongoHiredMerchantRepository}.cs`
- `src/Maple.Adapters.V113/Channel/{V113HiredMerchantHandler,V113HiredMerchantPackets,V113PlayerInteractionRouter,IHiredMerchantSessionDispatcher}.cs`
- `src/Maple.Host.Shared/HiredMerchantReloadHostedService.cs`
- `docs/specs/persistence-model.md`
- `tests/Maple.Core.Tests/PlayerShops/HiredMerchantTests.cs`
- `tests/Maple.Application.Tests/PlayerShops/PlayerShopServiceTests.cs`
- `tests/Maple.Persistence.Tests/HiredMerchantRepositoryTests.cs`
- `tests/Maple.Adapters.V113.Tests/{ChannelHiredMerchantHandlerTests,ChannelPlayerInteractionHiredMerchantTests}.cs`
- `docs/devlog/任務歷程/2026-07-06_02_移植_HiredMerchantCut1.md`、`2026-07-06_03_移植_HiredMerchantCut2Adapters.md`、`2026-07-07_01_移植_HiredMerchantCut3.md`、`2026-07-07_02_修補_P003D7b外部審查缺陷.md`、`2026-07-07_03_修補_P003D8b終審缺陷.md`（分件詳細任務歷程）
