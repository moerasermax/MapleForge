---
編號: 2026-09-25_88
標題: P086 — 時間置換（Time Leap 5121010）清除其他技能冷卻
類型: 移植
狀態: ✅ 完成
建立: 2026-09-25
更新: 2026-09-25
關聯里程碑: P086（/goal 第 16 件）
關聯記憶: <空>
關聯commit: 見 git log（P086）
---

## 🎯 目標（執行前先寫死，過程不偷改）

對照 Java `MapleStatEffect.applyTo` 的 `isTimeLeap()` 分支：拳霸「時間置換」成功施放後，清除自己以外所有技能冷卻並逐一送
`skillCooldown(id, 0)`。MapleForge 沒有這個分支（P071-073 冷卻機制已具備）。完成判準：測試綠燈、build 0/0、全測試無退化。

## 📋 背景與查證

- Java `applyTo`（約 778-785 行）：`isTimeLeap()`（`sourceid == 5121010`）→ `for cooldown in getCooldowns(): if skillId != 5121010 → removeCooldown + skillCooldown(skillId, 0)`。
- 時間置換自己的冷卻在 `SpecialMove` 的冷卻區塊已先登記，故保留。
- 順帶查證並**放棄**的候選：新進場玩家重播既有召喚獸——Java `MapleSummon.sendSpawnData` 是空方法，Java 本身不重播，依「不比神諭更完整」紀律不做。

## 🪜 計畫步驟

- [x] 1. Core `Player.ResetSkillCooldownsExcept(keep)`
- [x] 2. `SkillService.Cast` 成功施放 5121010 時重設並回傳 `ResetCooldownSkillIds`
- [x] 3. Adapter `CooldownResetPackets`，連線 handler 在 buff 封包後送出；測試、build、全測試、三本帳、commit

## 📜 執行歷程（邊做邊追加，附時間）

- 依計畫實作。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> 已完成並 commit。下一步 P087：時空門建立（`DoorService.CreateDoor` 零呼叫者；Java `applyTo` 的 `isMagicDoor()` 分支）。

## ✅ 結果與結論

- 達標：時間置換會清掉其他技能冷卻，客戶端圖示同步解除。
- 1141 passed / 1 skipped（P085 基準 1139 +2：Adapters.V113 562→564）；build 0/0；禁區 grep clean。

## 🔗 產出

- 修改：`src/Maple.Core/World/Player.Skills.cs`、`src/Maple.Application/Skills/SkillService.cs`、`src/Maple.Adapters.V113/Channel/V113SkillPackets.cs`、`src/Maple.Adapters.V113/Channel/V113ChannelConnectionHandler.cs`
- 測試：`tests/Maple.Adapters.V113.Tests/ChannelSkillPacketTests.cs`
