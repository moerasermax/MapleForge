---
編號: 2026-09-25_101
標題: P099 — 道具（藥水/寵物自動補藥）與技能施放的 HP 變動同步隊伍血條
類型: 移植
狀態: ✅ 完成
建立: 2026-09-25
更新: 2026-09-25
關聯里程碑: P099（/goal 第 29 件）
關聯記憶: <空>
關聯commit: 見 git log（P099）
---

## 🎯 目標（執行前先寫死，過程不偷改）

接 P095 留下的候選：藥水、寵物自動補藥、技能施放（HpCon/治癒）也會改變 HP，Java 都走 `setHp` → `updatePartyMemberHP`；MapleForge 這三條路徑
沒有同步隊伍血條。完成判準：HP 有變動才同步、測試綠燈、build 0/0、全測試無退化。

## 📋 背景與查證

- Java `InventoryHandler.UseItem` → `MapleStatEffect.applyTo` → `stat.setHp(...)`（非 silent）→ `updatePartyMemberHP`。
- Java `PetHandler.Pet_AutoPotion` 同樣走道具 `applyTo`。
- Java `SpecialMove` → `effect.applyTo`：`hpchange != 0` 時 `setHp` → `updatePartyMemberHP`（HpCon 扣血、治癒回血）。
- 只回 MP 的藥水 HP 不變 → Java `setHp` 不會被呼叫（`hpchange == 0`）→ 不同步。MapleForge 以「前後 HP 比較」等價判斷。

## 🪜 計畫步驟

- [x] 1. `V113PartyHpSync.BroadcastIfChangedAsync(player, hpBefore)`
- [x] 2. `USE_ITEM`、`PET_AUTO_POT`、`SPECIAL_MOVE` 三條路徑前後比較 HP 後呼叫
- [x] 3. 測試、build、全測試、三本帳、commit

## 📜 執行歷程（邊做邊追加，附時間）

- 依計畫實作；技能路徑放在 HP/MP 更新封包之後（對照 Java `applyTo` 先 `setHp` 再送 stats 的相對順序不影響客戶端）。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> 已完成並 commit。下一步 P100：本輪最後一件，挑一個範圍明確的剩餘候選收尾並整理本輪總結。

## ✅ 結果與結論

- 達標：喝藥水、寵物補藥、施放治癒/耗血技能時，同圖隊友的隊伍血條即時更新。
- 1187 passed / 1 skipped（P098 基準 1185 +2：Adapters.V113 579→581）；build 0/0；禁區 grep clean。

## 🔗 產出

- 修改：`src/Maple.Adapters.V113/Channel/V113PartyHpSync.cs`、`src/Maple.Adapters.V113/Channel/V113ChannelConnectionHandler.cs`
- 測試：`tests/Maple.Adapters.V113.Tests/ChannelPartyHpSyncTests.cs`
