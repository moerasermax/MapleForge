---
編號: 2026-09-24_75
標題: P073 — M4-2 世界 tick：技能冷卻到期移除 + COOLDOWN(skill, 0) 通知
類型: 移植
狀態: ✅ 完成
建立: 2026-09-24
更新: 2026-09-24
關聯里程碑: M4-2 / P073（/goal 第 3 件）
關聯記憶: <空>
關聯commit: 見 git log（P073）
---

## 🎯 目標（執行前先寫死，過程不偷改）

對照 Java `World.handleCooldowns` 的冷卻迴圈：世界 tick 對有玩家的地圖逐人檢查，已到期的技能冷卻移除並送
`skillCooldown(skillId, 0)` 給本人。完成判準：世界 tick 第三個消費者上線、測試綠燈、build 0/0、全測試無退化。

## 📋 背景與查證

- Java `World.handleMap`：`map.characterSize() > 0` 才對每個 `chr` 呼叫 `handleCooldowns`；冷卻迴圈
  `m.startTime + m.length < now` → `removeCooldown(skil)` + `sendPacket(skillCooldown(skil, 0))`（嚴格小於）。
- MapleForge：P071/P072 已登記冷卻並送開始封包；`Player.SkillIsCooling` 查詢時會順手移除到期冷卻，但沒有人主動通知客戶端冷卻結束。
- `IMapSessionRegistry.GetAll(mapId)` 已帶完整 `Player` 與 `SendPacket`（P047/P063），逐人處理不需要新增登記表 API。

## 🪜 計畫步驟

- [x] 1. Core `Player.RemoveExpiredSkillCooldowns(now)`；Application `SkillService.ExpireSkillCooldowns`
- [x] 2. Adapter `V113PlayerTickHandler.TickPlayersAsync`（逐玩家處理，之後其他 `handleCooldowns` 項目也掛這裡）
- [x] 3. `WorldTickHostedService` 第三個消費者 + DI 註冊
- [x] 4. 測試、build、全測試、三本帳、新增 `docs/design/世界tick-設計.md` 活文件、commit

## 📜 執行歷程（邊做邊追加，附時間）

- 依計畫實作；冷卻到期判斷沿用 Java 嚴格小於（剛好到期那一刻不移除）。
- 發現世界 tick 從 P063 起沒有任何設計文件，補 `docs/design/世界tick-設計.md`（消費者清單 + 尚未移植候選）。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> 已完成並 commit。下一個候選：`handleCooldowns` 其餘項目（地圖持續扣血/寵物飢餓/…），先查 MapleForge 資料模型是否具備。

## ✅ 結果與結論

- 達標：冷卻到期後 3 秒內客戶端會收到 `COOLDOWN(skill, 0)`；只送給冷卻擁有者本人。
- 1084 passed / 1 skipped（P072 基準 1080 +4：Core 153→154、Adapters.V113 547→550）；build 0/0；禁區 grep clean。
- 已知差異：`SkillIsCooling` 查詢時的懶惰移除不會送 0 秒封包（客戶端自身計時會到期，影響僅是少一個同步封包）。

## 🔗 產出

- 修改：`src/Maple.Core/World/Player.Skills.cs`、`src/Maple.Application/Skills/SkillService.cs`、`src/Maple.Host.Shared/WorldTickHostedService.cs`、`src/Maple.Host.Shared/MapleServerHost.cs`
- 新增：`src/Maple.Adapters.V113/Channel/V113PlayerTickHandler.cs`、`docs/design/世界tick-設計.md`
- 測試：`tests/Maple.Core.Tests/Skills/PlayerSkillCooldownTests.cs`、`tests/Maple.Adapters.V113.Tests/ChannelPlayerTickHandlerTests.cs`
