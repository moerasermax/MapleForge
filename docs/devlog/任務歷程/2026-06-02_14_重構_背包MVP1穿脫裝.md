---
編號: 2026-06-02_14
標題: 背包 MVP-1 穿脫裝（equip/unequip，rollback-safe 安全切片）
類型: 重構
狀態: ⏸️ 暫停（規劃階段，尚未動程式碼；下次 session 續接）
建立: 2026-06-02 22:50
更新: 2026-06-02 23:00
關聯里程碑: 移植路線圖 ②背包/道具 MVP-1
關聯記憶: v113-pivot-port-from-java, default-collaborate-with-ai-team, current-state-resume, pending-batched-gui-smoke
關聯commit: 接 f58da5b
---

## 🎯 目標（執行前先寫死，過程不偷改）

> 背包 MVP-1：c2s ITEM_MOVE 的**穿裝(src>0→dst<0)/脫裝(src<0→dst>0)**，server 在 Player 端原子轉移 + 回 ModifyInventory，使客戶端裝備外觀正確更新。rollback-safe（cosmetic，不改價值/所有權，不需 app-journal）。不弄壞已真客戶端驗證的 char-look。
>
> **完成判準（待 Gemini red-team 收斂後鎖）**：①Player 端原子 Equip/Unequip（背包 Equip 袋 ↔ 已穿戴）②ITEM_MOVE 負 slot 分支接線 ③ModifyInventory 回包（負 slot）④build 綠 + headless 驗證(穿→脫 round-trip、char-look 來源同步)⑤既有測試不退。

## 📋 背景與假設

> 背包 MVP-0 完成(commit 0cd9d3d)。**設計分歧**：已穿戴在 `Character.Equips`(負 slot,驅動 char-look,4 處讀,已真客戶端驗)；背包袋在 `Character.Items`。穿脫裝＝兩 store 間移動。A 轉移派(保兩 store,原子方法搬移,不碰 char-look) vs B 統一派(已穿戴收進 Items 負 slot,刪 Equips,改 4 處)。→ 發 Gemini red-team 定案(pid 31172)。
> 使用者：先記錄待驗、繼續推進、先不寫單元測試、headless 驗即可；GUI 驗併入批量(見 [[pending-batched-gui-smoke]])。

## 🪜 計畫步驟

- [ ] 0. Gemini red-team 定 A/B + 原子操作/封包/MVP 排除（pid 31172）。
- [ ] 1~N. 待收斂後填。

## 📜 執行歷程（邊做邊追加，附時間）

- **22:50** 背包 MVP-0 完成後使用者「先記錄待驗、繼續推進」。記憶 `pending-batched-gui-smoke` 已建。依安全性分線（shop 改價值需 journal→defer；穿脫裝 rollback-safe→先做）選 MVP-1 穿脫裝。發 Gemini red-team(pid 31172) 定 A 轉移 vs B 統一。
- **23:00 [使用者喊暫停、本 session 收尾]** 使用者「先暫停、記錄目前狀態、下次 session 續接」。Gemini consult(31172) 未收結果即終止。**本任務停在規劃階段、尚未動任何程式碼**（背包 MVP-0 commit 0cd9d3d/f58da5b 已是穩定 checkpoint，與本任務無關、不受影響）。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> ⏸️ **暫停於規劃階段，零程式碼改動。下次 session 從這裡續接 MVP-1 穿脫裝：**
> 1. （可選）重發 Gemini red-team：穿脫裝 **A 轉移派**（保 Equips+Items 兩 store、單一原子 Player 方法搬移、不碰已真客戶端驗證的 char-look 4 處）vs **B 統一派**（已穿戴收進 Items 負 slot、刪 Equips、改 4 處渲染）。**Opus 傾向 A**（低風險、session 已長、不破壞已驗 char-look；B 列為後續乾淨化技術債）。若採 A 可直接實作不必再 consult。
> 2. 實作（採 A）：Core `Player.Equip(srcSlot)` / `Unequip(srcEquipSlot, dstBagSlot)` 原子——從 Equip 袋(Items 正 slot)移除→加進 `Character.Equips`(負 slot)／反向；MVP 裝備零屬性（與 MVP-0 一致）。
> 3. Adapters handler `HandleItemMoveAsync`：現在只處理 `IsWithinBagMove`(src>0,dst>0)，加 **穿(src>0,dst<0)/脫(src<0,dst>0)** 分支 → 呼 Player.Equip/Unequip → 回 `ModifyMove`（負 slot；穿脫的 ModifyInventory 可能需 mode2 + 結尾 movement flag byte，見 agy 封包研究「裝備穿脫 Position 含負數→結尾多寫 1 byte」）。
> 4. char-look 廣播給同圖他人＝薄 follow-up，單客戶端 smoke 下 no-op，可 defer。
> 5. build 綠 + headless 驗（穿→脫 round-trip、Equips/Items 同步無 dupe）→ commit → 回填三本帳。
> **關鍵不破壞**：char-look 由 `Character.Equips` 驅動且已真客戶端驗證，A 派穿裝時加 EquipEntry 即自然更新；務必原子（同一 Player 方法內移除+加入，免兩 store desync = Gemini risk#4）。
> **已知背景**：agy 封包事實在記憶/任務歷程 13；穿脫 c2s=ITEM_MOVE(0x41) 負 slot；ModifyInventory=0x1B。

## ✅ 結果與結論

> （待回填）

## 🔗 產出

> （待回填）
