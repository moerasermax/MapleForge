---
編號: 2026-06-18_02
標題: 移植 Messenger 信差系統
類型: 移植
狀態: 🚧 執行中
建立: 2026-06-18 08:05
更新: 2026-06-18 08:13
關聯里程碑: M6-2
關聯記憶:
關聯commit: 待填
---

## 🎯 目標（執行前先寫死，過程不偷改）

移植 v113 `MESSENGER(0x72)` 的 create/join/exit/invite/decline/message 主幹到 MapleForge 分層架構，完成 Core domain、Application in-memory service、Adapters.V113 handler/packet encoder，並以 xUnit 至少覆蓋 create、join、chat 三個情境。

完成判準：

- `Maple.Core` 只新增 Messenger domain，不含 v113 opcode/byte layout。
- `Maple.Application` 提供 thread-safe Messenger registry 與 create/join/leave/get 語義。
- `Maple.Adapters.V113` 新增 Messenger handler 與 `MESSENGER(0x145)` 封包 encoder，v113 sub-op 與 byte layout 只在 Adapter。
- 新增 `MessengerHandlerTests.cs`，至少驗 create、join、chat。
- 跑 targeted `Maple.Adapters.V113.Tests` 驗證通過。

## 📋 背景與假設

Java ground truth：

- `handling/channel/handler/ChatHandler.java` 的 `Messenger` method lines 211-301。
- `handling/world/MapleMessenger.java`。
- `handling/world/MapleMessengerCharacter.java`。
- `tools/MaplePacketCreator.java` 的 `messengerInvite`、`addMessengerPlayer`、`removeMessengerPlayer`、`messengerChat`、`messengerNote`。
- `send.properties`：`MESSENGER = 0x145`。

限制：

- 不修改舊 Java server、client binaries、WZ references、sibling projects。
- 不修改 `V113ChannelConnectionHandler.cs` 或 `V113ChannelOpcodes.cs`，使用者自行接 dispatch。
- 本輪程式碼保持新增檔案為主；既有文件進度帳若需同步，收尾時依使用者「Create NEW files only」限制說明未改原因。

## 🪜 計畫步驟

- [x] 1. 讀 Java reference 與 MapleForge Party/Chat patterns。
- [x] 2. 新增 Core Messenger domain。
- [x] 3. 新增 Application MessengerService。
- [x] 4. 新增 Adapter handler/packets 與 session hook interface。
- [x] 5. 新增 Messenger handler tests。
- [x] 6. 跑 targeted tests，修正編譯/行為差異。

## 📜 執行歷程（邊做邊追加，附時間）

- **08:05** 建立任務歷程並鎖定 DoD；下一步讀 Java source map 與本地 Party/Chat 實作模式。
- **08:08** 完成 Java source map：`ChatHandler.Messenger`、`World.Messenger`、`MapleMessenger`、`MapleMessengerCharacter`、`MaplePacketCreator` messenger packet helpers。確認 join 流程需要額外 `joinMessenger(position)` S2C helper。
- **08:11** 新增 Core/Application/Adapters/Tests 檔案；未改 `V113ChannelConnectionHandler.cs` 或 `V113ChannelOpcodes.cs`。
- **08:12** `dotnet test tests/Maple.Adapters.V113.Tests/Maple.Adapters.V113.Tests.csproj --no-restore --nologo --filter Messenger` 通過：3/3。
- **08:13** `dotnet test tests/Maple.Adapters.V113.Tests/Maple.Adapters.V113.Tests.csproj --no-restore --nologo` 通過：290 passed / 1 skipped。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

Messenger 移植程式碼與測試已完成；下一步由使用者接 `V113ChannelConnectionHandler` dispatch/DI，並在正式接線時同步 protocol spec / 進度日誌。

## ✅ 結果與結論

達成本輪 DoD：新增 Messenger domain、thread-safe Application service、v113 handler/packet encoder 與 create/join/chat 測試。Java join 流程會送 `joinMessenger(position)`，本輪一併放入新 packet file，避免 join path 無法對齊 Java fanout。

限制：依使用者「Create NEW files only」要求，本輪未修改既有 `V113ChannelConnectionHandler.cs`、`V113ChannelOpcodes.cs`、protocol spec、進度日誌或任務追蹤。

## 🔗 產出

- `src/Maple.Core/Social/Messenger.cs`
- `src/Maple.Core/Social/MessengerMember.cs`
- `src/Maple.Application/Social/MessengerService.cs`
- `src/Maple.Adapters.V113/Channel/V113MessengerHandler.cs`
- `src/Maple.Adapters.V113/Channel/V113MessengerPackets.cs`
- `tests/Maple.Adapters.V113.Tests/MessengerHandlerTests.cs`
