---
編號: 2026-06-02_11
標題: 真客戶端進圖 smoke — 確認 Henesys NPC 顯示
類型: 驗證
狀態: ✅ 完成
建立: 2026-06-02 18:50
更新: 2026-06-02 19:10
關聯里程碑: 移植路線圖 ①in-game 基礎（地圖物件同步 NPC 的里程碑級 smoke）
關聯記憶: current-state-resume, default-collaborate-with-ai-team, gui-test-record-and-diagnose-visually, realclient-full-automation-decision
關聯commit: 接 4d39f81（NPC spawn 移植）
---

## 🎯 目標（執行前先寫死，過程不偷改）

> 用 `diag13-enter-game.ps1` 全自動把真客戶端帶進 Henesys(100000000)，**驗證上一任務新加的 SpawnNpc(0xF9)+RequestController(0xFB) 真的讓 NPC 在客戶端畫面渲染出來**（移植前不送 spawn → 地圖應無 NPC；移植後應看得到）。
>
> **完成判準（兩道獨立佐證都要過）**：
> 1. **server log 佐證**：`live-enter-diag.log` 出現 `地圖 100000000 送出 N 個 NPC spawn`，N>0（證明我的程式路徑真的執行、送了 N 個 NPC）。
> 2. **視覺佐證**：`diag-shots/enter-map-*.png` 經 AI 團隊（Gemini/claude-ultra 視覺）判讀，確認 Henesys 場景出現 NPC 立繪（非空地圖）。
> 3. 客戶端進圖後續留（沿用 task 06 的 ~24s 存活觀察，不因缺欄位彈 EOF）。

## 📋 背景與假設

> 地圖物件同步 NPC 移植已完成(commit 4d39f81)、單元測試逐位元組對齊真 Java。本任務＝里程碑級真客戶端 smoke（驗證原則：里程碑級才跑 GUI）。
> ground truth：MapleStory NPC 由 server 送 SPAWN_NPC 才渲染（客戶端不自行從 WZ 生 NPC）→ 若移植前畫面無 NPC、移植後有，即證明我方封包生效。
> 已備：`diag13-enter-game.ps1`（無人值守 login→世界→頻道→選角→進圖，可重現2次）；server 端已加 `送出 N 個 NPC spawn` log。
> 假設/坑：diag13 用 `--no-build` → **必須先 dotnet build** 才有新程式碼；機器須淨空勿搶前景焦點(task 04 教訓)；本機 BSOD 風險(AX210)。

## 🪜 計畫步驟

- [x] 1. 清殘留 .NET host/test 殭屍（本專案）
- [x] 2. dotnet build Host.Login（含新 NPC log 程式碼）
- [x] 3. 跑 diag13-enter-game.ps1（無人值守，提醒使用者勿碰機器）
- [x] 4. 驗 server log：`送出 26 個 NPC spawn` + 進圖續留 24s
- [x] 5. 收 diag-shots/enter-map-*.png → AI 團隊雙人視覺判讀確認 NPC（小幫手/計程車）
- [x] 6. 收尾：狀態切換、回填三本帳

## 📜 執行歷程（邊做邊追加，附時間）

- **18:50** 定目標、開工。讀 diag13（用 `--no-build`、log→`live-enter-diag.log`、shots→`diag-shots/enter-map-*.png`）；在 `SpawnMapNpcsAsync` 加 `送出 N 個 NPC spawn` server log 作獨立 ground truth。
- **18:55** 清殭屍 + `dotnet build Host.Login`（含新 log）建置成功。
- **19:00** 跑 diag13（一次過、無人值守）：login(自我修復 1 次過)→單擊雪吉拉→CH.1→前往伺服器(到達 CHARLIST)→單擊 TestHero→選擇角色鈕→**進入地圖 100000000，client 全程 24s 存活、channel 未斷、窗 class=MapleStoryClass**。
- **19:03** server log 鐵證：`[Channel] 地圖 100000000 送出 26 個 NPC spawn`（判準1 ✅，N=26>0）。客戶端未彈 EOF（判準3 ✅）。
- **19:08** 視覺判讀：**經 AI 團隊 Gemini 3.1 Pro + claude-ultra 雙人交叉判讀全解析度 `enter-map-6.png`，結論一致、信心度高——有 NPC 顯示**（判準2 ✅）：
  - 「小幫手」NPC（左側懸空平台，金髮戴耳機客服造型，綠底名牌正確渲染）
  - 「計程車」NPC（中央，TAXI 燈＋紅底「計程車」站牌＋燈泡互動提示，名牌正確）
  - 視窗僅見 2 個＝鏡頭視野有限，其餘約 24 個在鏡頭外（非未送出）。
- **19:10 [使用者提醒：要錄 c→s 封包]** 確認 diag13 已開 `MAPLEFORGE_(WINDOWER_)CAPTURE`，本輪擷取 `tools/windower/captures/windower_packets_32256_2172.ndjson`（9624B，36 c2s + 10 s2c）。**關鍵**：該檔含**兩個 getHello**(`7100`=v113)——#1 login(8484)、#2 channel(8585)，客戶端關 login socket 後 OS 重用同 fd 號→**login+channel 兩段 c→s 都進同一檔**。檔尾 seq44-46 即 in-game channel c2s。c2s 為加密 raw（需離線解碼器+兩段 getHello IV 解）；本輪未點 NPC，in-game c2s 主要為 PLAYER_LOGGEDIN/pong/移動，無 NPC talk。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> ✅ 本任務完成（三判準全過）。下一步＝路線圖②「NPC 對話/商店」（Jint + NPCConversationManager + c2s NPCHandler talk）。屆時可點 NPC 觸發 talk，並用本輪已驗證的 capture 流程錄 in-game c→s（NPC talk opcode）→離線解碼當 fixture。capture 檔：`tools/windower/captures/windower_packets_32256_2172.ndjson`（含兩段 getHello IV，可餵離線解碼器）。

## ✅ 結果與結論

> **真客戶端進圖 NPC smoke 一次過、三道獨立判準全達標**：①server log「送出 26 個 NPC spawn」②AI 團隊雙人視覺確認 NPC（小幫手/計程車）正確渲染含名牌 ③client 24s 續留未斷。**正向交叉驗證 SPAWN_NPC(0xF9)+RequestController(0xFB) 移植成立**——server 送、客戶端如實渲染 NPC 精靈圖與名牌。in-game 基礎①里程碑級 smoke 通過。
> 心法：截圖視覺判讀預設找團隊（Gemini+claude-ultra 雙人），比獨自盲判可靠（呼應 default-collaborate-with-ai-team）；server log 計數行＝截圖之外最強的獨立 ground truth，每個 in-game 移植都該補一行可 grep 的成效計數。c→s 擷取要注意 OS fd 重用會把多連線塞進同一 ndjson（靠檔內多個 getHello 切段）。

## 🔗 產出

> - 程式：`V113ChannelConnectionHandler.SpawnMapNpcsAsync` 加 `送出 N 個 NPC spawn` server log（commit 待填本次）。
> - 截圖：`diag-shots/enter-map-1~6.png`（map-6 經團隊判讀為證據幀）。
> - 封包擷取：`tools/windower/captures/windower_packets_32256_2172.ndjson`（login+channel 雙段 c→s/s2c，含兩 getHello IV）。
> - server log：`live-enter-diag.log`（`地圖 100000000 送出 26 個 NPC spawn`）。
