---
編號: 2026-07-07_03
標題: P003 D8b 終審缺陷修補
類型: 修補
狀態: ✅ 完成
建立: 2026-07-07 03:49
更新: 2026-07-07 03:59
關聯里程碑: P003
關聯記憶:
關聯commit: 本提交（P003-D8b 終審修補）
---

## 🎯 目標（執行前先寫死，過程不偷改）

> 修補獨立 Review 對 D7/D7b 指出的五項缺陷：HiredMerchant shop UI 廣播範圍錯誤、adapter 直改 repository 繞過 `PlayerShopService` lock、`CLOSE_MERCHANT` 錯封包、啟動 reload 例外未降級、D7 E2E fixture 缺 unverified 註記。完成判準：`dotnet build` 綠、動到測試專案單跑綠、逐專案總測不退化（基線 775+1skip）、廣播範圍有訪客/非訪客測試、禁區 grep clean、commit 並 push。

## 📋 背景與假設

> 基底 HEAD `ed49640`。Java oracle：`AbstractPlayerStore.broadcastToVisitors` 決定 shop 內部 UI 更新只送 owner + visitors；商人外觀 spawn/destroy 才是 map-level。鎖紀律需集中在 Application `PlayerShopService`，adapter 不直接 mutate repository。S2C fixture 沒有真 client capture 時必須標 Java-source candidate/unverified。

## 🪜 計畫步驟

- [x] 1. 讀 `V113PlayerInteractionRouter`、`V113HiredMerchantHandler`、`PlayerShopService`、reload hosted service、相關測試與 Java oracle。
- [x] 2. 建立 merchant session/visitor dispatcher，把店內 UI 封包改送 owner + visitors，保留商人外觀 map broadcast。
- [x] 3. 將 enter/leave/maintenance 類 mutation 移入 `PlayerShopService`，adapter 只呼叫 service。
- [x] 4. 修正 `CLOSE_MERCHANT` S2C 為 `ShopErrorMessage(error, type)`。
- [x] 5. 啟動 reload try/catch + log 降級；補/掃 unverified 註記。
- [x] 6. 跑 build、動到/全專案測試、禁區 grep，更新文件並 commit/push。

## 📜 執行歷程（邊做邊追加，附時間）

- **03:49** 建立 D8b 任務歷程；下一步讀 router/service/host/test 與 Java oracle。
- **03:59** 完成五項修補與驗證：新增店內 session dispatcher；enter/leave/maintenance mutation 進 `PlayerShopService` lock；close merchant 改 `shopErrorMessage(0x15,0)`；startup reload try/catch 降級；E2E fixture 補 unverified 註記並擴充廣播範圍測試。build 綠；逐專案總測 775+1skip；禁區 grep clean。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> D8b 程式、測試、文件與 commit 已完成，下一步 push。

## ✅ 結果與結論

> 達標。五項終審缺陷已修補；測試總數維持 775 passed / 1 skipped，沒有退化。店內 UI 廣播範圍已有訪客/非訪客測試證據；S2C fixture 維持 Java-source candidate/unverified。

## 🔗 產出

> 主要檔案：`PlayerShopService.cs`、`V113PlayerInteractionRouter.cs`、`IHiredMerchantSessionDispatcher.cs`、`V113HiredMerchantPackets.cs`、`V113HiredMerchantHandler.cs`、`HiredMerchantReloadHostedService.cs`、`ChannelPlayerInteractionHiredMerchantTests.cs`、`v113-protocol-spec.md` 與任務文件。commit：本提交。
