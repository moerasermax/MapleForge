---
編號: 2026-06-18_02
標題: 移植 NOTE_ACTION 留言系統
類型: 移植
狀態: ✅ 完成
建立: 2026-06-18 08:04
更新: 2026-06-18 08:11
關聯里程碑: M6-2
關聯記憶:
關聯commit: 8e7b5b7（統籌整合 commit，含 Note 新檔+dispatch+DI）
---

## 🎯 目標（執行前先寫死，過程不偷改）

移植 v113 `NOTE_ACTION(0x7B)` 留言系統的最小可測切片：新增 Core note domain/repository、Application note service、LiteDB repository、V113 note parser/packet encoder/handler，以及至少兩個 xUnit 測試（send note parse、SHOW_NOTES encoding）。完成判準是 targeted tests 綠、Core/Application 不含 v113 opcode/byte layout、且不修改既有 channel dispatch/opcode 檔。

## 📋 背景與假設

Java 行為神諭為 `PlayersHandler.Note` 與 `MTSCSPacket.showNotes`；send opcode `SHOW_NOTES=0x26`。本輪 MVP 依任務要求不做 cash item 驗證，type 0 只建立 note，type 1 依 note id 刪除並保留 fame flag 解析。因使用者會自行接 dispatch，本輪建立新 adapter 檔即可。

## 🪜 計畫步驟

- [x] 1. 讀 Java source 與 MapleForge 既有 guild/Duey/packet/test patterns。
- [x] 2. 新增 Core/Application/Persistence/Adapters/Test 檔案。
- [x] 3. 跑 targeted tests/build，修正編譯與封包 layout 問題。
- [x] 4. 回填任務歷程與必要活文件。

## 📜 執行歷程（邊做邊追加，附時間）

- **08:04** 建立任務歷程；下一步讀 Java reference 與既有 MapleForge patterns。
- **08:06** 確認 Java `PlayersHandler.Note`：type 0 讀 receiver/message/fame/int/cashId；type 1 讀 count、skip 2、逐筆 id + gainFame。
- **08:07** 找到 `MTSCSPacket.showNotes`，確認 S2C layout 為 `SHOW_NOTES(0x26)` 後接 mode byte `3`、count、note entries。
- **08:10** 新增 note domain/service/repository/adapter/tests；targeted note tests 3 綠，Application/Persistence build 0 warning/0 error。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

已完成。若需接續，下一步是使用者自行把 `V113NoteHandler.HandleNoteActionAsync` 接進 channel dispatch，並在 Host DI 註冊 `NoteService` / `LiteDbNoteRepository`。

## ✅ 結果與結論

完成 NOTE_ACTION MVP 移植：Core/Application 無 v113 opcode/byte layout；v113 parser/serializer 留在 `Maple.Adapters.V113/Channel`；LiteDB repository 使用 `notes` collection 並對 `ReceiverName` 建索引。Java `showNotes` 的 mode byte `3` 已保留。

## 🔗 產出

- `src/Maple.Core/Social/Note.cs`
- `src/Maple.Core/Social/INoteRepository.cs`
- `src/Maple.Application/Social/NoteService.cs`
- `src/Maple.Persistence/Notes/LiteDbNoteRepository.cs`
- `src/Maple.Adapters.V113/Channel/V113NotePackets.cs`
- `src/Maple.Adapters.V113/Channel/V113NoteHandler.cs`
- `tests/Maple.Adapters.V113.Tests/NoteHandlerTests.cs`
- 驗證：`dotnet test tests/Maple.Adapters.V113.Tests/Maple.Adapters.V113.Tests.csproj --filter Note --no-restore --nologo`；`dotnet build src/Maple.Application/Maple.Application.csproj --no-restore --nologo`；`dotnet build src/Maple.Persistence/Maple.Persistence.csproj --no-restore --nologo`
