---
編號: 2026-09-25_83
標題: P081 — 施放召喚技能時建立召喚獸並廣播 SPAWN_SUMMON
類型: 移植
狀態: ✅ 完成
建立: 2026-09-25
更新: 2026-09-25
關聯里程碑: P081（/goal 第 11 件，零呼叫者掃描 2/8）
關聯記憶: <空>
關聯commit: 見 git log（P081）
---

## 🎯 目標（執行前先寫死，過程不偷改）

P080 追查發現整個專案沒有任何 `new Summon(`：召喚技能只套 buff，召喚獸從未出現（移動/攻擊/受傷 handler 都在查不存在的物件）。
對照 Java `MapleStatEffect.applyTo` 建立召喚獸、加入 field、全圖廣播 `spawnSummon`；重複施放先移除舊的。完成判準：
建立/替換有測試、build 0/0、全測試無退化。召喚獸隨 buff 結束消失留 P082。

## 📋 背景與查證

- Java `applyTo`（731-745 行）：`getSummonMovementType() != null` → `new MapleSummon(applyfrom, this, pos ?? applyfrom.getPosition(), type)`
  → `map.spawnSummon(tosummon)`（`broadcastMessage(spawnSummon(summon, true))` 全圖含自己）→ `summons.put(sourceid, …)` →
  `addHP(x)`（暗黑之魂 1321007 再 +1）。`4341006` 取消 MIRROR_IMAGE（影分身系，v113 無暗影雙刀，略過）。
- Java `getSummonMovementType`：技能 ID → STATIONARY/CIRCLE_FOLLOW/CIRCLE_STATIONARY/WALK_STATIONARY/FOLLOW 對照表（全數搬入 Core）。
- 重複施放：`applyBuffEffect` → `cancelEffect(this, …)` → `deregisterBuffStats` 的 SUMMON/PUPPET 分支 `removeSummon(summon, true)` 並從 map 移除，
  之後 `applyTo` 才建立新召喚獸。
- WzSkillCatalog 已替召喚技能產生 SUMMON/PUPPET statup（409/427 行），buff 本身會正常套用與到期。

## 🪜 計畫步驟

- [x] 1. Core `Summon.GetMovementType(skillId)`（Java 對照表）
- [x] 2. Application `SummonService.TrySpawn`（HP=x、暗黑之魂 +1、同主人同技能先移除、物件 ID 從 2_000_000 起避開已用）
- [x] 3. Adapter：`HandleSpecialMoveAsync` 拿到 field，成功施放後 `lock(field)` 建立，鎖外廣播 Remove（替換時）+ Spawn；DI 註冊
- [x] 4. 測試、build、全測試、三本帳、commit

## 📜 執行歷程（邊做邊追加，附時間）

- 依計畫實作；召喚位置優先用 SPECIAL_MOVE 封包帶的座標（Java `pos`），沒帶則用玩家位置。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> 已完成並 commit。**下一步 P082（必做）**：SUMMON/PUPPET buff 取消或到期時移除召喚獸並廣播 `removeSummon`——在此之前召喚獸會一直留在地圖上。

## ✅ 結果與結論

- 達標：召喚技能施放後召喚獸出現在所有人畫面上，重複施放會替換舊召喚獸。
- 1130 passed / 1 skipped（P080 基準 1121 +9：Application 307→316）；build 0/0；禁區 grep clean。
- 已知暫時狀態：召喚獸生命週期尚未綁 buff（P082）、換圖/登出未清除（P083）、進場玩家看不到既有召喚獸（P084）。

## 🔗 產出

- 新增：`src/Maple.Application/Skills/SummonService.cs`
- 修改：`src/Maple.Core/World/Summon.cs`、`src/Maple.Adapters.V113/Channel/V113SkillPackets.cs`、`src/Maple.Adapters.V113/Channel/V113ChannelConnectionHandler.cs`、`src/Maple.Host.Shared/MapleServerHost.cs`
- 測試：`tests/Maple.Application.Tests/Skills/SummonServiceTests.cs`
