---
編號: 2026-09-24_76
標題: P074 — M4-2 世界 tick：地圖持續扣血資料層 + FieldHpDecay 狀態機（不變行為）
類型: 移植
狀態: ✅ 完成
建立: 2026-09-24
更新: 2026-09-24
關聯里程碑: M4-2 / P074（/goal 第 4 件）
關聯記憶: <空>
關聯commit: 見 git log（P074）
---

## 🎯 目標（執行前先寫死，過程不偷改）

為 Java `handleCooldowns` 的「地圖持續扣血（hurt）」鋪資料層：`MapData` 讀 WZ `info/decHP`、`decHPInterval`、
`protectItem`；Core 新增 `FieldHpDecay`（對照 `setHPDec`/`canHurt()`）；field 建立時初始化。**本 P 不實際扣血**
（P075 接世界 tick）。完成判準：資料/狀態機/初始化有測試、build 0/0、全測試無退化。

## 📋 背景與查證

- Java `MapleMapFactory`（254-256、427-429 行）：`setHPDec(info/decHP, 0)`、`setHPDecInterval(info/decHPInterval, 10000)`、
  `setHPDecProtect(info/protectItem, 0)`。
- Java `MapleMap.setHPDec`：`delta > 0 || mapid == 749040100` 才把 `lastHurtTime` 設成 now（啟動計時）；
  `canHurt()`：`lastHurtTime > 0 && lastHurtTime + decHPInterval < now` → 重設 `lastHurtTime = now` 並回 true。
- MapleForge：`MapData`/`FieldInstance` 都沒有這些欄位；`FieldInstance` 是執行期狀態的正確歸屬（每頻道一份，非 static）。

## 🪜 計畫步驟

- [x] 1. `MapData.DecHp/DecHpInterval/ProtectItem` + `MapService.LoadMap` 讀取
- [x] 2. Core `FieldHpDecay.Create/TryHurt` + `FieldInstance.HpDecay`
- [x] 3. `MapService.InitializeFieldEnvironment`，`EnterField` 建立 field 時呼叫（`lock(field)` 內）
- [x] 4. 測試、build、全測試、三本帳、世界 tick 活文件、commit

## 📜 執行歷程（邊做邊追加，附時間）

- 依計畫實作；`FieldHpDecay` 無扣血設定時回 null（對照 Java `lastHurtTime` 維持 0 → `canHurt` 永遠 false）。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> 已完成並 commit。下一步 P075：`V113PlayerTickHandler` 在 `field.HpDecay.TryHurt(now)` 時對活著且未穿 `protectItem` 的玩家扣 `DecHp` 並送 HP 更新。

## ✅ 結果與結論

- 達標：地圖扣血設定已載入並掛在 field 上，狀態機行為（嚴格小於、觸發後重設）與 Java 一致；玩家端尚無感。
- 1089 passed / 1 skipped（P073 基準 1084 +5：Core 154→157、Application 295→297）；build 0/0；禁區 grep clean。
- 證據層級：Java source；真 WZ 內哪些地圖有 `decHP` 尚未抽樣（synthetic provider 測試）。

## 🔗 產出

- 修改：`src/Maple.Core/Maps/MapData.cs`、`src/Maple.Core/World/FieldInstance.cs`、`src/Maple.Application/Maps/MapService.cs`、`src/Maple.Adapters.V113/Channel/V113ChannelConnectionHandler.cs`
- 新增：`src/Maple.Core/World/FieldHpDecay.cs`
- 測試：`tests/Maple.Core.Tests/World/FieldHpDecayTests.cs`、`tests/Maple.Application.Tests/Maps/MapServiceFieldLimitTests.cs`
- 文件：`docs/design/世界tick-設計.md`
