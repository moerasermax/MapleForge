---
編號: 2026-09-25_103
標題: P101 真機首測：Gr2D 視窗化、裝備 Owner null 斷線、攻擊診斷 log
類型: 驗證
狀態: ✅ 完成
建立: 2026-09-25 12:10
更新: 2026-09-25 13:35
關聯里程碑: #12（批次移植後真機 smoke）
關聯記憶: mapleforge-live-test-env
關聯commit: 待填
---

## 🎯 目標（執行前先寫死，過程不偷改）

> 使用者要親自玩：啟動 server + 真 v113 客戶端，建帳號 test1234，一路排除阻擋直到能進地圖打怪。
> 完成判準：客戶端能登入、建角、進城鎮與狩獵場、攻擊能打死怪物；過程中修的程式碼有測試、全測試綠、build 0/0。

## 📋 背景與假設

- P071-P100 新封包全標 unverified，本機這台第一次跑真機（client 在 `E:\備份\v113私服\v113_Client`，舊 AI_Lab 路徑已不存在）。
- 本機原本沒有 C++ 編譯器，`tools/windower/bin` 不在 repo。

## 🪜 計畫步驟

- [x] 1. 編 server、建帳號、啟動 server + client
- [x] 2. 逐一排除進遊戲的阻擋
- [x] 3. 攻擊打不死的診斷
- [x] 4. 全測試 + 三本帳 + commit

## 📜 執行歷程（邊做邊追加，附時間）

- **12:15** client 缺 `ijl15.dll`（只剩 `ijl15_BackUp 20250319.dll`）→ 使用者同意複製一份（client 資料夾，非 repo）。
- **12:20** 客戶端報 `Failed in finding proper screen mode for Gr2D`。使用者同意 winget 安裝 VS 2022 BuildTools，照 `build.bat` 參數編出 windower（`build.bat` 寫死 VS 18 Insiders 路徑，未改）。
- **12:25** windower log 證明錯誤發生在 `Direct3DCreate8` 之後、`CreateDevice` 之前：Gr2D 列舉不到 800x600 16-bit 模式。使用者選擇改 windower 原始碼：hook IDirect3D8 vtable slot 6/7/9，補 6 個合成模式、CheckDeviceType 失敗時放行（CreateDevice hook 本來就強制視窗化）。實測 `real=127 +synthetic=6`、CreateDevice hr=0。
- **12:40** 進頻道斷線：`appsettings.json` 的 `Instance.WzDirectory` 是佔位字串，蓋掉程式預設值 → 讀不到地圖。以環境變數 `Instance__WzDirectory` 啟動解決（未改 appsettings）。清理區塊 `V113ChannelConnectionHandler.cs:1757` 對 null player 丟 NRE，把原始例外蓋掉（未修，記為候選）。
- **13:00** 發現 server 實際 DB 是 `src\Maple.Host.Login\data\MapleForge Dev.db`，`tools/seed-test-data.ps1` 寫到 repo 根 `data\`；帳號其實是 AutoRegister 建的。
- **13:10** 進頻道 `ArgumentNullException`：`AddEquipItemInfo` 寫 `eq.Owner` 為 null。DB 內裝備沒有 Owner 欄位（LiteDB 預設 EmptyStringToNull，空字串存成 null 被省略，讀回 null）。寫出 Owner 的封包呼叫點 20+ 處，修在 `PacketWriter.WriteMapleString`/`WriteFixedAsciiString`：null 視為空字串（Java 端 owner 從 DB 讀出恆為空字串，線上行為一致）。
- **13:16** 打怪打不死：近戰攻擊路徑多處靜默 return，加 Debug 級診斷 log（parse 失敗原因、targets/hits）。實測每下只有 1 傷害——新角色初始裝備所有數值為 0（武器攻擊力 0）。
- **13:18** 查到系統性缺口：建角、怪物掉落（`DropService.cs:359`）、`Player.cs:63` 給予物品、商城產生的裝備都沒從 WZ 載入預設數值；只有 ItemMaker 走 `IItemMakeCatalog.CreateEquip`。本次只用 `CreateEquip` 讀 WZ 手動補使用者角色 DB（斧頭 Watk 17 等），程式修正留給下一個 P。
- **13:21** 進購物商城：客戶端收到頻道握手後立刻斷線、回登入，再關閉時跳 `abnormal program termination`（每次關閉都會跳，來源未查明）。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> 已 commit。下一步：P102 裝備預設數值（建角/掉落/給予/商城接 `CreateEquip`，先對照 Java `getEquipById`）。

## ✅ 結果與結論

- 達標：真客戶端可登入、建角、進弓箭手村與狩獵場，攻擊能打死怪（實測單下 5-22 傷害）。
- 驗證：新增 2 測試，1194 passed / 1 skipped（P100 基準 1192 +2）；Host build 0/0。
- 心法：清理區塊裡的次生例外會蓋掉原始例外，排查時先看「哪個變數是 null」反推原始失敗點；靜默 return 的路徑一定要有至少 Debug 級 log，否則真機問題無從定位。
- 新候選（見任務追蹤 P101 段）：裝備預設數值、商城進入握手斷線、關閉時 abort、1757 清理 NRE、環境路徑三處、地圖 0 全黑。

## 🔗 產出

- `src/Maple.Core/IO/PacketWriter.cs`：null 字串視為空字串
- `tests/Maple.Core.Tests/IO/PacketWriterNullStringTests.cs`：2 測試
- `src/Maple.Adapters.V113/Channel/V113ChannelConnectionHandler.cs`：近戰攻擊 Debug 診斷 log
- `tools/windower/windower.cpp`：EnumAdapterModes/GetAdapterModeCount/CheckDeviceType hook
- 記憶：`mapleforge-live-test-env`
