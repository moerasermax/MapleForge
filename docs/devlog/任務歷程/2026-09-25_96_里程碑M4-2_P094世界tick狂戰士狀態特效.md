---
編號: 2026-09-25_96
標題: P094 — M4-2 世界 tick：狂戰士之怒（Berserk 1320006，職業 132）狀態特效
類型: 移植
狀態: ✅ 完成
建立: 2026-09-25
更新: 2026-09-25
關聯里程碑: M4-2 / P094（/goal 第 24 件）
關聯記憶: <空>
關聯commit: 見 git log（P094）
---

## 🎯 目標（執行前先寫死，過程不偷改）

對照 Java `handleCooldowns` 的 `canBerserk()` + `doBerserk`：黑騎士學會 1320006 後，每 10 秒依 HP 門檻決定狂戰士狀態並送特效
（`showOwnBuffEffect(1320006, 1, dir)`，`dir` 一定寫出）。完成判準：測試綠燈、build 0/0、全測試無退化。

## 📋 背景與查證

- Java `canBerserk()`：`job == 132 && lastBerserkTime + 10000 < now`（初值 0 → 第一次一定通過）。
- Java `doBerserk`：1320006 等級 ≥ 1 → `lastBerserkTime = now`；`Berserk = hp <= currentMaxHp * (x / 100.0)`；
  `showOwnBuffEffect(1320006, 1, berserk ? 1 : 0)`；沒有 MORPH → `showBuffeffect(id, 1320006, 1, dir)` 廣播（不含自己）。等級 0 → `lastBerserkTime = -1`（下次繼續檢查）。
- Java `showOwnBuffEffect`/`showBuffeffect`：`direction != 3 || skillid == 1320006` 才寫 direction。
- Java 另在 `PlayerStats.setHp`（非 silent）呼叫 `doBerserk`，同樣受 10 秒節流；MapleForge 只走世界 tick（延遲 ≤ 3 秒）。`stats.Berserk` 影響伺服器端傷害上限計算，MapleForge 傷害由客戶端驅動，不移植。

## 🪜 計畫步驟

- [x] 1. Core `Player.CanCheckBerserk`/`MarkBerserkChecked`
- [x] 2. Application `SkillService.TryCheckBerserk`（職業 132、技能等級、effect.x 門檻）
- [x] 3. Adapter buff 特效封包加 direction（1320006 例外）+ `TickBerserkAsync`；測試、build、全測試、三本帳、活文件、commit

## 📜 執行歷程（邊做邊追加，附時間）

- 依計畫實作；tick 順序照 Java：龍之魂 → 狂戰士 → 回復術 → 地圖扣血。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> 已完成並 commit。候選：隊伍血條同步（updatePartyMemberHP）；寵物飢餓；怪物 explosiveReward/publicReward 旗標。

## ✅ 結果與結論

- 達標：黑騎士 HP 低於門檻時客戶端與同圖玩家看到狂戰士特效。
- 1172 passed / 1 skipped（P093 基準 1169 +3：Application 336→338、Adapters.V113 571→572）；build 0/0；禁區 grep clean。封包 unverified。

## 🔗 產出

- 修改：`src/Maple.Core/World/Player.PeriodicBuffs.cs`、`src/Maple.Application/Skills/SkillService.cs`、`src/Maple.Adapters.V113/Channel/V113SkillPackets.cs`、`src/Maple.Adapters.V113/Channel/V113PlayerTickHandler.cs`
- 測試：`tests/Maple.Application.Tests/Skills/SkillServiceTests.cs`、`tests/Maple.Adapters.V113.Tests/ChannelPlayerTickHandlerTests.cs`
- 文件：`docs/design/世界tick-設計.md`
