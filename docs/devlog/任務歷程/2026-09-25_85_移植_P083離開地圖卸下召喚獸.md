---
編號: 2026-09-25_85
標題: P083 — 主人離開地圖（換圖/進商城/登出）時卸下召喚獸
類型: 移植
狀態: ✅ 完成
建立: 2026-09-25
更新: 2026-09-25
關聯里程碑: P083（/goal 第 13 件）
關聯記憶: <空>
關聯commit: 見 git log（P083）
---

## 🎯 目標（執行前先寫死，過程不偷改）

P081 建立召喚獸後，主人換圖/進商城/登出時召喚獸會永遠留在舊地圖。對照 Java `MapleMap.removePlayer`：傀儡與原地型召喚獸
取消其 buff（連帶廣播 removeSummon），跟隨型只從舊地圖靜默移出。完成判準：三個離開點都接上、測試綠燈、build 0/0、全測試無退化。
跟隨型帶進新地圖留 P084。

## 📋 背景與查證

- Java `MapleMap.removePlayer`（2555 行起，尾段）：`chr.cancelEffectFromBuffStat(PUPPET)`；逐一召喚獸：STATIONARY/CIRCLE_STATIONARY/
  WALK_STATIONARY → 標記 `cancelSummons`，最後 `cancelEffectFromBuffStat(SUMMON)`（`deregisterBuffStats` 廣播 `removeSummon(summon, true)`）；
  其餘 → `setChangedMap(true)` + `removeMapObject`（不廣播，玩家離開本身讓其他人看不到）。
- Java `removePlayer` 在換圖、進商城、登出都會走到。MapleForge 對應三處：`WarpAsync`、`RemoveFromCurrentFieldForTransitionAsync`、登出清理。

## 🪜 計畫步驟

- [x] 1. `SummonService.DetachOwnerSummons` → `DetachedSummons(Cancelled, Carried)`
- [x] 2. 連線 handler `DetachSummonsOnLeaveAsync`：Cancelled → `CancelBuffBySource` + CancelBuff（有 session 時）+ 舊地圖他人 removeSummon；回傳 Carried
- [x] 3. 三個離開點呼叫；測試、build、全測試、三本帳、commit

## 📜 執行歷程（邊做邊追加，附時間）

- 依計畫實作；進商城/登出路徑不送 CancelBuff 給本人（客戶端正離開頻道），伺服器端 buff 狀態仍同步取消。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> 已完成並 commit。下一步 P084：`WarpAsync` 把 Carried 召喚獸在新地圖玩家位置重新出生並廣播（Java `addPlayer` 的 SUMMON buff 分支），並讓進場玩家看到既有召喚獸。

## ✅ 結果與結論

- 達標：換圖/進商城/登出後舊地圖不再殘留召喚獸；原地型召喚獸的 buff 同步取消。
- 1134 passed / 1 skipped（P082 基準 1133 +1：Application 319→320）；build 0/0；禁區 grep clean。
- 暫時狀態：跟隨型召喚獸換圖後 buff 仍在但新地圖看不到（P084 修）。

## 🔗 產出

- 修改：`src/Maple.Application/Skills/SummonService.cs`、`src/Maple.Adapters.V113/Channel/V113ChannelConnectionHandler.cs`
- 測試：`tests/Maple.Application.Tests/Skills/SummonServiceTests.cs`
