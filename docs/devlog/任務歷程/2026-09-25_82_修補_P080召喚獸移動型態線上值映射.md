---
編號: 2026-09-25_82
標題: P080 — 召喚獸移動型態線上值映射錯位修正（SPAWN_SUMMON）
類型: 修補
狀態: ✅ 完成
建立: 2026-09-25
更新: 2026-09-25
關聯里程碑: P080（/goal 第 10 件，零呼叫者掃描 2/8 前置）
關聯記憶: <空>
關聯commit: 見 git log（P080）
---

## 🎯 目標（執行前先寫死，過程不偷改）

零呼叫者掃描命中 `V113SummonPackets.SpawnSummon`；追查時發現兩個問題：(1) 整個專案沒有任何 `new Summon(`，召喚獸從未被建立
（另開 P081+）；(2) `SpawnSummon` 直接把 Core `SummonMovementType` 數值（1/2/4）寫上線，與 Java 線上值（0/1/2/3/4）全部錯開，
Core 列舉也缺 WALK_STATIONARY / CIRCLE_STATIONARY。本 P 只修 (2)：Core 補語義值、Adapter 做映射。完成判準：映射對照 Java、
既有錯誤斷言修正、build 0/0、全測試無退化。

## 📋 背景與查證

- Java `server.maps.SummonMovementType`：STATIONARY(0)、FOLLOW(1)、WALK_STATIONARY(2)、CIRCLE_FOLLOW(3)、CIRCLE_STATIONARY(4)。
- Java `MaplePacketCreator.spawnSummon`：`write(summon.getMovementType().getValue())`。
- MapleForge 原本 `Stationary=1, Follow=2, CircleFollow=4` 且 `(byte)summon.MovementType` 直寫 → FOLLOW 送成 2（= Java WALK_STATIONARY）。
- 既有測試 `SummonPacketTests` 以 `(byte)SummonMovementType.Follow` 為期望值，等於把錯誤鎖成真相；改成 Java 值 1（新斷言有 Java 依據，非遷就）。
- 架構：Core 列舉只保留語義（§4），線上值留 Adapter。

## 🪜 計畫步驟

- [x] 1. Core 列舉補 `WalkStationary`/`CircleStationary`（語義值）
- [x] 2. Adapter `V113SummonPackets.ToWireMovementType` 對照 Java 映射，`SpawnSummon` 改用它
- [x] 3. 修正既有斷言 + Theory 覆蓋五個值；build、全測試、三本帳、commit

## 📜 執行歷程（邊做邊追加，附時間）

- 依計畫實作。因召喚獸從未被建立，本修正目前沒有玩家可見影響，是 P081 建立召喚獸的前置。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> 已完成並 commit。下一步 P081：SPECIAL_MOVE 施放召喚技能時建立 `Summon`（Java `getSummonMovementType` + `map.spawnSummon`）。

## ✅ 結果與結論

- 達標：召喚獸移動型態線上值與 Java 一致。
- 1121 passed / 1 skipped（P079 基準 1116 +5：Adapters.V113 557→562）；build 0/0；禁區 grep clean。

## 🔗 產出

- 修改：`src/Maple.Core/World/SummonMovementType.cs`、`src/Maple.Adapters.V113/Channel/V113SummonPackets.cs`
- 測試：`tests/Maple.Adapters.V113.Tests/Channel/SummonPacketTests.cs`
