---
編號: 2026-06-12_01
標題: 道具使用四缺口 UseMountFood UseSummonBag UseReturnScroll UseCatchItem
類型: 移植
狀態: ✅ 完成
建立: 2026-06-12 18:44
更新: 2026-06-12 20:02
關聯里程碑: M4-6 / M4-1 / M3 CHANGE_MAP
關聯記憶:
關聯commit: 待填
---

## 🎯 目標（執行前先寫死，過程不偷改）

移植舊 Java `InventoryHandler.java` 的四個道具使用分支：`UseMountFood`、`UseSummonBag`、`UseReturnScroll`、`UseCatchItem`。

完成判準：

- 新增 `V113ItemUseHandler.cs` 承載四個 handler 方法與 v113 parser/流程，不修改既有 inventory USE_* 派發檔。
- 以新增檔案補齊必要 Core domain：`Player.Mount.cs`、召喚袋/返回卷軸/捕捉所需最小領域模型或 service stub，且 Core/Application 不含 v113 opcode/byte layout。
- 四個流程各有 targeted headless/unit 驗證：召喚袋生怪、返回卷軸換圖、坐騎飼料扣道具並增加飽食/馴養度、捕捉道具成功路徑給道具並移除怪物。
- `PORT_REPORT.md` 寫清楚 recv/send opcode 值、新增檔案、中央需接的派發 case/DI/opcode/Player partial、stub/延後事項、build/test 結果。
- 完成 commit，並在根目錄新增 `.codexdone`。

## 📋 背景與假設

使用者指定 branch `port/item-use`，批次協定要求平行 worktree 只新增新檔，不直接修改共用派發、opcode、DI、`Player.cs` 或既有 inventory USE_* 路由檔。Java 行為神諭為 `TestMapleStoryV113_Server/src/handling/channel/handler/InventoryHandler.java` 與 `RecvPacketOpcode.java` / `SendPacketOpcode.java`。真 v113 client/capture 是最終協定驗證，但本批以 headless/unit 驗證交付。

## 🪜 計畫步驟

- [x] 1. 探查現有 inventory、player、field/mob、warp、drop/catch 相關模型與近期自包含 port 風格。
- [x] 2. 讀 Java handler 與 opcode enum，摘出四個 C2S layout、S2C 需求與資料來源。
- [x] 3. 新增 Core/Application/Adapter 檔案，實作四個流程的最小乾淨切片與明確 stub。
- [x] 4. 新增 targeted 測試覆蓋四個 headless 驗證點。
- [x] 5. 跑 targeted build/test，修到通過。
- [x] 6. 更新本任務歷程、寫 `PORT_REPORT.md`、commit，建立 `.codexdone`。

## 📜 執行歷程（邊做邊追加，附時間）

- **18:44** 讀完 batch-5 平行協定、AGENTS 與 required first reads；確認本 worktree 在 `port/item-use`，工作樹已有本任務歷程檔未追蹤。
- **19:22** 完成 Java source map：四個 recv opcode 在 `recv.properties` active（`USE_SUMMON_BAG=0x45`、`USE_MOUNT_FOOD=0x47`、`USE_CATCH_ITEM=0x4B`、`USE_RETURN_SCROLL=0x4F`），disabled 鄰近項只有 `USE_EQUIP_SCROLL`/`USE_POTENTIAL_SCROLL`/`USE_MAGNIFY_GLASS` 等註解項；send 需求為 `SET_TAMING_MOB_INFO=0x2D`、`CATCH_MONSTER=0xF5`、既有 `MODIFY_INVENTORY_ITEM=0x1B` / `UPDATE_STATS enableActions=0x1D`。確認 `WarpAsync` private，不改中央，handler 改回傳 `WarpMapId` / `SpawnMonsterIds` / `BroadcastPackets` / `RemoveMonsterObjectId` 等 intent。
- **20:02** 補齊 Application 新增檔：`WzItemUseCatalog` 讀 `Item.wz` 的 return scroll `spec/moveTo` 與 summon bag `mob/*/{id,prob}`；`ItemUseService` 將 summon/catch intent 實際套用到 `FieldInstance`。序列驗證通過：`Maple.Application` build 0/0、`Maple.Adapters.V113` build 0/0、`PlayerMountTests` 4/4、Application Items 6/6、Adapter ItemUse 11/11。`PORT_REPORT.md` 已更新中央接線與 deferred stub。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

下一步：中央整合時按 `PORT_REPORT.md` 新增 opcode constants/dispatch/DI，註冊 `WzItemUseCatalog` / `ItemUseService`，並用 result intents 呼叫既有 `WarpAsync` 與 spawn/remove/broadcast 流程。`PORT_REPORT.md` / `.codexdone` 依 `.gitignore` 留在 worktree 根目錄作批次交付檔。

## ✅ 結果與結論

已達成本批 headless/unit 交付：四個 Java `InventoryHandler` 道具使用分支落成新增檔，不修改中央派發、opcode、DI 或既有 inventory route。中央能力均以 handler result intent 表示。

保留項：完整騎乘/坐騎持久化、field-limit 轉接、catch dropMessage packet、召喚袋精準 `calcPointBelow` 落地修正、真客戶端 smoke。

## 🔗 產出

- `PORT_REPORT.md`
- `src/Maple.Application/Items/WzItemUseCatalog.cs`
- `src/Maple.Application/Items/ItemUseService.cs`
- `src/Maple.Core/Items/IItemUseCatalog.cs`
- `src/Maple.Core/World/Player.ItemUse.cs`
- `src/Maple.Core/World/Player.Mount.cs`
- `src/Maple.Adapters.V113/Channel/V113ItemUsePackets.cs`
- `src/Maple.Adapters.V113/Channel/V113ItemUseHandler.cs`
- `tests/Maple.Application.Tests/Items/WzItemUseCatalogTests.cs`
- `tests/Maple.Application.Tests/Items/ItemUseServiceTests.cs`
- `tests/Maple.Core.Tests/World/PlayerMountTests.cs`
- `tests/Maple.Adapters.V113.Tests/ChannelItemUsePacketTests.cs`
- `tests/Maple.Adapters.V113.Tests/ChannelItemUseHandlerTests.cs`
- commit: 本任務收尾 commit `port(item-use): add item use intents`
