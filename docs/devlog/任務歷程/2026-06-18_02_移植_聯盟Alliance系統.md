---
編號: 2026-06-18_02
標題: 聯盟 Alliance 系統移植（ALLIANCE_OPERATION / DENY_ALLIANCE_REQUEST）
類型: 移植
狀態: ✅ 完成
建立: 2026-06-18 08:04
更新: 2026-06-18 08:19
關聯里程碑: M6-2
關聯記憶:
關聯commit: 5fca2cd
---

## 🎯 目標（執行前先寫死，過程不偷改）

> 依舊 Java v113 行為神諭，新增 MapleForge 聯盟系統的 Core domain、Application service、V113 adapter handler/packets，以及至少 create/add guild/deny 三個 xUnit 測試。
> 完成判準：
> 1. `Maple.Core/Alliances`、`Maple.Application/Alliances`、`Maple.Adapters.V113/Channel` 與 adapter tests 只新增檔案，不修改既有 dispatch/opcode 檔。
> 2. Core/Application 不含 v113 opcode 或 byte layout 知識。
> 3. `ALLIANCE_OPERATION(0x86)` sub-op 與 `DENY_ALLIANCE_REQUEST(0x87)` 解析/封包至少覆蓋 create、add guild、deny 測試。
> 4. targeted `Maple.Adapters.V113.Tests` 通過；若範圍允許，再跑 Core/Application/Adapters targeted build/test。

## 📋 背景與假設

舊 Java server 是本次 L1 行為神諭：`AllianceHandler.java` 與 `SendPacketOpcode.ALLIANCE_OPERATION=0x3B`。MapleForge 既有 Guild 模型、GuildService、V113 guild handler/packets 可作分層與測試形狀參考。使用者明確要求不修改 `V113ChannelConnectionHandler.cs` 與 `V113ChannelOpcodes.cs`，且本輪只新增檔案。

## 🪜 計畫步驟

- [x] 1. 讀 Java Alliance handler/send opcode 與 MapleForge guild/character/packet patterns。
- [x] 2. 新增 Core Alliance model/repository interface。
- [x] 3. 新增 Application AllianceService in-memory registry 與 business methods。
- [x] 4. 新增 V113AllianceHandler/V113AlliancePackets，讓 handler 可被未來 dispatch 呼叫。
- [x] 5. 新增 AllianceHandlerTests，跑 targeted tests/build。

## 📜 執行歷程（邊做邊追加，附時間）

- **08:04** 建立任務歷程，目標鎖定；下一步讀 Java 與既有 Guild/packet patterns。
- **08:19** 新增 Alliance Core/Application/Adapter/Tests；Alliance filter、Adapters full、Core full、Application full 測試通過。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> 聯盟系統新檔已完成且測試通過；尚未接 `V113ChannelConnectionHandler.cs` / `V113ChannelOpcodes.cs`，符合使用者要求由使用者後續自行接 dispatch。若要接線，從 `V113AllianceHandler.HandleAllianceOperationAsync` 與 `HandleDenyAllianceRequestAsync` 開始。

## ✅ 結果與結論

> 達標。新增 Core `Alliance` / `IAllianceRepository`，Application `AllianceService`（thread-safe in-memory registry + pending invite tracking），V113 `V113AlliancePackets` / `V113AllianceHandler`，以及 4 個 adapter tests 覆蓋 create、accept/add guild、invite、deny。Core/Application 無 v113/opcode/packet 知識；本輪依使用者「Create NEW files only」限制，未更新既有 protocol/progress living docs。

## 🔗 產出

> 新增：
> - `src/Maple.Core/Alliances/Alliance.cs`
> - `src/Maple.Core/Alliances/IAllianceRepository.cs`
> - `src/Maple.Application/Alliances/AllianceService.cs`
> - `src/Maple.Adapters.V113/Channel/V113AlliancePackets.cs`
> - `src/Maple.Adapters.V113/Channel/V113AllianceHandler.cs`
> - `tests/Maple.Adapters.V113.Tests/AllianceHandlerTests.cs`
>
> 驗證：
> - `dotnet test tests/Maple.Adapters.V113.Tests/Maple.Adapters.V113.Tests.csproj --filter Alliance --no-restore --nologo`：4 passed
> - `dotnet test tests/Maple.Adapters.V113.Tests/Maple.Adapters.V113.Tests.csproj --no-restore --nologo`：294 passed / 1 skipped
> - `dotnet test tests/Maple.Core.Tests/Maple.Core.Tests.csproj --no-restore --nologo`：82 passed
> - `dotnet test tests/Maple.Application.Tests/Maple.Application.Tests.csproj --no-restore --nologo`：134 passed
>
> commit：5fca2cd
