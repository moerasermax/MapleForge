---
編號: 2026-06-17_06
標題: 移植 v113 寵物系統 MVP
類型: 移植
狀態: ✅ 完成
建立: 2026-06-17 23:03
更新: 2026-06-17 23:21
關聯里程碑: M4-5 / M6+
關聯記憶: 
關聯commit: 待填
---

## 🎯 目標（執行前先寫死，過程不偷改）

新增 v113 寵物系統 MVP 的 Core domain、Application service、V113 parser/encoder/handler 與 targeted xUnit 測試；只新增檔案，不修改既有檔案，不接中央 dispatch。完成判準：指定新增檔案存在、Core/Application 無 v113 依賴、寵物封包 opcode 與基本結構測試通過，並執行使用者指定的 Core/Adapters 測試與 Host.Shared build。

## 📋 背景與假設

Java 行為神諭來源為 `TestMapleStoryV113_Server` 的 `PetHandler.java`、`MaplePet.java`、`PetPacket.java`。MVP 邊界：先支援 1 隻寵物；寵物撿物只廣播動作，不做道具拾取與 VAC；PetCommand catalog 先 hardcoded 預設 probability 50 / increase 1；MovePet 解析後 blob 轉發廣播。S2C 封包格式以 Java source 為候選真值，尚未經真 v113 client capture/live 驗證，測試不升級為 golden truth。

## 🪜 計畫步驟

- [x] 1. 讀 Java 寵物來源與現有 MapleForge Player/Packet/Handler patterns。
- [x] 2. 新增 Core 寵物模型與常數，補 xUnit domain tests。
- [x] 3. 新增 Application `PetService`，保持 DI/無 static mutable。
- [x] 4. 新增 V113 寵物 parser/encoder/handler 與封包測試。
- [x] 5. 跑指定 targeted tests/build，回填結果。

## 📜 執行歷程（邊做邊追加，附時間）

- **23:03** 建立任務檔；下一步先讀 Java source map 與現有 C# 封包/handler 風格。
- **23:06** 讀完 Java `PetHandler.java`、`MaplePet.java`、`PetPacket.java`；確認 `PET_CHAT` c2s 是 `[long petId][short command][short len][ascii]`，`PET_LOOT` 實際走 `InventoryHandler.PetPickup`。
- **23:10** 新增 Core `Pet` / `PetConstants`，採 Java `ClosenessTable`；Core 不含 v113 opcode/byte layout。
- **23:13** 新增 Application `PetService`，用 instance state 保存每角色一隻 active pet 與 CASH slot；不使用 static mutable。
- **23:16** 新增 V113 pet parser/encoder/static handler；handler 接收 `PetService` 與 broadcast delegate，尚未接中央 dispatch。
- **23:18** 新增 Core/Adapters xUnit 測試並執行指定驗證：Core 79/79、Adapters 280 passed + 1 skipped、Host.Shared build 0 warning / 0 error。
- **23:21** 發現同資料夾存在非本任務 `Math.cs` 未追蹤檔，將 `Pet.cs` 改為明確使用 `System.Math` 避免名稱遮蔽；三個指定驗證命令重跑後仍通過。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

若此刻斷線，寵物 MVP 新檔已完成且指定測試/build 通過；下一步若要接線，需由中央統籌修改 `V113ChannelConnectionHandler` / opcode table / Host DI，並決定寵物持久化資料形狀與真機 smoke 範圍。

## ✅ 結果與結論

已達成本任務 DoD：新增 Core domain、Application service、V113 parser/encoder/static handler 與 targeted tests；未修改既有程式檔與 dispatch 接線。S2C 封包格式證據層級為 Java source + xUnit 結構測試，尚未經真 v113 client capture/live 驗證，因此不標為 golden truth。依使用者「只建新檔」限制，本輪未更新既有 `任務追蹤.md` / `進度日誌.md` / `v113-protocol-spec.md`。

## 🔗 產出

新增檔案：

- `src/Maple.Core/Pets/Pet.cs`
- `src/Maple.Core/Pets/PetConstants.cs`
- `src/Maple.Application/Pets/PetService.cs`
- `src/Maple.Adapters.V113/Channel/V113PetPackets.cs`
- `src/Maple.Adapters.V113/Channel/V113PetHandler.cs`
- `tests/Maple.Core.Tests/Pets/PetTests.cs`
- `tests/Maple.Adapters.V113.Tests/Channel/PetPacketTests.cs`
- `docs/devlog/任務歷程/2026-06-17_06_移植_v113寵物系統.md`

驗證：

- `dotnet test tests/Maple.Core.Tests/ -v quiet --nologo`：通過 79/79。
- `dotnet test tests/Maple.Adapters.V113.Tests/ -v quiet --nologo`：通過 280，略過 1。
- `dotnet build src/Maple.Host.Shared/Maple.Host.Shared.csproj --nologo -v quiet`：0 警告 / 0 錯誤。

關聯 commit：待填（本任務未提交）。
