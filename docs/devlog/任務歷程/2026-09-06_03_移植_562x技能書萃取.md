---
編號: 2026-09-06_03
標題: 562x（Cygnus 五轉）技能書資料萃取
類型: 移植
狀態: ✅ 完成（結論：本 v113 client 無 562x 資料，非程式缺陷）
建立: 2026-09-06 02:30
更新: 2026-09-06 02:45
關聯里程碑: P004（P003 收尾殘餘 TODO：562x 技能書未萃取）
關聯記憶: <空>
關聯commit: 待填
---

## 🎯 目標（執行前先寫死，過程不偷改）

`Maple.Tools.SkillBookExtractor` 目前只掃 `0228`/`0229` 前綴（一般技能書/精通書）；對照 Java
`MapleItemInformationProvider.getSkillStats`（`itemId/10000 in {228,229,562}`），562x 是同一套
schema 的 Cygnus 五轉技能書，P003 收尾時列為殘餘 TODO 未萃取。完成判準：擴充萃取器涵蓋
562x、對真實 v113 Item.wz 跑一次萃取、`minimal-skill-books.v113.json` 含 562x 條目、既有
`JsonSkillBookCatalog`/測試不因新增資料而退。

## 📋 背景與假設

- Java 來源：`MapleItemInformationProvider.getSkillStats` 明確把 228/229/562 三個字首視為同一
  schema（skillid 清單 + masterLevel + reqSkillLevel + success + incXXX 加成）。
- MapleForge 既有 `Maple.Tools.SkillBookExtractor/Program.cs` 已完整實作 228x/229x 的萃取邏輯
  （遞迴找 `0228.img`/`0229.img`、讀 info/skill 節點），562x 只要新增前綴、放寬 itemId 過濾即可，
  不需改資料 schema（`SkillBookDto`/`ISkillBookCatalog` 已是通用 shape）。
- 前置任務 `2026-09-06_02` 已修好 WZ 路徑失聯，`E:\WorkSpace_離線資料\...\v113_Client\Item.wz`
  現在可用，具備實際跑萃取工具的條件。

## 🪜 計畫步驟

- [x] 1. 擴充 `Maple.Tools.SkillBookExtractor/Program.cs`：`0562` 前綴 + itemId 過濾放寬
- [x] 2. 對真實 Item.wz 跑萃取
- [x] 3. 追查 562x=0：直接用 `WzFile`/`WzDirectory` 遞迴掃描整個 Item.wz 樹找任何路徑含 `562` 的 image——**完全沒有**，確認非程式邏輯問題
- [x] 4. `dotnet build` + `git diff` 確認 `minimal-skill-books.v113.json` 無變化（165 筆不變，228x=26/229x=139/562x=0）
- [x] 5. 文件同步（任務歷程、進度日誌）+ commit

## 📜 執行歷程（邊做邊追加，附時間）

- **02:30** 讀完 Java 來源與既有萃取器程式碼，確認擴充範圍很小（純資料萃取，不涉及 Core/Application/Adapters 改動）。改 `Program.cs`：`Extract()` 前綴清單加 `"0562"`、`ExtractImage` 的 itemId 過濾放寬成 `is not (228 or 229 or 562)`、輸出訊息加 562x 計數。
- **02:35** 對真實 `E:\...\v113_Client\Item.wz` 跑萃取：`Wrote 165 skill-book entries ... (228x=26, 229x=139, 562x=0)`。`--inspect 5620000` 回報 not found。
- **02:40** 懷疑是 img 檔命名慣例猜錯或過濾條件有誤，寫一個獨立的小工具（`Maple.Content` 為 ProjectReference）直接遞迴列出 `Item.wz` 整棵樹裡任何路徑名稱包含 `562` 的 image 節點——**完全沒有任何結果**（相對照，`0228.img`/`0229.img` 確實都在 `/Consume/` 底下）。確認本 v113 client（2010-04 build）的 `Item.wz` 根本沒有 562x（Cygnus 五轉技能書）資料，不是程式碼命名前綴或過濾條件寫錯。`git diff` 確認輸出 json 與既有版本逐 byte 相同（562x 貢獻 0 筆，無變化）。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> 本任務已釐清並收斂：**562x 技能書無法從本 v113 client 萃取，因為該版本 Item.wz 根本沒有這份資料**（非萃取器程式碼缺陷——已保留 562x 支援程式碼，未來若拿到含 Cygnus 內容的更新版 client 即可直接跑）。若之後要讓私服的皇家騎士團/Cygnus 職業（`CharacterService.cs` job=1000 系列）真的有技能書可用，選項是：①找一份較新的、含 Cygnus WZ 資料的 v113+ client 補丁 ②改用其他機制（如直接把技能綁定角色而非透過技能書道具）發放這些技能，屬另一個獨立設計決策，超出本任務範圍，記錄供未來參考。

## ✅ 結果與結論

- **562x 技能書無法萃取的根因是資料不存在，不是程式問題**：本 v113 client 的 `Item.wz` 完全沒有任何 `562` 開頭的 item 節點。萃取器程式碼已依 Java oracle（`getSkillStats` 的 228/229/562 判斷）正確擴充並驗證過（能正確讀 228x/229x，過濾邏輯對 562x 前綴同樣有效，只是找不到資料源），保留下來對未來換用更新版 client 有價值。
- **可轉移心法**：P003 收尾把「562x 技能書未萃取」記成「還沒做的工作」，但深入後發現真相是「這份 client 資料本來就沒有」——這類殘留 TODO 在下一次撿起來做之前，值得先花幾分鐘確認「資料是否存在」，而不是預設「只是還沒寫程式」。避免把一個「已探明的資料缺口」誤判成「待辦事項」而重複嘗試。

## 🔗 產出

- 修改：`tools/Maple.Tools.SkillBookExtractor/Program.cs`（562x 前綴支援，forward-compatible，本次輸出無變化）
- 無變化：`src/Maple.Content/Skills/minimal-skill-books.v113.json`（165 筆不變）
- 文件：本檔、`docs/devlog/進度日誌.md`
