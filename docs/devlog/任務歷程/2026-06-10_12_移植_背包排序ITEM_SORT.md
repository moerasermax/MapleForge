---
編號: 2026-06-10_12
標題: 移植背包排序 ITEM_SORT
類型: 移植
狀態: 完成
建立: 2026-06-10
更新: 2026-06-10
關聯里程碑: Java→.NET 移植主線 / 背包
關聯記憶: v113-pivot-port-from-java
關聯commit: 未提交
---

## 目標

移植舊 Java `InventoryHandler.ItemSort` 主幹，將指定背包依 itemId 排序並重新放回前方格位。

完成判準：

1. Core `Inventory` 支援版本無關 sort 行為。
2. v113 adapter 接上 `ITEM_SORT(0x3F)` parser。
3. Channel handler 更新角色背包快照並送完成/放行封包。
4. 有 Core 與 Adapter 測試覆蓋。

## 執行歷程

- 建檔定目標。
- 對照舊 Java `InventoryHandler.ItemSort`，確認封包為 `tick + inventory type`，完成封包使用 Java enum `GATHER_ITEM_RESULT(0x32)`。
- Core `Inventory` 新增版本無關 `SortByItemId`，依 itemId 排序後從 slot 1 開始重新排列。
- Channel handler 與 `ITEM_GATHER` 共用 arrange 流程，成功或無變更皆送完成封包與 `EnableActions`。
- 補 Core inventory arrange 測試與 Adapter 完成封包測試。

## 接手點

- 本任務已完成。後續可補真機背包 sort 按鈕 smoke，並在完整 Item metadata 到位後校準 Java cash item/pet item 特殊排序。

## 結果

- 完成 `ITEM_SORT(0x3F)` 主幹移植。
- 驗證：Core 45/45、Application 83/83、Adapters.V113 165/165、Host build 0 警告 0 錯誤。
- 邊界：完成封包命名沿用舊 Java enum 實際寫法：sort 回 `0x32`，gather 回 `0x33`。
