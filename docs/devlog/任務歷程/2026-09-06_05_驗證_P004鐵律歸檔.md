---
編號: 2026-09-06_05
標題: P004 鐵律歸檔
類型: 驗證
狀態: ✅ 完成
建立: 2026-09-06 04:00
更新: 2026-09-06 04:10
關聯里程碑: P004
關聯記憶: <空>
關聯commit: 待填
---

## 🎯 目標（執行前先寫死，過程不偷改）

比照 P002/P003 收案慣例，把 P004（`2026-09-06_01`~`_04` 四個任務歷程）的三本帳（任務追蹤/進度日誌/移植狀態地圖）收斂一致、核對驗收基本線（session-invariants §8）全數過關，正式收案。

## 📋 背景與假設

P004 是 P003 收案時留下的殘餘 TODO 清尾，本次共完成 4 件工作：
1. PARTY_SEARCH_START/STOP 完整移植（`2026-09-06_01`）
2. 全站繁中封包編碼缺陷修補（同上，附帶發現）
3. WZ/scripts 路徑失聯修補（`2026-09-06_02`）
4. 562x 技能書資料缺口查明（`2026-09-06_03`）
5. RandomRewards 金/銀寶箱權重表（`2026-09-06_04`）

本次沒有多輪外部審查代理人（agy/Codex/GPT-5.5）可用，改由同一 session 自行覆核：逐項對照 session-invariants §8 驗收基本線，並重跑一次全測試套件確認累積效應無退化。

## 🪜 計畫步驟

- [x] 1. 逐項核對 4 個任務歷程的驗收基本線（Java 來源/Core-Application 邊界/測試/unverified 標記/文件同步）
- [x] 2. 重跑全 8 個測試專案，確認無退化
- [x] 3. `任務追蹤.md` 補 P004 段落與戰績
- [x] 4. commit

## 📜 執行歷程（邊做邊追加，附時間）

- **04:00** 逐項核對：
  - PARTY_SEARCH：Java 來源明確（`PartyHandler`/`World.PartySearch`/`MapleJob`）；Core/Application 邊界正確（`MapleJobFamily`在Core、`PartySearchService`在Application，皆不 import Adapters.V113）；有單元測試（Core 6、Application 17、Adapters 4，皆通過）；S2C 沿用既有 `PartyInvite` 封包（非新 candidate，不需額外 unverified 標記）；文件已同步（移植狀態地圖/進度日誌/任務追蹤）。
  - Big5 編碼修補：影響全域但改動集中在 `Maple.Core.IO`，無 Core/Application 邊界問題；有單元測試間接覆蓋（PartySearch 中文訊息 round-trip）；文件已於同任務歷程記載。
  - WZ 路徑修補：純設定/測試檔案路徑修正，無架構疑慮；驗證方式是重跑測試套件（25 個轉綠）。
  - RandomRewards：Java 來源明確（`RandomRewards.java`/`GameConstants`）；Core/Application 邊界正確；單元測試完整（Application 8 個全表覆蓋、Adapters 2 個含新舊 fixture）；S2C 沿用既有 `ShowRewardItemAnimation`（已標 unverified，未變動）；文件已同步。
  - 全數符合 session-invariants §8 驗收基本線。
- **04:05** 重跑全 8 個測試專案：Core 126、Application 186、Adapters 431/1 skip、Persistence 11、Net 2、Content 21、Tools.PacketDecoder 22、Tools.HeadlessClient 29 ＝ **828 passed / 1 skipped**，與 D1~D4 各自驗證時一致，無退化。
- **04:08** `docs/devlog/任務追蹤.md` 補上 P004 段落（比照 P002/P003 格式）與殘餘 TODO 轉入 P005+ 的清單。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> P004 已收案。下一步開 P005，範圍待定（ENTER_MTS/TOUCHING_MTS/MTS_TAB minimal 版、寶箱稀有度廣播、反作弊 3 件、Shammos/Node 前置研究、MONSTER_CARNIVAL 拆解評估 為候選），將依使用者「至少做到 P010」指示連續執行，每個 P 完成即歸檔，不中途回報。

## ✅ 結果與結論

P004 四項工作皆符合驗收基本線，828 passed / 1 skipped，Core/Application 禁區乾淨。收案。

## 🔗 產出

- 修改：`docs/devlog/任務追蹤.md`（新增 P004 段落）
- commit：待填
