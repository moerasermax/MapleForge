---
編號: 2026-06-06_02
標題: V113 整個外層 + MapleForge 檔案深度整理分類
類型: 重構
狀態: ✅ 完成（已 commit+push 私有 master：93db08c）
建立: 2026-06-06 (session)
更新: 2026-06-06 (session)
關聯里程碑: 專案衛生
關聯記憶: default-collaborate-with-ai-team, keyboard-injection-investigation
關聯commit: ab99a17
---

## 🎯 目標（執行前先寫死，過程不偷改）

把 `D:\WorkSpace\AI_Lab\研究中\MapleStory\V113`（外層）與 `MapleForge\`（內層）散亂檔案依內容分門別類、刪除確認過的垃圾，整理到根目錄乾淨可讀。
**驗收判準**：①MapleForge 根目錄不再有散落的 diag*.ps1/live-*.log/*.obj/截圖/暫存檔（腳本收進資料夾、垃圾刪除）②`dotnet build` 仍綠（搬移未破壞 build/config 路徑引用）③搬移後仍可運行的腳本實測或路徑已正確修補 ④三本帳回填。

## 📋 背景與假設（盤點結論）

- **git**：V113 外層**不是** git repo（純 fs 操作）；`MapleForge\` **是** git repo（搬追蹤檔用 `git mv`）。
- **必須留原位（絕對路徑寫死引用，搬了會壞）**：
  - `v113_Client`（appsettings.json `WzDirectory`、ServerInstanceOptions.cs、tools/test-windower.ps1 皆絕對路徑指它）
  - `TestMapleStoryV113_Server`（ServerInstanceOptions.cs `ScriptsDirectory` 絕對路徑、Java 神諭）
  - `V113\CLAUDE.md`（每 session 自動載入）、`MapleForge.slnx`/`Directory.Build.props`/`global.json`（build 根配置）
- **確認垃圾（直接刪）**：
  - 外層：`host.obj`、`windower.obj`、`windower_host.log`、`cache/`(ai-cli projects.json，會再生)
  - MapleForge：`_seq5_decoded.txt`(封包 dump)、`Dtmp_b64.txt`/`Dtmp_readme_b64.txt`(base64 暫存)、git 已忽略的 `live*.log`/`*.obj`/`windower_host.log`/`client-shot*.png`/`diag-shots/`/`cache/`
- **腳本搬移風險**：根目錄 15 個 `diag*.ps1` + `launch.ps1`/`test-live*.ps1`/`capture-manual-login.ps1` 都 `$Root=$PSScriptRoot` 當 repo 根再 `dotnet run --project src/...` → 搬進子資料夾後 `$PSScriptRoot` 不再是根 → **必須同步把根解析改成上一層**(`Split-Path $PSScriptRoot -Parent`)。被歷史文件提及但只是文字，搬後留指標即可。
- **可選分組（安全，非 code 引用）**：外層 `_wz_ref`(13M)/`_hare_ref`(102M)/`plugin_sample` 可收進 `_ref/`。

## 🪜 計畫步驟

- [ ] 1. 刪除確認垃圾（外層 + MapleForge 根，git-ignored 直接 rm）
- [ ] 2. Codex 續 session 覆核：搬移不破壞 build/csproj glob/腳本互相引用，refine 腳本 taxonomy
- [ ] 3. MapleForge 根腳本 `git mv` 進 `scripts/`(+`scripts/diag/`)，同步修補根路徑解析
- [ ] 4. `dotnet build` 驗證綠；抽一支搬後腳本確認路徑對
- [ ] 5.（可選）外層 `_ref/` 分組
- [ ] 6. commit（git mv + 修補）；回填三本帳

## 📜 執行歷程（邊做邊追加，附時間）

- **T0** 盤點：git 狀態、絕對路徑引用、可疑檔內容、腳本 $PSScriptRoot 用法、.gitignore 已忽略清單 — 全數查明。
- **T1** 刪垃圾：MapleForge 根刪 16×live*.log + 2×.obj + 2 截圖 + 2 暫存 txt + _seq5_decoded.txt + diag-shots/ + cache/；外層刪 host.obj/windower.obj/windower_host.log/cache。
- **T2** Codex(pid 43300, 續 session 019e9a19) 覆核：.slnx 只引用 src/tests/tools csproj、無 glob/Exec 掃根腳本 → 搬移 build 安全；抓到依賴 `test-live-capture→test-live`、多支 diag→`$Root\tools\seed-test-data.ps1`；建議統一兩層分類。
- **T3** `git mv` 18 支腳本：4 支→`scripts/live/`、14 支→`scripts/diag/`。
- **T4** 批次修補根解析：17 支 PowerShell 字面替換 + test-live.ps1(寫死絕對路徑) Edit → 全部 `$Root=(Resolve-Path "$PSScriptRoot\..\..").Path`；test-live-capture 委派改同層 `$PSScriptRoot`。
- **T5** 驗證：兩夾往上兩層皆解析回 MapleForge 根，slnx/seed-test-data.ps1 皆找得到 ✓。補 `scripts/README.md`（含舊路徑對照）。
- **T6** 外層 `_ref/` 分組：plugin_sample 成功；`_wz_ref`/`_hare_ref` 檔案鎖(access denied)無法搬 → 回退保持一致，三者留根層(已 _/sample 前綴靠邊)，確認 101M/12M 內容完好無損。

## ⏯️ 接手點（★崩潰救命行★ — 永遠保持最新一行）

> ✅ 整理完成、已驗證、**未 commit**。git working tree 有 18 個 staged rename(帶根路徑修補) + scripts/README.md + 兩份新任務歷程 + 取經夾。下一步若使用者要 commit：`git add -A`（排除無關的 2026-06-02_13 既存修改）後 commit；可選 push 私有 master。腳本搬移不影響 .NET build（build 輸入未動，Codex 已驗 .slnx scope），故未跑全建（本機 BSOD 風險、CLAUDE.md 戒全建）。

## ✅ 結果與結論

> 達標。**MapleForge 根目錄**從 19 散檔+大量垃圾 → 乾淨剩 build 配置(4)+資料夾(src/tests/tools/docs/data/management/scripts)。腳本分類進 `scripts/{live,diag}/` 且全部可正常運行（根解析統一往上兩層、依賴 seed-test-data 找得到、capture→live 委派同層）。**外層**刪除 obj/cache 垃圾；大資料夾因絕對路徑釘住(v113_Client/TestMapleStoryV113_Server)或檔案鎖(_wz_ref/_hare_ref)保留原位。驗收：根目錄乾淨✓、搬移未破壞 build 輸入(Codex 驗+路徑解析實測)✓、腳本路徑正確✓。
> 心法（可轉移）：搬被引用的腳本前，先查①build 是否 glob/Exec 掃到②腳本間互相 dot-source/呼叫③腳本對 repo-root 的相對假設 —— 三者齊備才搬，搬後同步修根解析並實測。

## 🔗 產出

> 18 個 `git mv`（scripts/live/ ×4、scripts/diag/ ×14，皆含根路徑修補）；新增 `scripts/README.md`；刪除約 26 個垃圾檔/夾（MapleForge 根 + V113 外層）。commit：未提交（待裁示）。
