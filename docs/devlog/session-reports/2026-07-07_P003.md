# Session Report — P003 移植清尾 + MVP stub 補完整（2026-07-06 ~ 2026-07-07）

## 任務計畫

計畫檔：`docs/devlog/執行計畫/P003_移植清尾與stub補完整.md`
任務歷程：`docs/devlog/任務歷程/2026-07-06_01_移植_P003清尾與stub補完整.md`

偵察核實 `docs/design/移植狀態地圖.md` 嚴重過期：標 ❌ 的 44 個 opcode 中，43 個其實已接 dispatch（~10 完整、~33 為 `EnableActions` 薄 stub），唯一真沒接的 `USE_FISH_MERCHANT` 在 Java 原版也是「尚未開放」佔位訊息。真正缺口集中在 **HiredMerchant（Java ~1024 行）、ItemMaker（~568 行）**；MonsterCarnival（agy 估 5-7 天）延 P004；MTS 維持 stub。

本 session 目標：①移植狀態地圖修正到與程式碼一致 ②小件 stub 補完整（PlayerHandler 雜項 8 項 + CouponCode/GamePoll/MapleTV/BeansUpdate + Mob 4 項依研究結論）③ItemMaker 完整 + SkillBook catalog 資料 ④HiredMerchant 全套三刀（Core→Adapters→啟動重載）⑤兩輪外部審查缺陷修補 ⑥鐵律歸檔。

---

## 成果總結

**HEAD `f3eadf1`+本次歸檔 commit，775 passed / 1 skipped**（起點基線 645，+130）

### 派工鏈（commit）

| 派工 | 內容 | 執行者 | commit |
|------|------|--------|--------|
| D0/D0b | 移植狀態地圖大修正 + Mob 特殊四項研究 | Sonnet | `34a7f91` + `ff87115` |
| D1 | opcode swap 修正 + SHOW_EXP_CHAIR/REWARD_ITEM/USE_TREASUER_CHEST/CP_UserThrowGrenade | Codex | `f9a7c22` |
| D2 | ARAN_COMBO combo buff + CYGNUS_SUMMON/SNOWBALL/LEFT_KNOCK_BACK | Codex | `1bc22a1` |
| D3 | COUPON_CODE/GAME_POLL/MAPLETV/CP_BeansUpdate + MONSTER_BOMB | Codex | `9f7a60d` |
| D4 | ItemMaker 合成系統完整移植 | Codex | `39f739a` |
| D4b | SkillBook catalog Item.wz 萃取 165 筆 | Codex | `bc021dc` |
| D5 | HiredMerchant Cut1 — Core 領域 + PlayerShopService + 持久化 | Codex | `f64bce1` |
| D6 | HiredMerchant Cut2 — Adapters 0x34/0x38/0x3A 接線+封包 | Codex | `8c22d6a` |
| D8-前置 | agy 唯讀審查 D0-D6（2 BLOCKER + 2 WARNING） | agy | —（唯讀） |
| D7 | HiredMerchant Cut3 — 0x73 子指令+啟動重載+整合測試 | Codex | `7a6f347` |
| D7b | 修補外部審查缺陷 — coupon封包/Claim race/期限語意/unverified註記 | Codex | `ed49640` |
| D8 | GPT-5.5 終審 D7+D7b（5 項缺陷） | GPT-5.5 | —（唯讀） |
| D8b | 終審修補 — 訪客廣播範圍/鎖紀律/封包版型/啟動降級/unverified | Codex | `f3eadf1` |
| D8複核 | agy 複核 9 項全 ✅，判定可收案 | agy | —（唯讀） |
| D9 | 鐵律歸檔（三本帳+地圖終版+session report） | Sonnet | 本次 |

### 測試變化

| 里程 | 測試數 | 差異 |
|------|--------|------|
| P003 起點基線 | 645 passed / 1 skipped | — |
| D1 | 714 passed / 1 skipped | +69 |
| D2 | 722 passed / 1 skipped | +8 |
| D3 | 733 passed / 1 skipped | +11 |
| D4 | 747 passed / 1 skipped | +14 |
| D4b | Content 18（含入 D4 合計） | — |
| D5 | 761 passed / 1 skipped | +14 |
| D6 | 767 passed / 1 skipped | +6 |
| D7 | 772 passed / 1 skipped | +5 |
| D7b | 775 passed / 1 skipped | +3 |
| D8b | 775 passed / 1 skipped | +0（修補不增測，維持不退化） |
| **最終** | **775 passed / 1 skipped** | **+130** |

`dotnet build --nologo` 全程 0 warning / 0 error；Core/Application 禁區 `rg -n "Maple.Adapters.V113" src/Maple.Core src/Maple.Application` 全程無命中。

### 主要交付

- **13 個小件 stub 補完整**：opcode 對調 bug（`CLIENT_FEEDBACK`/`CLIENT_ERROR`）、`SHOW_EXP_CHAIR`/`REWARD_ITEM`/`USE_TREASUER_CHEST`/`CP_UserThrowGrenade`、`ARAN_COMBO` combo buff、`CYGNUS_SUMMON`/`SNOWBALL`/`LEFT_KNOCK_BACK`、`COUPON_CODE` 兌換全鏈、`GAME_POLL`/`MAPLETV`/`CP_BeansUpdate`、`MONSTER_BOMB` 無獎勵擊殺路徑。
- **ItemMaker 完整合成系統**：Content `WzItemMakeCatalog` 實讀 `Etc.wz/ItemMake.img` 61 筆寶石 + 726 筆職業裝備配方（合計 787），Application `ItemMakerService` 支援製寶石/裝備/結晶/分解。
- **SkillBook catalog**：從 `Item.wz` 萃取 228x/229x 技能書 165 筆。
- **HiredMerchant 全套**：Core `IPlayerShop`/`PlayerShop`/`HiredMerchant` 領域模型、Application `PlayerShopService`（建店/上架/購買/收益/過期，owner/store 鎖保護）、Persistence LiteDB/Mongo、Adapters 0x34/0x38/0x3A + `PLAYER_INTERACTION(0x73)` 全 merchant 子指令、啟動過期重載服務。

### 審查與修補

- **agy D8-前置**（PID 252）：2 BLOCKER（coupon S2C 版型錯位有 crash 風險、`ClaimAsync` 雙重領取 race）+ 2 WARNING（coupon 永久期限語意、unverified 註記缺失）→ **D7b 全數修補**。
- **GPT-5.5 D8 終審**（PID 49356）：5 項缺陷（店內 UI 廣播範圍錯誤、adapter 繞過 service 鎖、`CLOSE_MERCHANT` 封包版型錯誤、啟動 reload 例外未降級、E2E fixture 缺 unverified）→ **D8b 全數修補**。
- **agy D8複核**（PID 35860）：9 項缺陷（D8-前置 4 + D8 5）全數確認修補到位，無新缺陷，**判定 P003 可收案**。

### 移植狀態地圖終版統計

| 狀態 | 數量 | 變化 |
|---|---|---|
| ✅ 已接 dispatch 且有完整/合理 handler | 146 | +5（REWARD_ITEM/USE_TREASUER_CHEST/HiredMerchant×3 由 🟨 升級） |
| 🟨 已接 dispatch，僅薄邏輯待補完整 | 12 | -5 |
| 🟦 有程式碼但不在 dispatch | 1 | — |
| ❌ 未移植 | 0 | — |
| 🚫 Java disabled / 不需移植 | 5 | — |
| **合計** | **164** | — |

### 過程事實（不美化，照實記錄）

- **3 次 Codex 額度撞限**：W2 的 D4+D4b 共用一次、D5 一次、D7 一次，皆由使用者兌換 usage limit reset 因應。
- **多次 CLI 假死中斷續跑**：D2、D4（×2）、D4b（×2）皆需 kill 殭屍行程後續跑完成。

---

## 下一步（P004 候選，殘餘 TODO 不擋本次收案）

- HiredMerchant / ItemMaker 全數 S2C 封包為 Java-source candidate/unverified，待真 v113 client GUI smoke 校準。
- `REWARD_ITEM` / `USE_TREASUER_CHEST` 的 `Etc.wz`/`RandomRewards` 權重 catalog（目前為 deterministic fallback）。
- Snowball event 子系統完整落地（生命週期/team assignment/prize/event broadcast）。
- Mob 特殊三項（`FRIENDLY_DAMAGE`/`HYPNOTIZE_DMG`/`DISPLAY_NODE`）依賴的 Shammos 護送 / Node 子系統。
- MonsterCarnival（agy 估 5-7 天）、MTS 完整拍賣。
- 562x 技能書尚未從 `Item.wz` 萃取。
