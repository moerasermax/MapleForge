# 知識入庫待補 — P003（2026-07-07）

> knowledge MCP 後端連續逾時（namespaces 可查、list/search/write 全逾時），先暫存於此。
> 後端恢復後以 `knowledge_write` 逐條入庫（建議 namespace：`楓之谷V113`，尚不存在、首寫自動建立），入庫後刪本檔。

## 條目 1｜lesson｜P003 移植清尾戰役總結 — HiredMerchant/ItemMaker 全鏈落地與審查閉環
tags: P003, HiredMerchant, ItemMaker, 移植, review-loop, MapleForge

P003（2026-07-06~07）把移植狀態地圖的 ❌ 清零：8 專案基準 704→775 測試（+71）、13 個小件 stub 補完整、ItemMaker 全鏈（Etc.wz/ItemMake.img 787 配方）、SkillBook catalog 165 筆、HiredMerchant 全套（Core IPlayerShop 領域+PlayerShopService 雙層鎖+LiteDB/Mongo+0x34/0x38/0x3A+0x73 子指令+啟動重載+E2E）。commit 鏈 f9a7c22..1fcd8b2，稽核 7/7（d0e719e）。

關鍵教訓：
1. 地圖類文件會嚴重過期（宣稱 ❌44 實際只 1 個未接）——動工前先派唯讀偵察核實，省下一半計畫量。
2. 審查閉環抓到 4+5 個真缺陷（coupon 封包結構錯位、Claim 雙重領取 race、店內 UI 誤用全圖廣播、adapter 繞過 store lock）——「實作者自測綠」不等於可收案，獨立審查席（不同 AI、不同 session）必要。
3. 大系統按架構層拆刀（Core→Adapters→啟動整合）比按 opcode 拆刀好審好續跑；多 opcode 全改同一 switch 檔時嚴禁平行派工。
4. 測試數字錨點要固定口徑：跨波次比較必須用同一組測試專案加總；稽核可用 git worktree 回基線 commit 實跑驗證。
5. 未驗證債：HiredMerchant/ItemMaker/coupon 全部 S2C 是 Java-source candidate（unverified），待真機 GUI smoke。P004 候選：MonsterCarnival（5-7天級）、MTS 保管箱、Snowball event、Shammos+Node 包、RandomRewards catalog。

## 條目 2｜architecture｜HiredMerchant 分層設計 — Java 繼承體系轉 C# 組合
tags: HiredMerchant, PlayerShop, 分層架構, OdinMS移植

Java OdinMS 的 AbstractPlayerStore（462 行）繼承體系（雇用商人與玩家商店共用）在 MapleForge 改為組合+介面：Core `IPlayerShop` 介面 + `PlayerShop`/`HiredMerchant` 領域模型（架上商品/遊客席/黑名單/累計楓幣/過期時間內聚）；Application `PlayerShopService` 集中全部 mutation（開店/上架/購買/關店結算/離線領回/過期轉 claimable），owner-level 與 store-level 兩層 SemaphoreSlim instance lock（無 static）；Persistence `hired_merchants`+`hired_merchant_items` 兩實體、LiteDB/Mongo 雙 provider；Adapters 只做 0x34/0x38/0x3A/0x73 封包編解碼並呼叫 service，**adapter 不得直改 repository（審查曾抓到繞鎖缺陷）**。店內 UI 封包經 `IHiredMerchantSessionDispatcher` 只送 owner/visitors，SPAWN/DESTROY 才全圖廣播（對照 Java broadcastToVisitors）。啟動重載走 `HiredMerchantReloadHostedService`（try/catch 降級，不殺 host）。

## 條目 3｜pitfall｜Codex CLI 額度與假死實測 SOP（建議入 _global）
tags: codex, ai-cli, quota, session-resume, SOP

實測（2026-07-06~07，ChatGPT 帳號）：
- 5h 滾動額度約 3 個 xhigh 重派工撞限；撞限錯誤訊息帶恢復時間；帳上 usage limit reset 可在 codex TUI `/usage` 兌換（消耗性）。
- CLI 長跑常無故假死（exit 1 或 4294967295，非額度）——**用 session_id resume 續跑即無損接續**（工作區變更與 session 脈絡都在）；判斷假死 vs 額度：看 stdout 是否有 usage limit 錯誤事件。
- 平行兩個 codex 派工更易同時假死，重要大刀改串行。
- 等額度恢復：背景 PowerShell 定時器（run_in_background + Start-Sleep 到目標時刻）喚醒後自動續派，配合任務歷程接手點可全自動過夜推進。
