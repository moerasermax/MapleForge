# MapleForge — 進度日誌（Worklog）

> 每次工作 session 一條，最新在上。記「做了什麼、卡在哪、下一步」。
> 與 `任務追蹤.md`（狀態）、git log（程式碼帳本）三者互補。

## 2026-07-06（P003 D6 — HiredMerchant Cut2 Adapters）

- ✅ 對照 Java `PlayerInteractionHandler.HiredMerchantRemoteControl`、`HiredMerchantHandler.UseHiredMerchant` / `MerchantItemStore`、`PlayerShopPacket`，把 `CP_HiredMerchantRemoteControl(0x34)`、`USE_HIRED_MERCHANT(0x38)`、`MERCH_ITEM_STORE(0x3A)` 從 EnableActions stub 接到 D5 `PlayerShopService` / `IHiredMerchantRepository`。
- ✅ 新增 `V113HiredMerchantHandler` 與 `V113HiredMerchantPackets`：遠端管理視窗、商人 permit/title box、Fredrick package list/claim、claim 後 inventory/meso 更新、open merchant 進場/換圖 replay spawn；`SPAWN_HIRED_MERCHANT(0x103)`、`MERCH_ITEM_STORE(0x143)`、`MERCH_ITEM_MSG(0x142)` 等 S2C 皆標 unverified。
- ✅ 0x38 支援 Java title probe 與 adapter candidate create fragment；真 client 商人 create/visit/add-item/buy/remove 仍在 `PLAYER_INTERACTION(0x73)` 子指令，現有 `V113PlayerInteractionRouter` 只支援 trade，列 Cut3/後續 TODO。
- ✅ Persistence 測試專案加 assembly-level 停用平行化，避免 LiteDB/BsonMapper roundtrip 測試並行干擾；不改產品行為。
- 🧪 驗證：`dotnet build --nologo -v quiet` 0 warning / 0 error；Core 110、Application 150、Persistence 10、Content 21、Net 2、Tools.PacketDecoder 22、Tools.HeadlessClient 29 全綠；Adapters 423 passed / 1 skipped；逐專案總測 767 passed / 1 skipped；Core/Application 禁區 `rg` 無 V113 依賴。

## 2026-07-06（P003 D5 — HiredMerchant Cut1 Core/App/Persistence）

- ✅ 對照 Java `HiredMerchantHandler` / `HiredMerchant` / `AbstractPlayerStore` / `PlayerInteractionHandler` 商店分支，建立 Core `IPlayerShop`、`PlayerShop`、`HiredMerchant`、listing/visitor/blacklist/settlement model 與 `IHiredMerchantRepository` contract。
- ✅ 新增 Application `PlayerShopService`：建立/開店、上架扣背包、訪客購買、下架、收益領取、關店暫存、離線領回、過期轉 claimable；購買規則涵蓋 bundle 售罄、楓幣上限、黑名單、過期與 Java `EntrustedStoreTax`。
- ✅ 新增 Persistence LiteDB/Mongo provider：`hired_merchants` header + `hired_merchant_items` item rows，接入 `AddMaplePersistence` provider switch；新增 `docs/specs/persistence-model.md` 記錄 collection shape。
- ⚠️ 邊界：本刀不接 `Maple.Adapters.V113`、不接 opcode dispatch、不產生 HiredMerchant UI S2C fixture；Cut2 需接 0x34/0x38/0x3A parser/handler/packets。
- 🧪 驗證：`dotnet build --nologo -v quiet` 0 warning / 0 error；Core 110、Application 150、Persistence 10、Content 21、Net 2、Tools.PacketDecoder 22、Tools.HeadlessClient 29 全綠；Adapters 417 passed / 1 skipped；逐專案總測 761 passed / 1 skipped；Core/Application 禁區 `rg` 無 V113 依賴。

## 2026-07-06（P003 D4 — ITEM_MAKER 完整合成系統）

- ✅ 對照 Java `ItemMakerHandler.ItemMaker` / `ItemMakerFactory`，將 `ITEM_MAKER(0x6B)` 從 `EnableActions` stub 升級為 Core contract、Content WZ catalog、Application service 與 v113 handler 全鏈。
- ✅ Content `WzItemMakeCatalog` 讀 `Etc.wz/ItemMake.img`：寶石配方 61 筆、職業裝備配方 726 筆；另以中性 contract 暴露必要 item metadata（`reqLevel`/`tuc`/`inc*`/`itemMakeLevel`/`flag`），不把 WZ path 洩漏到 Application。
- ✅ Application `ItemMakerService` 支援 makerType 1 寶石/裝備製作、makerType 3 100 個 ETC 轉結晶、makerType 4 裝備分解；處理 Maker 技能 gate、材料/楓幣扣除、催化劑 90% 成功率、寶石 stat 加成、裝備 randomize stats 與分解 RNG。
- ⚠️ 邊界：ItemMaker success effect S2C 依 Java `MaplePacketCreator.ItemMaker_Success*`，fixture 標 `unverified`；真 v113 client ItemMaker GUI smoke 未跑。
- 🧪 驗證：`dotnet build --nologo -v quiet` 0 warning / 0 error；Core 104、Content 21、Application 145、Persistence 7、Net 2、Tools.PacketDecoder 22、Tools.HeadlessClient 29 全綠；Adapters 417 passed / 1 skipped；逐專案總測 747 passed / 1 skipped；Core/Application 禁區 `rg` 無 V113 依賴。

## 2026-07-06（P003 D4b — SkillBook catalog Item.wz 萃取）

- ✅ 對照 Java `MapleItemInformationProvider.getSkillStats`，用既有 `Maple.Content.Wz` reader 從 v113 `Item.wz` 萃取 228x/229x 技能書資料，產出 `src/Maple.Content/Skills/minimal-skill-books.v113.json`。
- ✅ 萃取結果：165 筆（228x mastery book 26 筆、229x skill book 139 筆）。抽查 `2290096`：13 個四轉技能、success 70、reqSkillLevel 5、masterLevel 20；抽查 `2280000`：skill `2121003`、success 100、req 0、master 10。
- ✅ 新增 `tools/Maple.Tools.SkillBookExtractor` 一次性萃取工具；`JsonSkillBookCatalog` 補 `Count` 供測試驗證，不改 JSON schema。
- 🧪 驗證：`dotnet build --nologo -v quiet` 0 warning / 0 error；Content 18 passed；Core 104 passed；Application 140 passed；Adapters 412 passed / 1 skipped；SkillBookExtractor build 0 warning / 0 error。

## 2026-07-06（P003 D3 — coupon/poll/MapleTV/beans update/monster bomb）

- ✅ `COUPON_CODE(0xE7)` 對照 Java `CashShopOperation.CouponCode`：新增 Core coupon model/repository、LiteDB/Mongo `cash_coupons` persistence 與 `CashShopService.RedeemCouponAsync`，支援 Cash/GASH、MaplePoints、item、meso，失敗回 Java cash-shop fail `0xB3`。
- ✅ `GAME_POLL(0xA3)` 對照 Java `UserInterfaceHandler.InGamePoll`：此 Java tree `PollEnabled=false`，MapleForge 解析 tick/selection 後 `EnableActions` parity，保留 `GAME_POLL_REPLY(0xDB)` helper。
- ✅ `MAPLETV(0x10A)` 對照 Java 深度判定：enum/opcode 存在但 `MapleServerHandler` 無 dispatch，cash item path 也沒有 broadcaster；MapleForge 保留 payload parser + `EnableActions` parity。
- ✅ `CP_BeansUpdate(0xE1)` 對照 Java `BeanGame.BeansUpdate`：reset runtime `BeansGameSession`，送 exit beans packet + `EnableActions`。
- ✅ `MONSTER_BOMB(0xBB)` 對照 Java `MobHandler.handleMonsterBomb`：`MobStats.SelfDestructAnimation` 由 WZ `info/selfDestruction/action` 載入；job 421/422 + alive + selfD gate 後走 `CombatService.KillMobWithoutRewards`，廣播 kill animation。
- ⚠️ 邊界：coupon seed/admin tooling 與成功 UI live smoke 未做；item success / beans exit / monster kill animation S2C fixture 仍為 Java-source candidate/unverified；MapleTV broadcast、PollEnabled=true 狀態機、monster hidden gate 延後。
- 🧪 驗證：`dotnet build --nologo -v quiet` 0 warning / 0 error；Core 104、Application 140、Content 17、Persistence 7、Net 2、Tools.PacketDecoder 22、Tools.HeadlessClient 29 全綠；Adapters 412 passed / 1 skipped；逐專案總測 733 passed / 1 skipped；Core/Application 禁區 grep 無 V113 using。

## 2026-07-06（P003 D2 — ARAN combo buff + Cygnus/Snowball parity）

- ✅ `ARAN_COMBO(0x92)` 對照 Java `PlayerHandler.AranCombo` / `MapleStatEffect.applyComboBuff`：Core `Player` 新增 runtime combo count/last time，Application 對齊 Aran job gate、4 秒 reset、30000 cap、10..100 門檻與 `21000000` skill level gate，Adapter 達門檻送 `GIVE_BUFF(ARAN_COMBO)` Java-source candidate。
- ✅ `CYGNUS_SUMMON(0x91)` 對照 Java `UserInterfaceHandler.CygnusSummonNPCRequest`：job 2000 啟 NPC 1202000，job 1000 啟 NPC 1101008；腳本缺失時回 `EnableActions` 防卡。
- ✅ `SNOWBALL(0xCD)` / `LEFT_KNOCK_BACK(0xCE)` 依 PM 接受的深度判定做 handler parity：SNOWBALL 解析 team/unknown/position/stage 後 `EnableActions`；LEFT_KNOCK_BACK 在 snowball map 回 `LEFT_KNOCK_BACK(0x11A)` + `EnableActions`。完整 `MapleSnowball` event 子系統延 P004。
- ⚠️ 邊界：ARAN combo effect metadata 仍需 Skill.wz/live client 校準；ARAN/LEFT_KNOCK_BACK S2C fixture 標 `unverified`；Snowball lifecycle/timer/prize/warp/event broadcast 不在 D2 落地。
- 🧪 驗證：`dotnet build --nologo -v quiet` 0 warning / 0 error；Adapters 406 passed / 1 skipped；Core 104、Application 136、Content 17、Persistence 6、Net 2、Tools.PacketDecoder 22、Tools.HeadlessClient 29 全綠；逐專案總測 722 passed / 1 skipped；Core/Application 禁區 grep 無 V113 using。

## 2026-07-06（P003 D1 — opcode swap + reward/chest/grenade/show-exp-chair）

- ✅ 修正 Login opcode 對調：Java `RecvPacketOpcode.java` 為 `CLIENT_FEEDBACK=0x0C`、`CLIENT_ERROR=0x0F`，MapleForge 常數與 Login fixture 已對齊。
- ✅ `SHOW_EXP_CHAIR(0x24)` 對照 Java `PlayerHandler.ShowExpChair`：讀 `chairId` 後 `EnableActions`，確認為 Java parity no-op 並補 fixture。
- ✅ `REWARD_ITEM(0x6A)` / `USE_TREASUER_CHEST(0x6C)` 從純 stub 升級為解析 slot/itemId、驗來源道具、消耗箱/鑰匙、給 deterministic reward、送 inventory mutation + reward animation candidate；完整 `Etc.wz`/`StructRewardItem`/`RandomRewards` 資料源留 TODO。
- ✅ `CP_UserThrowGrenade(0x67)` 複核本 Java oracle 的 `PlayerHandler.ThrowGrenade` 為空 handler；MapleForge 保留 payload parser + `EnableActions`，不猜測廣播封包。
- 🧪 驗證：`dotnet build --nologo -v quiet` 0 warning / 0 error；Adapters 402 passed / 1 skipped；逐專案總測 714 passed / 1 skipped；Core/Application 禁區 grep 無 V113 using。

## 2026-06-19 真機 GUI Smoke 基線驗證

- 基線進圖 PASS（login→選角→進圖→NPC spawn 26→續留穩定）
- 7 項 in-game 操作驗證 PASS（移動/聊天/表情/NPC 對話×2/開背包/走 portal 換圖）
- Cash Shop FAIL（reconnect 架構 bug：需獨立 CS port 或修 OperationCanceledException 時序）
- 受限跳過 4 類（Lv1 空身：無道具/武器/SP/等級）
- 附帶發現：MongoDB provider 啟動 bug + diag13 Play! 自動化不穩定
- 下一步：增強 seed 腳本（給裝備/道具/等級）→ 補完跳過項 + 修 CS reconnect

## 2026-06-18 (session-04) — P002 三輪稽核最終修補

- ✅ P002 HEAD 口徑統一為 `cb66272+`：計畫檔、進度日誌 session-02/session-03、主任務歷程與移植狀態地圖已更新。
- ✅ Batch A `關聯commit` 回填 `a0c422c`；`_24` 修補歷程回填 `cb66272`；README 索引補齊主任務 `_14` 與本輪 `_26`。
- 🧪 驗證：殘留舊 HEAD/待填引用搜尋、`git diff --check`；本輪只改 `.md`。commit/push 見 git log。

## 2026-06-18 (session-03) — P002 二輪稽核殘留修補

- ✅ 修補主任務歷程 `2026-06-18_14_重構_P2技術債清償.md`：接手點 HEAD 改為 `cb66272+`，補齊 commits 串（見主任務歷程），計畫步驟全勾選。
- ✅ 修補 `docs/devlog/任務歷程/README.md`：補齊 `2026-06-18_16` 的 `USE_SKILL_BOOK 技能書 catalog 全鏈` 與 `2026-06-18_18` 的 `FieldLimitType enum` 索引行，16-21 標題以各檔 frontmatter 為準。
- 🧪 驗證：Markdown/frontmatter 比對與 `git diff --check`；本輪只改 `.md`。

## 2026-06-18 (session-02) — P002 技術債清償

- ✅ 完成 P002 三項技術債，10 個派工全收（8 Codex + 1 GPT-5.5 Review + 1 Sonnet 歸檔）
- ✅ T1 ENTER_CASH_SHOP(0x20)：完整 reconnect 模式切換（pending transition + CS packet loop + WarpCashShop 封包 + 30s stale expiry）
- ✅ T2 USE_SKILL_BOOK(0x4C)：ISkillBookCatalog 全鏈（SkillBookDefinition/ISkillBookCatalog/JsonSkillBookCatalog/V113SkillBookHandler），catalog 空待 Item.wz 資料
- ✅ T3 USE_CASH_ITEM(0x49) A-D 四批 30+ itemId：黑板/寵物/留言 + AP/SP/Lock/Karma/Hammer + 喇叭 8 類 + 傳送石 7 類
- ✅ 新增基礎設施：V113BroadcastPackets(SERVERMESSAGE 0x3D)、FieldLimitType(VipRock 0x40)、ISkillBookCatalog
- ✅ GPT-5.5 Review：禁區通過，2 blocker 已修（stale CS transition expiry + skill book RNG guard），2 warning 已知（tele rock map validation + RNG bounds diff）
- 測試 645 綠（+100 from 545 baseline）：Core 102 / Content 17 / App 134 / Adapters 392+1skip
- HEAD `cb66272+`
- ⚠️ 保留：SkillBook catalog 需 Item.wz 資料、Vega/旅行商人等 stub 待補、喇叭僅 map broadcast（channel/server-wide 待基礎設施）、傳送石未做 FieldLimit per-map 驗證、ENTER_CASH_SHOP 需真客戶端 smoke 驗證 reconnect

## 2026-06-18（D9 — P002 技術債批次 code review）

- ✅ 完成 `a0684f2..HEAD` 的 P002 review，覆蓋 ENTER_CASH_SHOP、USE_SKILL_BOOK、USE_CASH_ITEM 三個風險區。
- ✅ 禁區稽核 clean：Core/Application 無 `using Maple.Adapters.V113`。
- 🧪 驗證：Adapters.V113 391 passed + 1 skipped；Content 17 passed；Core 102 passed；Host.Shared build 0 warning / 0 error。
- 🧱 Findings：runtime skill-book catalog 預設空資料導致 live `USE_SKILL_BOOK` 全部 catalog miss；cash-shop pending transition 無 timeout/stale cleanup；cash teleport rock map mode 未驗 target map/eligibility 即消耗並 warp。

## 2026-06-18（D5 — USE_CASH_ITEM Batch C megaphone/broadcast）

- ✅ `USE_CASH_ITEM(0x49)` Batch C 接入：5070000 map megaphone、5071000/5072000 super/high-performance、5073000 heart、5074000 skull、5076000 item megaphone、5077000 triple megaphone、5390000-5390006/5390029 avatar fallback。
- ✅ MapleTV 5075000-5075002 依任務範圍保守 `EnableActions` stub，不消耗現金道具；avatar megaphone 先用 SuperMegaphone encoding fallback，完整 `AVATAR_MEGA` 外觀編碼待後續。
- ✅ Channel dispatch 傳入 one-based channel，handler 透過既有 `MapPackets` 做 map-level broadcast MVP；不新增真正 channel/server-wide broadcast transport。
- 🧪 驗證：focused `ChannelUseCashItemTests` 56 passed；`dotnet test tests/Maple.Adapters.V113.Tests/Maple.Adapters.V113.Tests.csproj -v quiet --nologo` 391 passed + 1 skipped；Core/Application `using Maple.Adapters.V113` 檢查無命中。
- ⚠️ 邊界：SERVERMESSAGE layout 仍沿用 D4 Java-source candidate；真 v113 client megaphone UI smoke、avatar look packet、channel/world broadcast infrastructure 待後續。

## 2026-06-18（D7 — USE_CASH_ITEM Batch D 傳送石）

- ✅ `USE_CASH_ITEM(0x49)` Batch D 接入：固定目的地 `5042000→701000200`、`5042001→741000000`；cash teleport rock `5040000/5040001/5041000/2320000` map-mode；any-door `5560000/5561000` map-mode。
- ✅ `V113UseCashItemResult` 新增 `WarpToMapId` intent，`V113ChannelConnectionHandler` 收到後走既有 `WarpAsync`；cash-item handler 不直接碰 connection/map runtime。
- ✅ player-name mode / non-map any-door mode 暫不支援，保守 `EnableActions` 且不消耗現金道具；FieldLimit / MapleLand / saved-rock / continent / event-instance checks 留 TODO 等 map field-limit 資料。
- 🧪 驗證：`dotnet test tests/Maple.Adapters.V113.Tests/Maple.Adapters.V113.Tests.csproj -v quiet --nologo` 377 passed + 1 skipped；Core/Application `^using Maple.Adapters.V113` 檢查無命中。
- ⚠️ 邊界：未跑真 v113 client teleport UI smoke；完整 FieldLimit 與 teleport-rock map eligibility 待後續資料基礎。

## 2026-06-18（D3 — USE_CASH_ITEM Batch B）

- ✅ `USE_CASH_ITEM(0x49)` Batch B 接入：Item Tag、Sealing Lock（永久/7/30/90天）、Karma/Premium Karma、SP Reset、基礎四屬 AP Reset、Vicious Hammer。
- ✅ Core 補 `ItemFlags.KarmaUse/KarmaEquip`、裝備 `ViciousHammer` 持久欄位、AP/SP reset domain helper；Core/Application 未加入 v113 adapter `using`。
- ✅ Vega、Peanut、Gas/Flower、Predict Card、Travelling Merchant、Contact Lenses、Steel Ball Box、NPC Script、Incubator 依本批範圍保持 log + `EnableActions` stub，不消耗道具。
- 🧪 驗證：`dotnet test tests/Maple.Adapters.V113.Tests/Maple.Adapters.V113.Tests.csproj -v quiet --nologo` 367 passed + 1 skipped；`dotnet test tests/Maple.Core.Tests/Maple.Core.Tests.csproj -v quiet --nologo` 102 passed；Core/Application adapter `using` 檢查無命中。
- ⚠️ 邊界：AP Reset HP/MP job-specific 公式、Vega 成功率語義、Karma item eligibility catalog、完整 cash-item 子系統與真 v113 client smoke 待後續。

## 2026-06-18（D4 — SERVERMESSAGE broadcast packet infrastructure）

- ✅ 新增 v113 `SERVERMESSAGE(0x3D)` / `AVATAR_MEGA(0x6D)` send opcode constants；`AVATAR_MEGA` 只補常數，外觀編碼留後續。
- ✅ 新增 `V113BroadcastPackets`：type 2 map megaphone、type 3 super megaphone、type 8 item megaphone（含 null/item）、type 10 triple megaphone、type 11 heart、type 12 skull，對齊 Java `MaplePacketCreator.broadcastMessage`。
- ✅ 新增 `ChannelBroadcastPacketTests` 8 個測試，驗 opcode/type、Maple string、channel-1、ear flag、item-present flag、zero-position item info 與 triple lines。
- 📚 同步 `v113-protocol-spec.md`：標記此 S2C layout 為 Java-source candidate，真 v113 client megaphone UI smoke 待 D5/後續。
- 🧪 驗證：`dotnet test tests/Maple.Adapters.V113.Tests/Maple.Adapters.V113.Tests.csproj -v quiet --nologo` 340 passed + 1 skipped。
- ⚠️ 邊界：未實作 megaphone cash item handlers、channel/server-wide broadcast transport、avatar megaphone character look packet。

## 2026-06-18（P2 Phase A — 5 MVP stub quick completions）

- ✅ 升級 5 個既有 v113 channel stub：`ITEM_UNLOCK(0x95)` 清 Equip 背包裝備 `LOCK` flag 並送完整 `MODIFY_INVENTORY_ITEM` update；`USE_TELE_ROCK(0x4E)` map mode 驗 map 存在後送 candidate `MAP_TRANSFER_RESULT` 並走既有 `WarpAsync`；`USE_SCRIPTED_NPC_ITEM(0x48)` 驗 Use 背包 slot/itemId 後消耗 1 個道具；`MOB_NODE(0xBD)` 驗目前 field mob OID 並 log node progression；`CP_UserOldAntiMacroQuestionResult(0x63)` 讀取並 log answer。
- ✅ 新增 adapter helper tests：unlock success / missing / not locked、tele-rock success / invalid map、scripted item parse + consume、mob-node existing mob；新增 Core `ItemFlags` helper 與 Application `MapService.MapExists`。
- 📚 同步 `v113-protocol-spec.md`：記錄 Phase A 行為、deferred 範圍與 `MAP_TRANSFER_RESULT` use-result candidate 尚未 live-client verified。
- 🧪 驗證：`dotnet build src/Maple.Host.Shared/Maple.Host.Shared.csproj --nologo -v quiet` 0 warning/0 error；`dotnet test tests/Maple.Adapters.V113.Tests/Maple.Adapters.V113.Tests.csproj -v quiet --nologo` 313 passed + 1 skipped；`dotnet test tests/Maple.Core.Tests/Maple.Core.Tests.csproj -v quiet --nologo` 98 passed；`dotnet test tests/Maple.Application.Tests/Maple.Application.Tests.csproj -v quiet --nologo` 134 passed；Core/Application `using Maple.Adapters` check clean。

## 2026-06-18（EventSystems 三個 MVP stub 升級 — RPS/Coconut/Beans）

- ✅ `RPS_GAME(0x80)` 從 read+EnableActions stub 升級：Core `RpsSession` / `RpsChoice` / `RpsResult`，支援 entry fee、win/lose/tie、continue、timeout、cashout；v113 `RPS_GAME(0x144)` packet builder/handler 接上。
- ✅ `COCONUT(0xCF)` 從 stub 升級：Core `CoconutEvent` 追蹤 coconut state、running flag、Maple/Story score；Application `CoconutEventService` 以 mapId lazy 建 MVP running event；v113 `HIT_COCONUT(0x11B)` / `COCONUT_SCORE(0x11C)` builder 並廣播同圖。
- ✅ `CP_BeansGameAction(0xE0)` 從 stub 升級：`Character.Beans` + Core `BeansGameSession`；START 扣 1 bean、SHOOT 扣 count，回 `LP_BeanGameShow(0x153)` / `UPDATE_BEANS(0x6A)` / `LP_BeanGameShoot(0x154)` 主要路徑。
- 🧪 驗證：Core 98/98、Adapters.V113 313 passed + 1 skipped；Host.Shared build 0 warning/0 error。真 v113 client event smoke 未跑，S2C layout 仍是 Java-source candidate/unverified。
- ⚠️ 邊界：Coconut lifecycle/team assignment、RPS Java item reward/world notice、Beans 完整中獎節奏與跑馬燈 reward gate 後續另案。

## 2026-06-18（P2 Migration Wave 4 — heavy opcode MVP stubs）

- ✅ 新增 7 個 v113 channel recv opcode 常數與 dispatch：`ENTER_CASH_SHOP(0x20)`、`CP_HiredMerchantRemoteControl(0x34)`、`USE_HIRED_MERCHANT(0x38)`、`MERCH_ITEM_STORE(0x3A)`、`ENTER_MTS(0x99)`、`TOUCHING_MTS(0xFA)`、`MTS_TAB(0xFB)`。
- ✅ MVP 行為只讀任務指定最小欄位或 no-op 後送 `EnableActions`；不新增 Core/Application model、service 或 test file，不移植 CashShop/HiredMerchant/MTS 子系統。
- 📚 同步 `v113-protocol-spec.md`，註記完整 cross-server cash shop、merchant runtime/persistence、MTS auction/cart/listing flow 後續另案；部分 Java full handler first-read 與本 migration scope MVP 欄位不同，完整移植需再以 Java source + 真 client capture 校準。
- 🧪 驗證：`dotnet build src/Maple.Host.Shared/Maple.Host.Shared.csproj --nologo -v quiet` 0 warning/0 error；`dotnet test tests/Maple.Adapters.V113.Tests/Maple.Adapters.V113.Tests.csproj -v quiet --nologo` 299 passed + 1 skipped。

## 2026-06-18（P2 Migration Wave 3 — complex opcode MVP stubs）

- ✅ 新增 6 個 v113 channel recv opcode 常數與 dispatch：`CP_UserAntiMacroItemUseRequest(0x61)`、`CP_UserAntiMacroSkillUseRequest(0x62)`、`REWARD_ITEM(0x6A)`、`ITEM_MAKER(0x6B)`、`USE_TREASUER_CHEST(0x6C)`、`MONSTER_CARNIVAL(0xD5)`。
- ✅ MVP 行為只讀任務指定最小欄位並送 `EnableActions`；不新增 Core/Application model、service 或 test file，不移植 crafting/reward/chest/anti-macro/Monster Carnival 子系統。
- 📚 同步 `v113-protocol-spec.md`，註記完整 subsystem 後續另案；此 Java tree 的 `PlayersHandler.AntiMacro` full handler 與任務指定 MVP 欄位存在差異，完整移植需再以真 client capture 或最終 Java source map 校準。
- 🧪 驗證：`dotnet build src/Maple.Host.Shared/Maple.Host.Shared.csproj --nologo -v quiet` 0 warning/0 error；`dotnet test tests/Maple.Adapters.V113.Tests/Maple.Adapters.V113.Tests.csproj -v quiet --nologo` 299 passed + 1 skipped。

## 2026-06-18（P2 Batch 2C — CashShop + AntiMacro simple opcode stubs）

- ✅ 新增 3 個 v113 channel recv opcode 常數與 dispatch：`CP_UserOldAntiMacroQuestionResult(0x63)`、`ITEM_UNLOCK(0x95)`、`COUPON_CODE(0xE7)`。
- ✅ MVP 行為只讀最小欄位並送 `EnableActions`：anti-macro answer 讀 Maple string、item unlock 讀 short、coupon code 讀 skip short + Maple string；不新增 Core/Application service。
- ✅ 同步 `v113-protocol-spec.md`，標明完整 coupon DB/reward、anti-macro state、item lock flag update 後續另案；`ITEM_UNLOCK` 在此 Java tree 實際來源為 `PlayersHandler.UnlockItem`。
- 🧪 驗證：`dotnet build src/Maple.Host.Shared/Maple.Host.Shared.csproj --nologo -v quiet` 0 warning/0 error；`dotnet test tests/Maple.Adapters.V113.Tests/Maple.Adapters.V113.Tests.csproj -v quiet --nologo` 299 passed + 1 skipped。

## 2026-06-18（P2 Batch 2B — misc medium 5 opcode stubs）

- ✅ 新增 5 個 v113 channel recv opcode 常數與 dispatch：`MOB_NODE(0xBD)`、`USE_TELE_ROCK(0x4E)`、`QUEST_ITEM(0x10C)`、`USE_SCRIPTED_NPC_ITEM(0x48)`、`CP_UserThrowGrenade(0x67)`。
- ✅ MVP 行為維持 adapter-only：讀取任務指定欄位後 `EnableActions`，`QUEST_ITEM` no-op；不新增 service、不移植 escort quest、tele-rock warp、scripted NPC item 或 grenade relay。
- 📚 同步 `v113-protocol-spec.md` P2 Batch 2B 註記；真 v113 client smoke 待後續批量驗證。
- 🧪 驗證：`dotnet build src/Maple.Host.Shared/Maple.Host.Shared.csproj --nologo -v quiet` 0 warning/0 error；`dotnet test tests/Maple.Adapters.V113.Tests/Maple.Adapters.V113.Tests.csproj -v quiet --nologo` 299 passed + 1 skipped。

## 2026-06-18（P2 Batch 2A — Event Systems MVP stubs）

- ✅ **P2 Batch 2A — Event Systems MVP stubs**：新增 3 個 v113 channel recv opcode 常數與 dispatch：`COCONUT(0xCF)`、`RPS_GAME(0x80)`、`CP_BeansGameAction(0xE0)`。
- ✅ MVP 行為只讀最小欄位並送 `EnableActions`：Coconut 讀 `short coconutId`、RPS 讀 `byte mode`、BeansGameAction 讀 `byte subType`；未新增 Core/Application event model 或 service。
- ✅ 同步 `v113-protocol-spec.md`，標明完整 Coconut/RPS/Beans event state machine 後續另案。
- 🧪 驗證：`dotnet build src/Maple.Host.Shared/Maple.Host.Shared.csproj --nologo -v quiet` 0 warning/0 error；`dotnet test tests/Maple.Adapters.V113.Tests/Maple.Adapters.V113.Tests.csproj -v quiet --nologo` 299 passed + 1 skipped。
- ⚠️ 未 commit：工作區同時出現未歸屬本批的 Batch 2B/2C opcode edits 與未追蹤任務檔；本批收尾不混入 checkpoint。

## 2026-06-18（P2 Batch 1B — 簡易 handler 8 opcode）

- ✅ 新增 8 個 v113 channel recv opcode 常數與 dispatch：`PASSIVE_ENERGY(0x28)`、`WHEEL_OF_FORTUNE(0x2E)`、`ARAN_COMBO(0x92)`、`FRIENDLY_DAMAGE(0xBA)`、`MONSTER_BOMB(0xBB)`、`HYPNOTIZE_DMG(0xBC)`、`DISPLAY_NODE(0xBE)`、`CS_UPDATE(0xE5)`。
- ✅ `PASSIVE_ENERGY` 併入既有 `CloseRangeAttack` handler；`WHEEL_OF_FORTUNE` 併入既有 `UseItemEffect` handler。
- ✅ MVP stubs 只讀任務指定欄位並放行；`CS_UPDATE` 使用既有 cash balance + Cash inventory packet，gifts/wishlist 待完整 CashShop model。
- 🧪 驗證：`dotnet build src/Maple.Host.Shared/Maple.Host.Shared.csproj --nologo -v quiet` 0 warning/0 error；`dotnet test tests/Maple.Adapters.V113.Tests/Maple.Adapters.V113.Tests.csproj -v quiet --nologo` 299 passed + 1 skipped。
- ⚠️ 未 commit：工作區已有 Batch 1A / Login 併行修改與未追蹤任務檔；需在整批收斂時避免混入不屬於單一 batch 的變更。

## 2026-06-18（P2 Batch 1A trivial no-op/stub/log opcode handlers）

- ✅ 新增 12 個 trivial recv opcode 接線：Channel `STRANGE_DATA` / `CP_UserCalcDamageStatSetRequest` no-op，`SHOW_EXP_CHAIR` read-int + `EnableActions`，`CYGNUS_SUMMON` / `SNOWBALL` / `LEFT_KNOCK_BACK` / `GAME_POLL` / `MAPLETV` / `CP_BeansUpdate` stub `EnableActions`；Login `CLIENT_ERROR` / `CLIENT_FEEDBACK` / `CLIENT_LOGOUT` log-only。
- ✅ 協定細節只放 `Maple.Adapters.V113`，並同步 `v113-protocol-spec.md` P2 Batch 1A 註記；未新增測試檔。
- 🧪 驗證：`dotnet build src/Maple.Host.Shared/Maple.Host.Shared.csproj --nologo -v quiet` 0 warning / 0 error；`dotnet test tests/Maple.Adapters.V113.Tests/Maple.Adapters.V113.Tests.csproj -v quiet --nologo` 299 passed + 1 skipped。
- ⚠️ 工作區同時存在非本批的 P2 untracked journal / handler-opcode 變更；本輪保留不回退。

## 2026-06-18（新協作流程落地 — 七條鐵則+Hook+自動推進）

- ✅ 5 輪圓桌定案（GPT-5.5/Sonnet/agy）：分工表/觸發門檻/判定標準/留痕格式
- ✅ CLAUDE.md 落地：「協作工作模式」整段替換為七條鐵則版本
- ✅ settings.json：Hook A(Edit禁.cs) + A2(Write禁.cs) + C(派工後提醒wait)
- ✅ 計畫檔範本：docs/devlog/執行計畫/_範本.md 三段式
- ✅ 自動推進模式文件化（保守原則/範圍鎖定/停止條件）
- HEAD `18ba904`，已 push 私有 master

## 2026-06-18（P1 社交完整度 20 opcode 全量移植 — 三波完成）

- ✅ **第一波快攻 5 opcode**（統籌自做）：CANCEL_DEBUFF(no-op)/AUTO_ASSIGN_AP(自動配點)/DENY_PARTY_REQUEST(組隊邀請)/PARTY_SEARCH x2(MVP stub)。commit `b3d52fc`。
- ✅ **第二波 4 系統**（ai-cli Codex x4 平行 + 統籌接線）：Alliance(0x86/0x87)/Messenger(0x72)/USE_DOOR(0x7D)/NOTE_ACTION(0x7B)。commit `5fca2cd`/`d4b05b2`/`8e7b5b7`。
- ✅ **第三波家族 9 opcode**（ai-cli Codex + 統籌接線）：REQUEST/OPEN/OPERATION/DELETE_JUNIOR/DELETE_SENIOR/ACCEPT/USE/PRECEPT/SUMMON。commit `3062d0a`/`ef29edf`。
- 📊 移植狀態：✅ 94→114（+20）、❌ 64→44
- 🧪 測試：493→515 綠（Core 82 / App 134 / Adapters 299+1skip）
- 新增基礎設施：InMemoryAllianceRepository/InMemoryFamilyRepository/LiteDbNoteRepository + 3 CentralSessionHook
- HEAD `b5ea250`，已 push 私有 master

## 2026-06-18（移植 Family 家族系統）

- ✅ 移植 v113 Family 9 opcode 主幹（不接中央 dispatch）：Core 新增 `Family` / `FamilyMember` / `FamilyBuff` / `IFamilyRepository`；Character 文件補 `FamilyId`、`SeniorId`、`Junior1/2`、`CurrentRep`、`TotalRep`。
- ✅ Application 新增 lock-based `FamilyService`、`IFamilyRegistry`、`InMemoryFamilyRepository`，支援 invite+accept、delete junior/senior、buff rep spend、precept、summon request/accept、tree split。
- ✅ Adapters.V113 新增 `V113FamilyHandler` / `V113FamilyPackets` / `IV113FamilySessionHook`，send opcode `0x56..0x60` 封包 layout 對齊 Java `FamilyPacket`，並移除 Java GM-only disable gate。
- 🧪 驗證：Adapters.V113 full test 299 passed + 1 existing skip；Core 82/82；Application build 0 warning/0 error；Host.Shared build 0 warning/0 error。
- ⚠️ 邊界：依使用者要求未修改 `V113ChannelConnectionHandler.cs` / `V113ChannelOpcodes.cs`；實際 dispatch、warp/buff effect application、family UI live smoke 待後續接線後驗證。S2C family chart/info layout 目前為 Java-source candidate，尚未真 client verified。

## 2026-06-18（移植 USE_DOOR 傳送門）

- ✅ 移植 `USE_DOOR(0x7D)` handler slice：Core 新增 runtime-only `Door`，Application 新增 thread-safe `DoorService`，Adapters.V113 新增 `V113DoorHandler` / `V113DoorPackets`。
- ✅ 語義：按 Java `mode == 0` 解為 target-to-town/backwarp；owner 或同 party 可使用，回傳 destination map/position decision，dispatch/WarpAsync 接線留給使用者。
- 🧪 驗證：Door focused 5/5、Adapters.V113 287+1skip、Core 82/82、Application 134/134 通過。
- ⚠️ 邊界：Mystic Door 建立仍待 `SPECIAL_MOVE(0x55)`；未修改 `V113ChannelConnectionHandler.cs` / `V113ChannelOpcodes.cs`。

## 2026-06-18（移植 NOTE_ACTION 留言系統）

- ✅ 移植 v113 `NOTE_ACTION(0x7B)` MVP：新增 Core `Note` / `INoteRepository`、Application `NoteService`、Persistence `LiteDbNoteRepository`、Adapters.V113 `V113NotePackets` / `V113NoteHandler`。
- ✅ 封包語義：type 0 解析 receiver/message/fame/int/cashId 後建立 note；type 1 解析 count、skip 2、逐筆 id + gainFame；`SHOW_NOTES(0x26)` 依 Java `MTSCSPacket.showNotes` 寫 mode byte `3`、count、id/from/message/korean timestamp/fame。
- 🧪 驗證：Adapters.V113 note filter 3 passed；Application build 0 warning/0 error；Persistence build 0 warning/0 error。
- ⚠️ 邊界：依使用者要求未修改 `V113ChannelConnectionHandler.cs` / `V113ChannelOpcodes.cs`，dispatch 與 DI 接線待使用者後續處理；cash note item 驗證暫不做，待 CashInventory 完整整合。

## 2026-06-17～18（P0 移植批次 — 17 opcode 全部完成）

- ✅ **P0 批次 17 個 recv opcode 全部接入 dispatch**，分 4 工作包完成：
  - C: NPC_ACTION(0xBF) relay + CHANGE_MAP_SPECIAL(0x5E) script portal + SKILL_EFFECT(0x57) 廣播 — 直接實作
  - D: DELETE_CHAR(0x0D) 刪角色全鏈(介面+兩 persistence+7 test fake) + USE_SKILL_BOOK(0x4C) MVP stub — 直接實作
  - A: 寵物系統 8 opcode — ai-cli Codex-ultra 移植（Core Pet 模型+PetService+handler+packets+14 測試）
  - B: 召喚獸系統 4 opcode — ai-cli Codex-ultra 平行移植（Core Summon+handler+packets+測試）
- ✅ 中央接線：12 RecvOp 常數 + 12 dispatch case + PetService DI + 修正 handler 參數簽名
- ✅ 移植狀態地圖建立（`docs/design/移植狀態地圖.md`）：recv.properties 164 條逐條追蹤，三輪機械式驗證
- ✅ 基礎設施：`.claude/settings.json` Agent/Workflow hook 攔截 + CLAUDE.md 兩條新鐵律（禁 Agent + run 後必 wait）
- 🧪 測試：462 → **493 綠**（Core 79 / App 134 / Adapters 280+1skip），+31 新測試，零退化
- ⚠️ ENTER_CASH_SHOP 暫緩（跨 server 設計）；USE_SKILL_BOOK 為 stub（catalog 待 Item.wz）
- ⏭️ 下一步：更新移植狀態地圖統計（✅ 77→89）、繼續 P1 移植（家族/聯盟/信差等）或真機 GUI smoke

## 2026-06-17（移植升級卷軸 USE_UPGRADE_SCROLL）

- ✅ 移植 `USE_UPGRADE_SCROLL(0x50)` MVP：Core 新增 `ScrollEffect` / `IScrollCatalog`，Application 新增 deterministic `ScrollService` 與 `HardcodedScrollCatalog`，Adapters.V113 新增 parser/handler/packet encoder 並接入 Channel dispatch/DI。
- ✅ 主幹語義：成功套 stat + 扣 upgrade slot + level+1；失敗扣 slot；白衣卷軸保護失敗扣 slot 並消耗 `2340000`；詛咒失敗移除裝備；卷軸各結果皆消耗。
- ✅ 裝備資料模型加固：`EquipEntry` 補齊 mutable equip stats，穿脫裝轉換與 `SET_FIELD` equipped item info 會保留/送出卷軸後 stats。
- 🧪 驗證：Core 65/65、Application 127/127、Adapters.V113 263+1skip 通過。
- ⚠️ 邊界：scroll catalog 仍是 hardcoded MVP + 204xxxx catch-all；裝備適用性、特殊卷軸、WZ scroll metadata、真 v113 client scroll UI/effect smoke 待後續。

## 2026-06-17（移植 USE_ITEM 消耗補藥）

- ✅ 移植 v113 `USE_ITEM(0x42)` 一般消耗補藥主幹：Core 新增 `ItemEffect` / `IItemEffectCatalog`，Application 新增 `UseItemService` 與 `HardcodedItemEffectCatalog`，Adapters.V113 新增 `V113UseConsumableHandler` 並接 Channel switch / Host DI。
- ✅ 補藥語義：驗活著、Use 欄 slot/itemId/quantity、依固定值與 MaxHP/MaxMP 百分比補 HP/MP、夾到上限、消耗 1 個 Use 道具，回 `MODIFY_INVENTORY_ITEM` + `UPDATE_STATS` + `ENABLE_ACTIONS`。
- 🧪 驗證：Core 65/65、Application 134/134、Adapters.V113 257/258（1 skip）全綠；`dotnet build src/Maple.Host.Shared/Maple.Host.Shared.csproj --nologo -v quiet` 0 警告 0 錯誤。
- ⚠️ 邊界：item effect catalog 先硬編常見 v113 補藥與 `2000000..2099999` fallback；尚未接 Item.wz 真完整效果、地圖 PotionUse field limit、consume cooldown、疾病禁止補藥與真機 GUI smoke。

## 2026-06-17（移植 CHANGE_CHANNEL）

- ✅ 移植 `CHANGE_CHANNEL(0x1F)` 單進程 MVP：新增 v113 parser/serializer 與 opcode 常數，Channel handler 收 target channel byte 後 flush inventory、保存角色文件，回設定的同一個 `ChannelIp/ChannelPort`（預設 `127.0.0.1:8585`）。
- ✅ Host 投影 `ServerInstanceOptions.ChannelIp/ChannelPort` 到 `V113ChannelOptions`，避免在 handler 硬編 7575；實際 channel port 來源確認為 `8585`。
- ✅ 同步 `v113-protocol-spec.md`，記錄 Java source map：`InterServerHandler.ChangeChannel`、`MapleCharacter.changeChannel`、`MaplePacketCreator.getChannelChange`。
- 🧪 驗證：Adapters.V113 254 passed + 1 skipped；`dotnet build src/Maple.Host.Login/Maple.Host.Login.csproj --no-restore --nologo` 0 warning / 0 error；`git diff --check` exit 0（僅 CRLF normalization warnings）。
- ⚠️ 邊界：尚未跑真 v113 client GUI smoke；buff transfer、多 channel handoff 與跨 server storage 不做，待正式多實例/多頻道設計。

## 2026-06-17（batch-5 第 8 路 item-use 收官）

- ✅ 接續非預期關閉後的 Claude Code session，恢復 `2026-06-17_01_移植_整合item-use到master.md`；確認 item-use 整合程式碼已在 working tree 且 Core 65 / Application 118 / Adapters.V113 226+1skip 全綠。
- ✅ commit 並 push `74800d6 feat(batch5): 整合第8路 item-use 系統（batch-5 收官）` 到 `origin/master`，batch-5 八路平行移植全部收回 master。
- ✅ item-use 中央接線完成：`USE_SUMMON_BAG(0x45)`、`USE_MOUNT_FOOD(0x47)`、`USE_CATCH_ITEM(0x4B)`、`USE_RETURN_SCROLL(0x4F)` dispatch、DI、result intent 處理、spawn/catch/warp/persist 流程。
- ✅ 清理 `mf-item-use` worktree、刪除 `port/item-use` branch，移除 `MapleForge-wt-p0c` 殘留目錄；最後確認 `master` 與 `origin/master` 同步、工作區乾淨。
- ⚠️ 保留：item-use 仍是 headless/unit 證據，完整真機 GUI smoke 未跑；返回卷軸 field-limit、召喚袋精準落點、捕捉 dropMessage、完整騎乘/坐騎持久化仍待後續。
- ⏭️ 下一步：補文件同步後，優先 #12 真 v113 client batch smoke（SET_FIELD/SPAWN_PLAYER/CharacterInfo 不退、CHANGE_MAP portal、batch-5 代表功能）；或另開 channel integration harness，把斷線 cleanup 的 skip 回歸測試補成真綠。

## 2026-06-12（batch-5 七系統中央整合）

- ✅ 整合 7 個已 copy 進 master 工作區的新系統：reactor、trade、Duey、guild BBS、ring/follow、npc-services、buff-items；item-use 依使用者指示不納入本批。
- ✅ 中央接線：新增 v113 recv/send opcode 常數、dispatch case、Host DI、trade/runtime player registry 登入/登出 hook、reactor 進圖/warp replay hook，並把 `FieldObjectType.Reactor` 正式加入 Core enum。
- ✅ disabled/未驗證 opcode 保留編譯但不接 dispatch：`FOLLOW_REQUEST/FOLLOW_REPLY`（recv.properties disabled，`0x7A` 撞 BuddyListModify）、`REPAIR/REPAIR_ALL`（recv.properties disabled，`0x73/0x72` 撞 PlayerInteraction/Messenger）。
- 🧪 驗證：`dotnet build MapleForge.slnx` 0 warning / 0 error；Core 61/61、Application 114/114、Adapters.V113 214/214、Persistence 6/6、Net 2/2 全綠；`git diff --check` 無 whitespace error（僅 CRLF 提示）。
- ⚠️ 保留：Owl cash-item `5230000` 尚無既有 `USE_CASH_ITEM` route 可接；trade registry 與 runtime registry 待合一；follow/repair opcode 待真機擷取；repair/Owl catalog 與 Xmas cash catalog 仍是內容缺口；本批尚未跑真 v113 client smoke。

## 2026-06-10（存檔與流程鐵律稽核）

- ✅ 依使用者要求先存檔並檢查鐵律/標準流程；新增任務歷程 `2026-06-10_13_驗證_存檔與流程鐵律稽核.md`。
- ✅ 稽核：本輪 5 任務已同步任務歷程、任務追蹤、進度日誌與 `v113-protocol-spec.md`；Core/Application 沒有新增 `Maple.Adapters.V113` 依賴，新增 opcode/parser/serializer 仍在 `Maple.Adapters.V113`。
- ✅ 驗證：Core 45/45、Application 83/83、Adapters.V113 165/165；`dotnet build src\Maple.Host.Login\Maple.Host.Login.csproj --no-restore` 0 警告 0 錯誤；`git diff --check` 無空白錯誤。
- ⚠️ 稽核保留項：既有 `Maple.Core.IO.PacketReader/PacketWriter` 是分層灰區，嚴格看不完全符合「Core 不知道封包/byte layout」的字面鐵律；本輪未新增該灰區，建議後續另開重構。
- 💾 Checkpoint commit：本地 checkpoint commit `chore: checkpoint migration slices and audit`。

## 2026-06-10（自動推進 5 任務：受傷/丟楓幣/內部傳點/背包聚集排序）

- ✅ 依使用者要求「至少推 5 個任務」繼續自動推進，完成 5 個低耦合舊 Java 行為切片，仍不等待真機整體 smoke。
- ✅ `TAKE_DAMAGE(0x29)`：新增 parser 與 `DAMAGE_PLAYER(0xB8)` encoder；Channel handler 驗傷害/怪物 oid、扣玩家 HP、回 `UPDATE_STATS(HP)` 並對同圖其他玩家廣播受傷動畫。
- ✅ `MESO_DROP(0x58)`：`DropService.TryDropMeso` 支援 10~50000 楓幣限制、餘額檢查、扣款與 player-drop 型楓幣掉落；Channel 回 `UPDATE_STATS(MESO)` 並廣播 `DROP_ITEM_FROM_MAPOBJECT`。
- ✅ `USE_INNER_PORTAL(0x5F)`：解析 skip/name/x/y，同圖驗 portal 名稱後更新玩家 runtime 座標，回 `CURRENT_MAP_WARP(0xC8)`。
- ✅ `ITEM_GATHER(0x40)` / `ITEM_SORT(0x3F)`：Core `Inventory` 新增依 itemId 重新排列主幹；Channel 更新角色背包快照並送 Java 對應完成封包。
- 🧪 驗證：Core 45/45、Application 83/83、Adapters.V113 165/165；`dotnet build src\Maple.Host.Login\Maple.Host.Login.csproj --no-restore` 0 警告 0 錯誤。
- ⚠️ 邊界：本批仍是 headless/單元驗證；受傷反傷/fake dodge/buff 交互、楓幣地形落點/拾取權限時間、inner portal 距離反作弊、背包 cash/pet 特殊排序與真機 UI smoke 待後續補齊。

## 2026-06-10（自動推進 5 任務：角色資訊更新/黑板/自動補藥/傳送石）

- ✅ 依使用者要求「至少推 5 個任務」，延續玩家體感與低耦合功能線，完成 5 個舊 Java 行為切片。
- ✅ `UPDATE_CHAR_INFO(0x97)`：Core 保存個人訊息、資訊頁表情、血型、生日、星座；Channel handler 解析 type 0/1/2 並持久化；`CHAR_INFO(0x36)` 改寫實際欄位。
- ✅ `CLOSE_CHALKBOARD(0x2B)`：Core `Player` 保存 runtime-only 黑板文字；Adapters 新增 `CHALKBOARD(0x9C)` 有文字/清除 layout；handler 清除並送自己+同圖其他玩家。
- ✅ `CHANGE_KEYMAP(0x7F)` pet auto-pot 短分支：parser 保留 `type/data`，Core 保存 `PetAutoHpItemId` / `PetAutoMpItemId`，handler 更新後放行。
- ✅ 傳送石清單：Core 保存普通 5 格/VIP 10 格，空值對齊 Java `999999999`；`SET_FIELD` AddRocksInfo 修正為普通 5 格再 VIP 10 格。
- ✅ `TROCK_ADD_MAP(0x60)`：新增 parser 與 `MAP_TRANSFER_RESULT(0x27)` refresh，支援普通/VIP 傳送石新增目前地圖、刪除指定地圖與 Java 明寫 map id 限制。
- 🧪 驗證：Core 43/43、Adapters.V113 154/154 通過；整合 build 另於本 session 收尾執行。
- ⚠️ 邊界：本批仍是 headless/單元驗證；黑板開啟依賴 `USE_CASH_ITEM` 現金道具，寵物自動補藥實際施放依賴寵物系統，`FieldLimitType.VipRock` 尚未移植，真機 UI smoke 待整體依賴完成後批量跑。

## 2026-06-10（移植怪物書封面 MONSTER_BOOK_COVER）

- ✅ 延續角色資訊卡相鄰功能：移植舊 Java `PlayerHandler.ChangeMonsterBookCover` 與 `MonsterBookPacket.changeCover` 基礎流程。
- ✅ 落地：Core `Character.MonsterBookCover` 與更新方法；Adapters.V113 新增 `MONSTER_BOOK_COVER(0x32)` parser、`MONSTERBOOK_CHANGE_COVER(0x4E)` encoder；Channel handler 驗 `0` 或 `238xxxx` 怪物卡 itemId 後持久化角色文件並回封面變更封包；`SET_FIELD` MonsterBookInfo 會寫入 cover itemId。
- 🧪 驗證：Core 40/40、Adapters.V113 145/145；`dotnet build src\Maple.Host.Login\Maple.Host.Login.csproj --no-restore` 0 警告 0 錯誤。
- ⚠️ 邊界：完整 monster book cards 尚未移植，所以本輪未驗「是否實際持有該卡」；角色資訊卡需要 card itemId → mobId 對照，目前仍保守寫 0，待 ItemInformationProvider/MonsterBook catalog 補齊。

## 2026-06-10（移植角色資訊 CHAR_INFO_REQUEST）

- ✅ 延續玩家體感/互動切片：移植舊 Java `PlayerHandler.CharInfoRequest` 與 `MaplePacketCreator.charInfo` 的基礎角色資訊卡。
- ✅ 落地：Adapters.V113 新增 `CHAR_INFO_REQUEST(0x5B)` parser 與 `CHAR_INFO(0x36)` encoder；Channel handler 只回同地圖目標，依 Java 流程先送 `EnableActions`，再送角色等級/職業/人氣/公會名稱與空的未移植欄位。
- 🧪 驗證：`dotnet test tests\Maple.Adapters.V113.Tests\Maple.Adapters.V113.Tests.csproj --no-restore` 通過 138/138；`dotnet build src\Maple.Host.Login\Maple.Host.Login.csproj --no-restore` 0 警告 0 錯誤。
- ⚠️ 邊界：MonsterBook/Pet/Mount/Wishlist/Medal/Marriage/Family 尚未移植，封包段先以 Java 相容空值保留；目前 `PacketWriter.WriteMapleString` 仍是單位元組字串，未加入公會/聯盟文案先不寫舊 Java 中文預設字。

## 2026-06-09（移植玩家技能宏 SKILL_MACRO）

- ✅ 延續鍵盤/技能操作設定切片：移植舊 Java `PlayerHandler.ChangeSkillMacro` 與 `MaplePacketCreator.getMacros`。
- ✅ 落地：Core 新增 `SkillMacroRecord`、`Character.SkillMacros` 與更新方法；Adapters.V113 新增 `SKILL_MACRO(0x68/0x7A)` parser/encoder；Channel handler 收到變更後更新並持久化角色文件，登入後有 macro 才送 `SKILL_MACRO`。
- 🧪 驗證：Core SkillMacro 4/4、Adapters SkillMacro 5/5 targeted 測試通過；完整 Core 37/37、Application 79/79、Adapters.V113 135/135；`dotnet build src\Maple.Host.Login\Maple.Host.Login.csproj --no-restore` 0 警告 0 錯誤。
- ⚠️ 邊界：本輪只保存/送出技能宏設定，不驗證 skill id 是否已學會；技能施放、cooldown、buff 語義仍由既有技能/戰鬥系統與後續真機 smoke 覆蓋。

## 2026-06-09（移植玩家鍵位 CHANGE_KEYMAP / KEYMAP）

- ✅ 延續低耦合玩家體感/設定切片：移植舊 Java `PlayerHandler.ChangeKeymap` 一般鍵位分支與 `MapleKeyLayout.writeData`。
- ✅ 落地：Core 新增 `KeyBindingRecord`、`Character.Keymap` 與 key binding 變更方法；Application 建角套 Java `saveNewCharToDB` 預設 keymap；Adapters.V113 新增 `CHANGE_KEYMAP(0x7F)`、`KEYMAP(0x163)` parser/encoder，Channel 登入後送 `KEYMAP`，收到變更後更新並持久化角色文件。
- 🧪 驗證：Core Keymap 3/3、Adapters Keymap 4/4、Application Keymap 1/1 targeted 測試通過；完整 Core 33/33、Application 79/79、Adapters.V113 130/130；`dotnet build src\Maple.Host.Login\Maple.Host.Login.csproj --no-restore` 0 警告 0 錯誤。
- ⚠️ 邊界：`CHANGE_KEYMAP` 短封包 pet auto-pot 分支先放行不改資料；真機鍵位 UI smoke 尚未做。

## 2026-06-09（移植玩家道具效果 USE_ITEMEFFECT）

- ✅ 延續玩家體感切片：移植舊 Java `PlayerHandler.UseItemEffect` 的基礎道具效果視覺同步，並接上 `CANCEL_ITEM_EFFECT` 基礎清除。
- ✅ 落地：Core `Player` 新增 runtime-only `ItemEffectItemId` 與 Use/Cancel 方法；Adapters.V113 新增 `USE_ITEMEFFECT(0x2D)`、`CANCEL_ITEM_EFFECT(0x43)`、`SHOW_ITEM_EFFECT(0xBA)`，並接上 Channel handler。
- 🧪 驗證：Core ItemEffect 2/2、Adapters ItemEffect 3/3；完整 Core 測試 30/30、Adapters.V113 測試 126/126；`dotnet build src\Maple.Host.Login\Maple.Host.Login.csproj --no-restore` 0 警告 0 錯誤。
- ⚠️ 邊界：`CANCEL_ITEM_EFFECT` 目前以 `SHOW_ITEM_EFFECT(itemId=0)` 清除視覺效果，因舊 Java 是經 `cancelEffect(getItemEffect(-id))` 間接取消，仍需真機 UI smoke 確認。

## 2026-06-09（移植玩家椅子 USE_CHAIR / CANCEL_CHAIR）

- ✅ 延續玩家體感切片：移植舊 Java `PlayerHandler.UseChair` / `CancelChair` 的基礎椅子視覺同步。
- ✅ 落地：Core `Player` 新增 runtime-only `ChairItemId` 與 Use/Cancel 方法；Adapters.V113 新增 `USE_CHAIR(0x23)`、`CANCEL_CHAIR(0x22)`、`SHOW_CHAIR(0xBD)`、`CANCEL_CHAIR(0xC6)`，並接上 Channel handler。
- 🧪 驗證：Core Chair 3/3、Adapters Chair 4/4；完整 Core 測試 28/28、Adapters.V113 測試 123/123；`dotnet build src\Maple.Host.Login\Maple.Host.Login.csproj --no-restore` 0 警告 0 錯誤。
- ⚠️ 邊界：本輪只做基礎坐椅子/取消椅子與背包持有檢查；舊 Java 的釣魚椅、飛行椅 mount buff、反作弊封鎖分支仍待後續系統補齊。真機椅子 UI smoke 尚未做。

## 2026-06-09（移植玩家表情 FACE_EXPRESSION）

- ✅ 繼續 Java→MapleForge 低耦合玩家體感切片：移植舊 Java `PlayerHandler.ChangeEmotion` / `FACE_EXPRESSION(0x2C)`。
- ✅ 落地：`V113ChannelRecvOp.FaceExpression=0x2C`、`V113ChannelSendOp.FacialExpression=0xB9`、`V113MapPackets.FacialExpression` 對齊 Java `[opcode][charId][expression]`，並在 Channel handler 接上同地圖其他玩家廣播。
- 🧪 驗證：`dotnet test tests\Maple.Adapters.V113.Tests\Maple.Adapters.V113.Tests.csproj --filter FaceExpression --no-restore` 通過 2/2；完整 Adapters.V113 測試通過 119/119；`dotnet build src\Maple.Host.Login\Maple.Host.Login.csproj --no-restore` 0 警告 0 錯誤。
- ⚠️ 邊界：舊 Java 對 `emote > 7` 會檢查現金表情道具持有；MapleForge 目前先完成基礎表情廣播，完整持有檢查待道具使用/現金道具系統補齊。真機 UI 表情動畫 smoke 尚未做。

## 2026-06-09（移植 Give Fame + 完整移植後效能分析）

- ✅ 依使用者指示「先繼續移植，測試等完成後再做」，本輪選低耦合玩家體感切片：移植舊 Java `PlayersHandler.GiveFame` / `GIVE_FAME(0x59)`。
- ✅ 落地：Core 新增 `FameRecord` 與角色人氣節流欄位；Application 新增 `FameService`（同圖、不可自己、等級 15、每日一次、同目標 30 天、正負人氣上限）；Adapters.V113 新增 `V113FamePackets`（`FAME_RESPONSE(0x24)` 成功/錯誤/被加人氣通知）並接上 Channel handler；Host DI 註冊 `FameService`。
- 🧪 驗證：`dotnet test tests\Maple.Application.Tests\Maple.Application.Tests.csproj --filter Fame --no-restore` 通過 6/6；`dotnet test tests\Maple.Adapters.V113.Tests\Maple.Adapters.V113.Tests.csproj --filter Fame --no-restore` 通過 4/4；`dotnet build src\Maple.Host.Login\Maple.Host.Login.csproj --no-restore` 0 警告 0 錯誤。
- 👥 團隊效能分析結論：完整移植到 `.NET+Mongo` 後，最大勝率在 checkpoint/logout/storage 類持久化（舊服多表 delete/insert/update vs 新服文件聚合，推估 3x-10x+），慢連線隔離也已由 per-session outbound queue 改善；移動/聊天多半接近或小幅改善，戰鬥在 full parity 後不宜期待穩定 2x。
- ⚠️ 主要風險：目前 Mongo repository 仍是 full document replace，buddy/guild/cash 這類小改可能比 MySQL 單 row update 慢；`MapService.LoadMap` 未 cache MapData、同圖 fanout 仍 O(N²)、Field actor 未落地、Guild/Party 粗鎖與 Jint 冷啟動仍是後續壓測重點。
- ⏭️ 下一步：繼續補其他低耦合玩家體感功能（角色資訊/表情/椅子/鍵位等）或回到 #12 真機 smoke；效能定量需另開 benchmark：saveToDB vs Mongo replace/delta、同圖 1/10/50 人 fanout、LoadMap cold/warm、NPC Jint cold/warm、guild/buddy/cash 小改。

## 2026-06-09（文件同步：補回 6/6 實際進度）

- ✅ 依使用者要求同步文件狀態：補回 2026-06-06 的批次移植、技術債、效能 P0 修補、CHANGE_MAP 接手點，避免 `任務追蹤.md` / `進度日誌.md` 落後於 git log 與任務歷程。
- ✅ `任務追蹤.md` 儀表板改為保守真相：原任務 DoD 完成數 35/56；M4/M5/M6 多項功能已入 master，但只標 🟦「headless+單元已整合，待真機 smoke」，不誤勾完成。
- ✅ 明確保留下一個 gate：**#12 真客戶端 smoke**。先用 `Persistence:Provider=LiteDb` 跑 SET_FIELD/SPAWN_PLAYER/CharacterInfo 不退驗證，再驗 `CHANGE_MAP(0x1E)` 腳走 portal 出城/回城。
- ⚠️ 文件同步發現：`.live-*.pid` 仍是 tracked runtime 檔，後續可另開小修從版本控制移除並補 `.gitignore`；本次只同步文件，不處理程式或 repo hygiene。

## 2026-06-09（方法論融合 + 新 session 常駐鐵律）

- ✅ 依使用者要求，先建立備份空 commit `ab99a17 chore: backup before methodology integration` 並 push 到 `origin/master`，再開始本次文件工作。
- ✅ 新增 `AGENTS.md` 作為 MapleForge repo 的 agent 入口，要求新 session 先讀 `session-invariants`、任務歷程、架構規範、設計書與當前追蹤文件。
- ✅ 新增 `docs/specs/session-invariants.md`，收斂每次新 session 必載的北極星、任務歷程鐵律、架構邊界、Java/真客戶端證據規則、文件同步與資產邊界。
- ✅ 新增 `docs/design/MapleForge方法論融合綱領.md`，把跑跑卡丁車的真客戶端短迴路/byte-exact 證據鏈，以及黃易群俠傳的 Doc-Sync/修復經驗/工具資產化紀律，轉成 MapleForge 自己的方法論。
- 🧭 定調：MapleForge 取回的是「制度與證據方法」，不是照搬 source-less 逆向優先級、Python 目錄、8-context 外形或過早重 gate。
- ⏭️ 下一步：若後續要把這套方法從文件變成硬閘門，可再拆任務做 Doc-Sync Gate MVP、tools/README、persistence-model、PacketArchive。

## 2026-06-06（批次移植 + 技術債 + P0 修補 + CHANGE_MAP）

- ✅ **Java→.NET 批次移植大幅推進**：以 worktree 隔離 + agents 只產新檔 + 中央接線流程，連續整合 4 批共 15 個系統並 push master。
- ✅ batch-1 `115dc4a`：穿脫/商店/戰鬥/倉庫/MongoDB provider 整合，整解 build 0/0、111 測試綠；注意 host 預設 provider 曾切 MongoDB，本機無 mongod 的 live smoke 要用 `Persistence:Provider=LiteDb`。
- ✅ batch-2~4：Party/Buddy/Quest/角色屬性、Skills/Drop+EXP/RangedMagic、Guild/CashShop/多頻道聊天整合到 `95fc535` / `d8d1f57` / `c62f18a`，當時全測試 221 綠。
- ⚠️ **重要邊界**：上述 15 系統全部僅 headless+單元驗證，零真客戶端驗證。因多系統碰 `SET_FIELD` / `CharacterInfo` / `SPAWN_PLAYER`，必須先做 #12 真機 smoke，通過後才續 batch-5。
- ✅ 技術債修補：WZ parser 長字串分支修正（解鎖真實 Quest.wz/Skill.wz catalog，`89ccd51`），線上玩家 registry 收斂成 `IOnlinePlayerRegistry`（`f28bc28`），並補文件收尾 `f219355`。
- ✅ 效能稽核：三方報告指出 P0 送包錯誤（`SendAsync` 原地加密共用廣播 buffer）與慢連線隔離問題，綜整 commit `7584a40`。
- ✅ P0 修補完成：`b35e108` 修 copy-then-crypt + per-connection outbound queue；`dbb0bf1` 加 session token 防重連競態；merge `d95681c`。Adapters/Application/Maple.Net 相關測試綠。
- 🚧 **CHANGE_MAP(0x1E) 腳走 portal 換圖**：程式碼+單元測試完成並 commit `3fd97c9`，但任務 `2026-06-06_08` 仍在執行中，待真機 GUI 驗 TestHero 從 Henesys 走 portal 出城/回城不卡。
- ⏭️ 下一步：#12 真機 smoke（SET_FIELD/SPAWN_PLAYER/CharacterInfo 不退）→ CHANGE_MAP GUI smoke → 回填任務 `2026-06-06_08` 結果；通過後再考慮 batch-5。

## 2026-06-02（深夜・路線圖②後半：背包/道具 MVP-0）

- ✅✅ **背包/道具系統 MVP-0 移植完成**（任務歷程 `2026-06-02_13` + 設計 doc `docs/design/背包道具-領域分層設計.md`，commit 0cd9d3d，已 push）。使用者「請團隊繼續自動推進」。**團隊紅隊決策+設計**（Gemini MVP scoping/red-team、agy v113 背包封包事實研究；claude-ultra 跑太久卡死被 kill、Codex 帳號失效缺席）。
- 🧩 **關鍵架構決策（紅隊推翻初版）**：持久扁平 `ItemRecord` DTO（嵌 Character、LiteDB 友善、原子寫）**≠** 執行期富 `Inventory` 聚合（hydrate 於 Player、變動唯一經 Player，比照 Position 前例）；別讓 LiteDB 重建 Item/Equip 繼承樹。MVP 切線用 **rollback 安全性**分（reorder/equip 無害=先做不需 journal；use/drop/sell 改價值=需 app-journal=defer）。
- 🧩 落地：Core/Inventory(InventoryType/Item/Equip/Inventory/Inventories/ItemRecord)、Character.Items(並存 Equips)、Player hydrate/GainItem/HasItem/MoveItem/Flush、Adapters AddInventoryInfo 五袋真實序列化(取代 stub)+AddBagItemInfo+V113InventoryPackets(ITEM_MOVE 0x41/ModifyMove 0x1B)、cm.gainItem/haveItem。為控風險**增量**：背包袋(正 slot)先行、Equips/穿脫裝 unification 留 MVP-1。
- 🟢 build 綠(0 error 含 MF0001)；既有測試 Adapters 38/Application 28 不退。
- 🔬 **headless 驗證跑通**(拋棄式 console)：gain 4 道具(正確 type/slot/equip)→flush→**hydrate roundtrip 保真**→move 1→5→swap 5↔2→**merge 併量 350**→cm.gainItem 類型路由(USE/EQUIP 分流)正確。
- ⏭️ 下一步(自動推進中,多選)：背包 MVP-1 穿脫裝 / NPC 商店 / 真客戶端 GUI smoke 批量驗。

## 2026-06-02（夜・路線圖②：NPC 對話腳本引擎移植）

- ✅✅ **NPC 對話腳本引擎移植完成**（任務歷程 `2026-06-02_12`，commit 122c468，已 push master）。**團隊按能力分工設計**（Gemini red-team / agy Jint 相容研究 / claude-ultra 分層綜整；Codex 因 ChatGPT 帳號不支援 gpt-5.3-codex 兩次失敗缺席，C# 骨架由 Opus 補）。使用者強調「別照 OdinMS static 風格、要讓團隊發揮、自動推進、先不寫測試」。
- 🧩 乾淨分層落地：Core(`Character.Meso`+`Player.GainMeso`)、Application/Npcs(`NpcDialog` DTO/`INpcScriptContext` cm surface/`NpcContext` 薄橋接 queue-and-flush/`NpcConversation` session-local 免 registry)、Scripting(Jint 4.9.2 跑既有 .js、CLR sandbox off、regex 剝 load/importPackage+shim、cm 以 lowercase 委派物件 expose)、Adapters(c2s NPC_TALK 0x33/NPC_TALK_MORE 0x35、s2c NPC_TALK 0x13C、`V113NpcDialogEncoder` 對齊 Java getNPCTalk、handler 接線 oid→npcId/start/resume/warp)、DI 接上。
- 🟢 **build 綠**（0 error，含 MF0001 零-static）；既有測試 Adapters 38 / Application 28 不退。
- 🔬 **headless runtime 健檢通過**（拋棄式 console 驅動真計程車 1002000.js，用完即刪）：start→Next、多輪 Simple 選單（`getJob()==0` 折扣文字生效）、YesNo、YES→**warp=100000000 + meso 1000→900**（`gainMeso(-100)` 生效）。**跨封包狀態（status/town/selectedMap）持續、Jint 跑既有 OdinMS .js 成立（免重寫 2644 腳本）**。
- 💡 心法：①大設計取捨「開場就召團隊按能力分工」是不待開口的預設動作（使用者明顯不耐煩再被提醒，記憶已加重零容忍版）②GUI smoke 前先 headless 驗 runtime 訊號，避免昂貴 GUI 盲跑③Jint 把 C# 委派物件當 cm facade＝與命名解耦最穩、CLR off＝真 sandbox。
- ⏭️ 下一步：NPC 對話真客戶端 GUI smoke（進 Henesys 點計程車 NPC→對話框→傳送→驗換圖，座標找團隊）。技術債見任務歷程 12。

## 2026-06-02（傍晚・in-game 基礎收官：地圖物件同步 NPC）

- ✅ **地圖物件同步 NPC 移植完成**（任務歷程 `2026-06-02_10`，commit 4d39f81，已 push master）。承接上個 session 已寫完程式+測試、僅卡在權限分類器無法跑測/分發的狀態，新 session 解除阻塞收尾。
- 🟢 **跑測全綠**：Adapters.V113 38（+4 NPC 黃金向量）、Application 28（+2 FieldWorld、+1 MapService）。
- 🔍 **改用「直接比對真 Java 源」取代原計畫的 Codex 覆核**——更強的 ground truth。逐位元組核對 `MaplePacketCreator.spawnNPC/removeNPC/spawnNPCRequestController`、`SendPacketOpcode`(0xF9/0xFA/0xFB)、`MapleMapFactory.loadLife`、`MapleNPC.sendSpawnData`，欄位序/前綴/尾旗標/送包順序/id<9901000 gate **全部吻合**，不確定性消除故未再拉團隊。
- 🧩 落地：Core `MapNpc`/`MapData.Npcs`/`World.Npc(IFieldObject)`；Application `MapService.LoadNpcs`(讀 life type=n)；Adapters `V113MapPackets.SpawnNpc/Controller/RemoveNpc` + handler `SpawnMapNpcsAsync`(進場送 spawn+controller，objectId base 1000)。
- 📌 **in-game 基礎三塊（移動/聊天/地圖物件）全數完成**，路線圖①收官。
- ✅✅ **真客戶端進圖 NPC smoke 一次過**（任務歷程 `2026-06-02_11`）：diag13 全自動進 Henesys 100000000，**三道獨立判準全達標**——①server log `地圖 100000000 送出 26 個 NPC spawn` ②AI 團隊 Gemini 3.1 Pro + claude-ultra **雙人視覺交叉判讀**確認 NPC「小幫手」「計程車」正確渲染含名牌、信心度高 ③client 24s 續留未斷。**正向交叉驗證 SPAWN_NPC(0xF9)+RequestController(0xFB) 移植成立**。
- 📦 **c→s 封包同步錄到**（使用者提醒）：`tools/windower/captures/windower_packets_32256_2172.ndjson`（36 c2s+10 s2c）。發現 OS fd 重用→login(8484)+channel(8585) 兩段 c→s 進同一檔，靠檔內**兩個 getHello**(v113 0x71)切段。
- 💡 心法：截圖視覺判讀預設找團隊雙人比獨判可靠；server log 計數行＝截圖外最強獨立 ground truth，每個 in-game 移植都該補一條可 grep 的成效計數。
- ⏭️ 下一步：路線圖②「NPC 對話/商店」（移植 Jint + NPCConversationManager + c2s NPCHandler talk）；屆時點 NPC 觸發 talk，可錄 in-game c→s（NPC talk opcode）離線解碼當 fixture。後續仍有 IFieldRegistry+sink 重構、proper 每-Field objectId 配發器。

## 2026-06-02（AuthSuccess 彈回解決 + 任務歷程制度上線 + 逆向自主原則）

- ⚡ **AX210 Wi-Fi 驅動藍頻重開機**：打斷在「AuthSuccess 彈回修補的回測」當下。恢復後從檔案 mtime + capture 反推現場接手。
- 🏆 **blocker#2「AuthSuccess 登入彈回」完全解決**（封包層+視覺層雙驗證）：根因=**v113 客戶端登入成功後不主動請求世界列表，server 漏送 ServerList→互等→彈回**。修補=`V113LoginConnectionHandler.cs:154-159` 在 AuthSuccess 後主動連送 `ServerList`+`EndOfServerList`。離線解碼回測 capture 證實 s2c 從 1 個變 3 個；`diag11` 二次跑（使用者手放開不搶焦點）→ server `✓ 登入成功` + 真客戶端**前進到「選擇伺服器/頻道」畫面、世界「雪吉拉」(Scania)**，不再彈回。**全程未翻 Java**（呼應逆向自主原則）。詳見任務歷程 `2026-06-02_01`。
- 🐛 **GUI 自動化脆弱點實證**：`diag11` 第一次跑失敗（帳密注入成功但登入鈕點空、server 無 "✓ 登入成功"）＝**使用者同時操作機器搶走前景焦點**。教訓：無人值守 GUI 驗證期間機器須淨空。`0x01(Login)=0` 計數器是假訊號（handled opcode 不印），真訊號看 `✓ 登入成功`。
- 📓 **任務歷程制度上線**（`docs/devlog/任務歷程/`）：一任務一檔，鐵則=**執行前先寫死 🎯 目標再動手、執行中保持 ⏯️ 接手點最新（崩潰救命行）**。補既有兩本帳缺的「單一任務執行中」顆粒。已寫進 CLAUDE.md 開發慣例＋記憶 `task-journal-discipline`，跨 session 常駐。
- 🧭 **逆向自主原則拍板**：預設不跟 Java 取經、必要逆向團隊自主完成（Java 只當最後仲裁者且偷看要記錄）。Why=下一個目標【黃易群俠傳】只有主程式、零 server 原始碼。記憶 `protocol-re-without-source`（強化）＋`heya-qunxia-next-target`（新）。
- 🏆 **blocker#3「進地圖」協定層解決**（headless 角色進地圖）：headless client 整條鏈 charlist→select→server_ip→channel→PlayerLoggedIn→**SetField** 一次跑通，**角色 TestHero 進入地圖 100000000**（server log `已進入地圖`）。**真因不是缺角色**，而是 **server 與 seed 讀寫不同 DB**——`DataDirectory:"data"` 相對路徑在 `dotnet run --project` 下解析到專案目錄 `src/Maple.Host.Login/data`，seed 卻寫 repo-root `data`；**AutoRegister 讓登入照樣成功，長期遮蔽此 bug**（登入成功≠讀對 DB）。另 seed 帳號未設 Gender/PIN 會撞 CHOOSE_GENDER gate。修：appsettings DataDirectory 絕對化 + seed 補 Gender/SecondPassword。詳見任務歷程 `2026-06-02_02`。
- 💡 **心法**：headless 協定客戶端＝最強 focus-independent 驗證利器（不碰 GUI 把整條登入→進圖自動跑通，比真客戶端穩定可重複）——黃易那邊也該先建等價 headless。
- 🐛 **發現後續清理項**：客戶端正常斷線時 server 在 `MapleSession.RunAsync`(MapleSession.cs:85) 拋例外、`TcpChannelListener` log `fail:`+stacktrace → 應辨識為正常斷線記 info。
- ✅ **斷線優雅處理修補**（task `2026-06-02_03`）：`MapleSession.RunAsync` 把讀迴圈 I/O 包 try、`when` 分流 `EndOfStreamException/IOException/SocketException/ObjectDisposedException`→正常斷線記 info，真錯誤仍上拋 listener 記 Error（一處修同涵蓋 login+channel）。順手修 2 個自 11:13 blocker#2 修補以來潛伏紅掉的整合測試（登入後主動 ServerList 導致讀取錯位）。Adapters.V113 29/29 綠 + live 斷線 log 噪音歸零。
- ⏸️ **真客戶端視覺進圖最後一哩中斷**（task `2026-06-02_04`）：寫了 `diag13-enter-game.ps1`（登入→續點世界/頻道/選角/進圖），但**卡在 windower 鍵盤注入「每字元疊兩次」的 race**——帳號變 `tteessttuusseerr`→CHOOSE_GENDER 對話框，到不了世界選擇（連兩次重現，非焦點問題）。**單一阻塞＝windower 注入疊字**（Codex binary/injection 本行）。正面：真客戶端證實能正確處理 CHOOSE_GENDER 對話框。
- ✅ **windower 注入疊字硬化完成（委派 Codex，task `2026-06-02_05`）**：根因＝synthetic WM_CHAR 路徑與 SendInput 路徑雙送（共用 g_keyboardQueue 雙索引）。Codex 兩輪修：①停用 synthetic 注入 gate；②修回歸（`g_keyboardWorkerActive` 重入旗標含記憶體掃描太晚清→後續批次被 drop→改送鍵階段返回即 re-arm）。**我 live 驗證：account='testuser' 單次、5 批全 consumed、每字一次、登入成功**。GUI 自動登入現在可靠。分工心法：Opus 出根因假設+log 鐵證+live 閘門，Codex（C++本行）動刀；windower 的 loaded/consumed/dropped log 是跨 agent 秒定位關鍵。
- 🚧 **真客戶端視覺進圖（task `2026-06-02_04`）續攻中**：登入已穩→到達世界選擇「雪吉拉」+頻道條出現，但頻道/選角/進入鈕座標仍在自繪 UI 上逐次逼近（每輪 4 分鐘）。暫停盲磨、回報使用者決策。
- 🏆🏆 **真客戶端全自動進地圖達成（task `2026-06-02_04`，可重現 2 次）**：`diag13-enter-game.ps1` 無人值守跑完整鏈 login→世界(雪吉拉)→頻道(CH.1+確認鈕)→選角(TestHero+選擇角色鈕)→**server `已進入地圖 100000000`**。
- 👥 **團隊協作大勝（呼應使用者三度提醒「別單打」）**：卡關的自繪 UI 座標靠 **Gemini 3.1 Pro + claude-ultra 多 AI 視覺判讀 + 使用者實玩確認 + 精細座標網格疊圖** 一兩輪突破（對照我一個人盲試三輪全敗）。記成常駐偏好 `default-collaborate-with-ai-team`。確定性點擊模式(單擊選取→按確認鈕)＋「動作後驗 server 訊號再決定」最穩。
- 🐛 **發現 GUI 自動化 vs 使用者看圖搶焦點**（拆開做）；**kbd.txt 寫鎖間歇競爭**（windower 輪詢 vs Set-Content，疑 Codex re-arm 後加劇）。
- ⏭️ 下一步見下方 in-game 推進。

## 2026-06-02（in-game：真客戶端進地圖且續留 + SET_FIELD 補欄位 + 多條工作模式硬規則）

- 🏆🏆🏆 **真客戶端進地圖且「續留」成功（task `2026-06-02_06`）**：原本進圖 ~4 秒彈 `error code 38（EOF/到達檔案結尾）` 對話框自斷線。**團隊（claude-ultra + agy）對 Java 逐欄破解** → 我方 `SET_FIELD/addCharacterInfo` 缺欄位：**CRand 4→3 int、AddEquipItemInfo 全重寫（位置先寫1byte絕對值→type、15 short、incSkill byte、itemExp int、tracking long）、AddRingInfo 3→4 short、AddInventoryInfo marker 8→7**。修正後 live 驗：**24 秒全程 client 存活、channel 不斷、窗 class=MapleStoryClass、server 無「離開地圖」；`enter-map-6.png` 見 TestHero 在 Henesys 含 HUD/NPC對話/技能列**。SET_FIELD 經真客戶端續留行為神諭驗證。
- 🐛 **登入大小寫坑**：注入出大寫（server 收 `帳密錯誤 'TESTUSER'`）→ 密碼 BCrypt 大小寫敏感不符。diag13 加 `ClearCapsLock` 暫解；治本應 Codex 讓 windower 注入 case-exact。（使用者看畫面糾正「帳密錯誤」+ 建議全程截圖診斷，記 `gui-test-record-and-diagnose-visually`。）
- 📜 **多條工作模式硬規則入 CLAUDE.md**：①預設找團隊討論（使用者三度提醒，記 `default-collaborate-with-ai-team`）②無人值守＝動作→驗訊號→分支 ③升 ultra/xhigh 需團隊全數同意、僅限大難題、解完調回（記 `escalate-ultra-mode-needs-team-consensus`）。
- 🧹 截圖定時清理（使用者要求）：diag-shots 78→42MB。
- ⏭️ 下一步（in-game 續攻）：行走（windower DInput 方向鍵）→ NPC 對話 → 攻擊。

## 2026-06-01（能力圓桌 → workflow §2 改版）

- **重跑「能力圓桌」**（3 輪實際多 AI 跨評，非 Opus 腦補）：R1 各 AI 對 8 能力軸（A 實跑/B 底層逆向/C 量產/D 架構/E 品質/F 研究/G 脈絡/H 成本速度）誠實自評 → R2 互評指出彼此高估/低估、逐軸投票「真正第一」 → R3 收斂三大分歧。
- **修正方法論**：過去圓桌讓 AI 競標 M3-8/M4/M5 具體功能 → 改為**先 profiling 能力領域、再依任務性質路由**（記入記憶 `roundtable-capability-not-feature-bidding`）。中途使用者於舊框架第 7 輪喊停糾正。
- **三大產出**（已寫進 `workflow.md` §2，狀態升 v2）：
  1. 八能力軸共識 owner 表（D 架構/F 研究為全票）。
  2. 五個 AI 的本質定位卡 + 禁區。
  3. 三個反直覺結論：①binary 逆向是全隊短板、靠流程不靠人 ②品質守門分三層（agy 源頭→Codex 第一線→Opus 最終閘門）③Kiro 誠實定位為平行備援+結構重構工兵。
- **派活判準**＋鐵則「實作主責≠驗收主責」入庫。順手修掉 §2 原本重複貼上的路由表。
- 額度（圓桌前查）：Kiro 99%、Codex 5h 66%/週 85%、agy Gemini 60%、Claude session 28%——Usage Guard 通過。
- **下一步**：能力地圖是「派活依據」；實際里程碑（M3-8/M4…）分配是下游動作，待開工時拿這張表去套。視窗化 live 驗證仍待使用者手動啟動真客戶端。

## 2026-06-01（晚・視窗化突破 + 圓桌分工改革）★存檔點（使用者重啟電腦）

### 🎯 重啟後最優先要做的事（下次 session 從這裡接）
1. **完成視窗化 live 驗證**：跑 `.\test-live.ps1 -Mode Auto`，看遊戲是否以視窗模式出現。
   - 已知：windower 注入成功（`C:\windower_inject.log` 有 `D3D8 vtable hook 完成`）
   - 卡點：之前測試沒走到 D3D 初始化（要 login→選角→進地圖才會觸發 CreateDevice）
   - test-live.ps1 已整合：[2a]建測試角色 [2b]windower hook [2]啟動client
   - **注意**：要先讓 server 跑一次建 DB，create-test-char.ps1 才找得到 `data\爸爸的楓之谷.db`
2. **繼續圓桌第 6-10 輪**（分工討論只做了 5 輪）：未決議項見下方「圓桌待續」

### ✅ 本 session 完成
- **登入成功 live 驗證**：真客戶端 `testuser` 自動建帳登入成功（server log `✓ 登入成功 account='testuser' id=1`）
- **修 locale bug**（commit 3e66ee6）：Channel Hello locale 8→6（Codex 審查發現，TMS 正確值）
- **清理 Host.Channel**（commit 3497e1b）：移除空 stub（Channel 已整合進 Host.Login）
- **修 .gitignore**（commit cee3f3a）：data/*.db 不入版控（之前誤入已移除）
- **🪟 自製視窗化 Windower**（commit 7be89aa, ab642eb）：
  - `tools/windower/`：windower.cpp（D3D8 vtable hook）+ host.cpp（WinMain 訊息迴圈）+ d3d8min.h（自製型別，無需 DirectX SDK）
  - 用 MSVC x86 cl.exe 編譯（VS18 Insiders）
  - **SetWindowsHookEx 注入成功**（不像 dgVoodoo2 被 Themida 擋）→ notepad + MapleStory 都注入到，vtable hook 完成
  - 比舊 DxWnd 好：無中文路徑 bug、可整進 repo、可擴充
- **圓桌 5 輪分工改革**（commit ea3a1b2）：
  - 任務卡新格式：執行者 + 資格要求(live-debug/static-only) + 前置探索(agy:xxx)
  - workflow.md §4 加「檢查執行者資格」步驟 → 防止 Sonnet 搶 live-debug 任務
  - 已套用 M3-8 + 全部 M4 任務

### 👥 確定的 AI 分工（圓桌共識）
- **Codex**：binary/封包/DLL injection/C++ 執行/需邊跑邊修 → 視窗化也是 Codex 主責
- **Sonnet(我)**：架構/跨層整合 owner/流程編排/使用者對話（**不要搶 live-debug/binary 任務**）
- **Kiro**：品質守門/文件/contract tests/任務格式維護
- **agy**：前置探索(WZ/算法/PoC)，輸出「可實驗假設+最小驗證步驟」，M4前1週/M5前1.5週/M6前2週交付

### 🔄 圓桌待續（第 6-10 輪，下次繼續）
- workflow「禁止自做清單」具體條文（命中 binary/client/C++injection 強制委派）
- M5 多實例隔離架構探索（agy 認為這關卡探索價值最高）
- M6 商城 Cash point + 組隊跨實例廣播切入點
- 任務追蹤 M5/M6 欄位更新
- agy 觸發條件精確化

### 📊 進度：儀表板 32/55 (58%)。M3 剩 M3-8（版本抽象，Sonnet 主責，需 agy 前置探索）

---

## 2026-06-01（深夜・死機恢復 + WZ reader 深度審查 + M3-1/2/3 修正）

- ⚡ **死機恢復**：session 意外中斷，恢復後執行完整性檢查。build server 清理（Defender 鎖定暫時問題）。
- ✅ **M2-R 收尾**（commit 1cec148）：①④done，②→M5，③tech debt，⑤CT文件化。
- ✅ **X2 補齊**（commit 0098556）：`docs/conventions.md` 存在。
- ✅ **M3-1/2/3 初版 commit**（commit 0098556）：WzFile.Open+目錄解析+字串解密+vector，2個L2測試。
- 🔍 **AI 團隊審查**：Kiro 唯讀審查（git一致、37/37全綠、無TODO）；Codex 深度二進位審查。
- 🐛 **Codex 發現 WZ reader 4大類問題**：①type3/4反了 ②type2 back-ref不完整 ③offset需XOR解密(ReadWzOffset ROL+versionHash) ④ZLZ.dll才是真實key/IV來源（0x10040/0x10060），不是固定GMS常數。
- 🔑 **重大發現**：TMS v113 WZ 加密 key 存在客戶端 `ZLZ.dll`，offset 解密需 rotate-left + version hash。WzModel.cs 原版邏輯不完整。
- ✅ **Codex 修正完成**（commit a4dadc0）：type2/3/4修正、ReadWzOffset解密、ZLZ.dll key載入(fallback到DefaultWzAesKey)、string分離(ReadWzString vs ReadWzStringBlock)、image preamble修正、ReadExtendedProperty(Canvas/Sound/Vector/Convex/UOL)、+3強測試。**真實驗證：`Eqp.img → 1000000.name = 藍色毛帽` 讀出正確！**
- 📊 **測試 40/40**（5+24+11，從37升40）。儀表板 28/55(51%)。
- ⏭️ 下一步：M3-4 IDataProvider 抽象（Core介面+WZ快取）→ M3-5 Channel host（進地圖）。

## 2026-06-01（深夜→凌晨・M3 全面推進：死機恢復+WZ reader+IDataProvider+Channel+MapService+移動廣播）

- ⚡ **死機恢復**：session 意外中斷，build server 清理，git/任務追蹤一致性確認。
- ✅ **M2-R 收尾**(commit 1cec148)：①④done，②→M5，③tech debt，⑤CT文件化。
- ✅ **X2+M3-1/2/3 WZ reader 初版**(commit 0098556)：conventions.md+PKG1 header+dir+props+strings+vectors+2測試。
- 🔍 **AI 團隊死機後完整性審查**：Kiro=37/37綠、無TODO、git一致；Codex=深度WZ binary審查。
- 🐛 **Codex 發現 WZ 4大問題 + 修正**(commit a4dadc0)：type3/4反了+offset XOR解密+ZLZ.dll key/IV+string分離+extended prop+真實「藍色毛帽」驗證。55→40測試。
- ✅ **M3-4 IDataProvider**(commit d510db8)：IDataProvider/IDataNode(Core)+WzDataProvider process級快取+WzDataNode adapter+4測試。
- ✅ **M3-5 Channel host**(commit f420980)：TcpChannelListener+IChannelConnectionHandler+V113ChannelOpcodes+V113ChannelPackets.SetField(完整addCharacterInfo)+V113ChannelConnectionHandler PLAYER_LOGGEDIN→進地圖。44→44測試。
- ✅ **M3-6 地圖載入+WZ子目錄遞迴讀取關鍵修正**(commit b31ff79)：MapData/MapPortal/MapFoothold(Core)+MapService.LoadMap(Application)+WZ子目錄遞迴讀取bug fix(子目錄children未讀取)+Henesys portal/foothold/spawn驗證。55測試。
- ✅ **M3-7 移動廣播**(commit afe6e28)：IMapSessionRegistry+InMemoryMapSessionRegistry(Application)+V113MapPackets(SPAWN_PLAYER簡化/REMOVE_PLAYER/MOVE_PLAYER廣播)+V113ChannelConnectionHandler廣播流程。55測試綠。
- 📊 **儀表板 32/55 (58%)**。M3 剩 M3-8(版本抽象提取,L-sized,需圓桌)。
- ⏭️ 下一步：請使用者確認 M3-8 方向後開工（或先做 live 驗證 M3-5~7）。

## 2026-05-31（深夜・M2-4 核心 + 並行 Codex 審查 + 記憶體事故）
- 💾 存檔點：所有 docs 已併入 MapleForge repo（git tag save-2026-05-31）。
- ✅ **M2-4 核心**(commit d395965, Opus)：挖舊 Java 登入流程(getAuthSuccess/serverlist 結構、LOGIN_PASSWORD=[opcode][帳號][密碼])。實作 `PacketReader`、`AuthSuccess` 封包、handler 接 `AuthService`(autoRegister)→登入成功/失敗、DI 接好持久層+驗證。整合測試升級成「合成客戶端→autoRegister→AuthSuccess」全自動 E2E。33 測試綠。剩 SERVERLIST + live。
- 🔍 **並行 Codex 唯讀審查**(守規矩沒改檔)：抓到 autoRegister race、多實例 DI 單例(M5重要)、Account port 污染、帳號名正規化、缺LiteDB/並發測試 → 記為 M2-R backlog。
- 🧹 **記憶體事故**：`dotnet test` 撞 OutOfMemoryException——非全域不足(還有12GB)，是**累積的 11 dotnet+11 MSBuild build server + 12 個殭屍 codex agent**(kill不確實的後遺症)。`dotnet build-server shutdown` + 殺 codex 殭屍後恢復。教訓：長 session 要定期清 build server/agent 殭屍。


---

## 2026-05-31（晚・資料層圓桌 + 首次成功委派 M2）
- 🗳️ **資料層圓桌(5輪)**：使用者「不綁MySQL、效能為主」→ 定案 **文件模型 + LiteDB**(~86%共識，連PG派Codex/Mongo派Sonnet都讓步)。每實例一.db檔=呼應多實例；角色=文件Load/Save原子；交易用Saga；Repository留退路換Mongo/PG。設計書§4.4更新。
- ⚠️ **協調事故**：先前委派Codex(MySQL方向)被kill但沒真停，自行做完MySQL M2-1/2還覆蓋任務追蹤.md。git還原回乾淨M1。教訓寫進workflow.md(premise未定不委派、kill後驗git status、叮囑別動docs)。
- 👥 **M2分配會議**：Codex/Sonnet都搶M2-1/2/3鏈→Opus裁決(能力+成本)：**Sonnet接鏈(便宜、明確)、Kiro接M2-6 analyzer、Codex審查、Opus保留M2-4/5 live**。
- ✅ **首次成功委派**：Sonnet完成 **M2-1/2/3**(LiteDB持久層+Account+IAccountRepository+BCrypt帳密驗證+AuthService)。**守護欄(只動程式碼沒碰docs)**。Opus親驗：git乾淨、build 0/0、32測試綠。commit a5c7940。儀表板19/55(35%)。
- ⏭️ 下一步：Codex審查持久層 / Kiro做M2-6 / Opus做M2-4(登入成功封包,需接host+live驗證)。

## 2026-05-31（傍晚2・AI 分工會議）
- 🤝 應使用者要求（觀察到 Codex 執行期閒置），開 5 輪 AI 分工會議（Opus+Codex+Sonnet+Kiro）。結論更新進 `workflow.md §2`：
  - **核心：按「可驗收工作塊」分，不按角色標籤分**；誰適合/有空認領、寫到測試通過，Opus 審+live驗證+整合。
  - **Opus 不可替代**：架構、使用者對話、**live 客戶端測試（唯一能碰本機）**、整合驗證、協定關鍵碼。
  - **CLI agents**：Codex=難題/複雜邏輯、Sonnet=量大明確實作(便宜5×)、Kiro=研究/品質/追蹤（皆不能 live 測試）。
  - **委派判準**：複雜度×獨立性−審核成本；省得下寫碼+測試、省不下 live 驗證。
  - **M2 分派**：M2-1/2/3/6 委派 CLI agent；M2-4/5 Opus 留(需真客戶端驗證)。

## 2026-05-31（傍晚・M1 真客戶端終極驗證 + 視窗化探索）
- 🏆 **M1 完整完成（8/8）**：真 v113 客戶端連上 MapleForge → server log `解密封包 opcode=0x17 len=2`＝**真客戶端的加密封包被我們正確解密**。這是比 L2/L3 更強的終極證明。四層驗證全綠。
- 🔧 過程排障：①「Themida 擋住」其實是**缺 `ijl15.dll`**（從備份還原，順手修好客戶端）②Gr2D「找不到顯示模式」用 **AppCompat WINXPSP3+16BITCOLOR** 解掉（640X480 旗標會改桌面解析度→已棄用）③客戶端是**啟動器+Play!/ESC** 啟動，按 **ESC 直接開始**。
- 🪟 **視窗化探索**：客戶端用 **Direct3D8**（Gr2D_DX8 引用 d3d8.dll）→ 試 **dgVoodoo2** d3d8 wrapper 強制視窗化。桌面有維持 2560x1440（方向對），但 **Themida 反竄改偵測到非原版 d3d8.dll → 靜默終止程序**。→ **結論：此 Themida packed 客戶端擋外部 wrapper，視窗化無法用 wrapper 達成**。已還原（移除 d3d8.dll/dgVoodoo.conf）。視窗化留待「非 Themida 客戶端 / 未來版本」。
- 🧹 全程清理乾淨，桌面解析度還原。儀表板 16/55 (29%)。

## 2026-05-31（下午・開工 M0）
- 🔧 環境確認：機器為 **.NET 10**（無 .NET 8）→ 拍板目標框架改 `net10.0`（現行 LTS），global.json pin 穩定版 10.0.201。git 2.53 ✓。MySQL 未裝（M2 前置）。
- ✅ **X1**：`MapleForge/` git repo init（commit 39100f4）。
- ✅ **M0 完成（5/5）**：11 個分層專案（`.slnx`）、依賴方向由外往內、`Host.CreateApplicationBuilder` 裝配、`ServerInstanceOptions`+appsettings、MEL logging、`TcpLoginListener` stub。
- 🧪 M0 驗收：Host 啟動、讀到實例設定、監聽 0.0.0.0:8484、**port 探測 True**、連線被接受記錄。
- 📄 應使用者要求建立 `workflow.md`(v1)：自動推進/Usage Guard/共識分級/Approval Queue/Phase 邊界/圓桌規範。
- ✅ **X5（行為神諭）**：翻舊 Java 萃取 v113 協定規格 → `MapleForge/docs/v113-protocol-spec.md`。關鍵：MAPLE_VERSION=113/patch="1"、AES-OFB key+funnyBytes、getHello 握手格式、send cipher 用 0xFFFF-113 / recv 用 113、opcodes（LOGIN_PASSWORD=0x01、LOGIN_STATUS=0x00…）、登入失敗封包。
- ▶️ 進入 M1（管線打通）：規格就緒。
- ✅ **M1-3（cipher port，最高風險）**：C# 重現 AES-OFB（`Adapters.V113/Crypto/`：MapleAesOfb + V113CipherFactory + 常數）。逐 byte 照抄 AES key/funnyBytes，嚴格保留 Java byte 截斷語意。建 `IPacketCipher`/`IVersionCipherFactory`（Versioning 層，review must-fix：方向化 cipher）。
- 🧪 cipher 測試 15 項全綠（round-trip 含 0x5B0 邊界、連續封包 IV 演化、header 編解碼、自我 check、工廠雙實例）。**註**：結構測試證明自洽；bit 級對真客戶端待 X4 真封包或 live client（M1-4 未完全達標）。
- 💡 設計「全自動自我測試」策略（使用者提問）→ `MapleForge/docs/test-strategy.md`。四層金字塔，關鍵洞察：**舊 Java cipher ≡ 真客戶端**，故用舊 Java 當預言機可全自動關閉 bit 級盲點。
- ✅ **X4（黃金框架）+ M1-4（cipher 黃金測試）**：機器有 Java 21 且舊 `MapleAESOFB.class` 還在 → 寫 `tools/oracle/GoldenVectors.java` 跑舊 cipher 產 byte 級向量 → C# 5 項黃金測試逐 byte 相符（共 20 測試全綠）。**cipher 對真客戶端 bit 級相容已被機器證明，不需手動開客戶端。**
- ✅ **M1 pipeline（M1-1/2/5/6/7）**：讀舊 MINA encoder/decoder + MapleServerHandler 確認 framing（getHello 開頭 short 是長度前綴、未加密；收 = 4-byte header→Check→ReadLength→body→解密→LE opcode）。實作 `Maple.Net`(MapleSession + IConnectionHandler + 重構 listener)、`Maple.Core/IO/PacketWriter`、`Adapters.V113/Login`(opcodes + getHello + getLoginFailed + V113LoginConnectionHandler)、Host.Shared 註冊。
- ✅ **L3 整合測試**：真 loopback socket，假客戶端走完整握手→送 LOGIN_PASSWORD→收登入失敗。**21 測試全綠**。M1 功能完成且全自動 E2E 驗證。
- 🧪 試 M1-8 真客戶端 live：背景起 server（聽 8484 OK）→ 啟動真 `MapleStory.exe 127.0.0.1 8484`。**發現客戶端被 Themida 保護殼包裝**：視窗卡在 `Themida - oreans.com` 對話框、從未連線（CPU≈0、零 TCP）。→ AI 無法可靠自動啟動真客戶端（撞反竄改殼）。
- 📌 修正：例行驗證靠 L1+L2+L3（已綠、不需客戶端）；L4 真客戶端改「使用者手動啟動 + 機器 server 端觀察」。不繞 Themida。已殺乾淨進程。
- ⏭️ M1 剩 M1-8（改為 watch-server 觀察式）。Phase 邊界 M1→M2，待使用者確認方向。

## 2026-05-31（上午・規劃）
- 📐 完成架構設計書 `重構架構設計書.md`（C#/.NET 8 + 版本抽象 + 多實例兩大支柱）。
- 🔬 經 5 輪 AI 對抗式 review，設計書升級 v0.2（§0.5 修訂摘要）：v113 先具體、抽象延到 M3、零 static 降級為分層稽核、cipher 介面方向化、誠實標註單進程無故障隔離、客戶端多開升為 M2.5 workstream。
- 🗂️ 建立任務追蹤清單 `任務追蹤.md`（54 任務，X + M0~M6）。
- ✅ 確立追蹤法：markdown 為真相來源 + git commit 帶任務 ID + 本日誌記敘事。
- ⏭️ 下一步：開工 **M0**（Solution 骨架）。
